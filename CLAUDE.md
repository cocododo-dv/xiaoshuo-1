# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Product Scope

Single-machine, single-author long-form novel writing system (Chinese-language product). The root `README.md` (Chinese) is the product-truth entry point and `docs/README.md` indexes all documentation — the operator manual documents only the React workbench; dated plans/evidence describe their original run, not the current contract. Product mainline: create work → 10-step snowflake ideation → materialize chapters/scenes → per-scene AI drafting or manual writing → author review promotes authoritative text → final approval in 成稿中心. Explicitly *not* a multi-user SaaS, HA service, or automatic publication arbiter.

**2026-09 subtraction (batch 1)** removed, wholesale: the legacy Vue frontend (`frontend/`), the outcome-governance / blind-evaluation / hidden-benchmark cluster, the knowledge-promotion + versioning + vector-alias / reindex / verify layer and its 知识/索引/域 consoles, the interop (bundle-worksheet) center, the 长篇控制塔 backend (anchors, chapter contracts, audits, structure guidance), the legacy `style_profile` extractor and its six rule tables, the advisory narrative overlays (theme anchor, tension curve, foreshadow lifecycle, character arc/psychology, relationship matrix, voice fingerprint, POV coloring, style-drift guidance, reverse causal skeleton, work profiles), chapter runtime backfill/manual-hold, project backtrack items, scene quality contracts / auto-rewrite, author structure extraction / discovery drafts, writer-review LLM diagnosis nodes, writer-room / author-desk, and most operator tools (only `reset_author_state`, `db_backup`, `sync_prompt_templates`, `raise_llm_output_budget`, `chroma_smoke` remain). A few thin v1 primitives were deliberately kept because the test suite builds on them even though React never calls them: `POST/GET /api/v1/projects|chapters|scenes`, `GET /api/v1/projects/{id}/dashboard`, the v1 `snowflake` route, `POST …/chapters/{id}/run` (sync), and `POST /api/v1/scenes/{id}/run/full`. Migration `20260904_0083` drops their tables irreversibly. Do not re-introduce these; batch 2 (simplify approvals, accounting, system config) and batch 3 (scene pipeline rewrite) are planned next. **Demo data / fake generation are retired**: there is no product demo work, no seeded 「潮汐档案」/「盐镇来信」, and no offline/deterministic stub generation. Every generation node is fail-closed — with no live LLM configured it returns a 409/502 with an `author_action` (never canned prose). The old demo seed lives on only as neutral **test fixtures** under `backend/tests/fixture_works.py` (works `work-a`/`work-b`) + `backend/tests/fixture_runtime.py` (`seed_runtime_fixture`), consumed by tests and the contract-E2E lane; the production runtime never seeds them.

## Common Development Commands

### Full Stack Lifecycle (Windows)
- Start full stack: `.\start-dev.cmd` (runs backend + frontend, opens browser)
- Stop full stack: `.\stop-dev.cmd`
- Restart full stack: `.\restart-dev.cmd`
- Reset runtime DB/artifacts but keep LLM config: `.\reset-runtime-keep-llm.cmd` (→ `scripts/reset_runtime_keep_llm.ps1 -StopServices`; distinct from the Python `reset_author_state` tool below)

On Linux use the shell equivalents: `scripts/start-all-linux.sh` / `scripts/stop-all-linux.sh` (one-shot backend + React frontend, health-probed, logs + pidfiles under `.codex-run/`), or the per-leg `scripts/start-backend-linux.sh` / `scripts/start-frontend-linux.sh` (all safe to re-run — each leg stops its own previous instance first; shared helpers in `scripts/lib/dev-lifecycle.sh`). The backend leg runs `alembic upgrade head` with **`backend/.venv/bin/python`** (not system python) and forces `NOVEL_SYSTEM_VECTOR_BACKEND=memory`; the frontend leg resolves Node in the order `NOVEL_SYSTEM_NODE_BIN` (a `bin/` dir) → nvm at `~/.nvm` (falling back to `nvm use 16` only when no default is on PATH) → `~/.local/node/bin` (plain tarball install) → `node` already on `PATH`, failing with a clear message if none is found, and always preloads `frontend-react/crypto-polyfill.cjs` via `NODE_OPTIONS --require` (a no-op on Node ≥ 18; needed on Node 16, the ceiling of the old CentOS 7 / glibc 2.17 host, which lacks the global WebCrypto Vite 6 needs). `start-all-linux.sh` fails fast if a leg's process exits during startup and prints the tail of that leg's log. CI (`.github/workflows/ci.yml`, Ubuntu + Node 22) installs locked frontend dependencies with `npm ci`, runs unit/build gates, and uses `scripts/verify_react_e2e.sh` for an isolated migrated backend + React browser contract lane.

Default addresses: the **React frontend** `http://127.0.0.1:5174` (what `start-dev.cmd` auto-opens) and the backend `http://127.0.0.1:8000`. `start-dev.cmd` (→ `scripts/dev.ps1`) brings up the backend + React frontend in one shot: it runs `alembic upgrade head` (**no demo seed** — the production launcher never injects demo works), forces `NOVEL_SYSTEM_VECTOR_BACKEND=memory`, auto-generates a `NOVEL_SYSTEM_CONFIG_SECRET`, and writes pid/url files under `.codex-run/` (`backend.url`, `frontend.url`, `frontend-react.url`). If port 8000 is busy it scans upward and records the chosen URL in `.codex-run/backend.url`. Backend readiness is probed at `GET /ready` (a 90s timeout — if migrations are stale the probe fails and the browser never opens). The backend also exposes `GET /live` (process liveness only) and `GET /ready` (adds DB connectivity + migration revision + required-structure checks) — deployment probes should use their distinct semantics.

### Backend (Python 3.12 / FastAPI)
Located in `backend/`. **On Windows, do not activate a venv** — run backend `pytest` / `alembic` directly with the Anaconda Python on `PATH`, from inside `backend/`. The `.venv` / `.venv-wsl` dirs, when present, are Linux/WSL venvs (no `Scripts\Activate.ps1`) and are only consumed by the WSL/release lanes (e.g. `scripts/verify_wsl_strict.sh`). `pyproject.toml` sets `pythonpath=["src"]` and `testpaths=["tests"]`, so pytest/alembic must run **from `backend/`** with paths relative to it — and the PowerShell working dir resets to repo root between calls, so always `cd backend` in the same command.

CI installs `requirements.lock` with `pip --require-hashes`; `uv.lock` is the canonical cross-platform resolution. After changing `pyproject.toml`, run `uv lock --python 3.12` and then `uv export --locked --extra dev --no-emit-project --format requirements-txt --output-file requirements.lock` (the `dev` extra only — `--all-extras` would pull the `chroma` extra and its ~50 transitive packages into the hashed lock that every CI job installs), and review both files. To lift one flagged package without touching `pyproject.toml`, use `uv lock --python 3.12 --upgrade-package <name>` and re-export. The root is not an npm package; only `frontend-react/` owns a Node lockfile.

```powershell
cd backend
python -m pytest                                        # all Windows-safe tests
python -m pytest tests/test_snowflake_workspace_v2.py          # single file (path is relative to backend/)
python -m pytest -k "test_materialize"                 # by name pattern
python -m pytest -m "not chroma_integration"           # exclude Linux-only tests
```

Tests marked `@pytest.mark.chroma_integration` require real ChromaDB and are auto-skipped on Windows by `backend/tests/conftest.py`. Run them via WSL:
```
wsl -d Ubuntu-24.04 bash -lc "cd <wsl-path> && bash scripts/verify_wsl_strict.sh"
```
The other declared marker, `consistency_validation` (blueprint §17 Action B recall/precision), is **not** auto-skipped and runs in the default Windows suite.

Full Windows CI lane: `powershell -ExecutionPolicy Bypass -File scripts/verify_windows.ps1` (pytest `-m "not chroma_integration"` + the **React mainline** `frontend-react` `npm test` (vitest) + build). Full release lane (Windows CI → **React mainline contract E2E** (`scripts/verify_react_e2e.ps1`, see below) → WSL strict Chroma): `scripts/verify_release.ps1`.

GitHub Actions (`.github/workflows/ci.yml`) gates on every PR/push: **Backend Quality Gates** (ruff + architecture tests), **Backend Tests** (pytest `-m "not chroma_integration"`, 4 shards), **Backend Chroma Integration**, **Frontend Tests (React mainline)** (`frontend-react`: `npm ci` → vitest → build), and **React Contract E2E** (fresh migration + isolated seeded backend + real Chromium). WSL strict Chroma remains local in `verify_release.ps1`; React contract E2E runs both in CI and in that local release lane.

**Schema-drift guards** (`backend/tests/test_metadata_isolation.py`) — the most important non-obvious gotcha. Historical Alembic revisions are frozen explicit DDL and may not import the live application ORM. The test suite builds its schema via `Base.metadata.create_all`, while dev/prod use Alembic `upgrade head` (`auto_create_tables` defaults to `False`); the guard builds it **both** ways and diffs tables/columns/named-indexes. An ORM change without a matching migration must fail here instead of surfacing later as `OperationalError: no such column`. If it fails: write the missing migration, or declare a migration-only index in the model's `__table_args__`. Run: `cd backend; python -m pytest tests/test_metadata_isolation.py`.

### Frontend (React, primary) — `frontend-react/`
The production frontend is the maintained Vite + React 18 「潮汐工作台」 in
`frontend-react/`. `start-dev.cmd` serves it on `http://127.0.0.1:5174` and opens it
by default (landing view is `主页`, not the snowflake view).

```powershell
cd frontend-react
npm ci             # exact versions from package-lock.json
npm run dev        # http://127.0.0.1:5174
npm run build
```

Architecture rules:
- **Store layer only**: views keep the prototype's store contracts — `WsWorks` / `WsCatalog` +
  `WsTrashStore` / `WsReview` (the review store) / `WsLibrary` / `Lf7Bridge`, plus the newer
  `WsManuStore` (成稿中心, `ws-manuscripts-store.jsx`), `WsCost` (`ws-cost.jsx`), `WsEval`
  (`ws-eval.jsx`), `WsAiProviders` (`ws-ai-providers.jsx`), and the writer-side
  `WrDocs`/`WrDocVersions`/`WrRecovery` (`wr-doc-store.jsx`). These are **runtime globals attached
  to `window`** (`Object.assign(window, {...})`) from kebab-case files — grep `window.WsWorks`,
  not an ES import. Stores are API-backed with sync in-memory caches (optimistic write +
  rollback / refetch-on-failure). Writer/advanced mode gating lives here too (`ws-app.jsx`
  `WS_NAV_GROUPS`).
- **Store unit tests** (vitest, `src/*.test.jsx`, ~20 suites): cover the optimistic-write +
  rollback/refetch contract per store — `ws-works`, `ws-catalog` (incl. `WsTrashStore`),
  `ws-review`, `lf7-bridge`, and the newer `ws-ai-providers` / `ws-chapter-run` / `ws-cost` /
  `ws-scene-run` / `ws-manuscripts` / `wr-doc-store` / `wr-canonical-control` /
  `wr-content-safety-review` / `wr-recovery-center` suites.
  They `vi.mock("./lib/client.js")` and route `apiGet` by URL through the shared
  `src/test-helpers.js` `installApiRouter`; each store loads in isolation via `vi.resetModules()` +
  dynamic import (the active work falls back to a `__loading__` placeholder until a seeded
  `/api/v2/projects` resolves with a real `project_id`). Run `npm test` (or the `verify_windows.ps1`
  React gate / GitHub CI). Keep tests falsifiable — verify rollback/alert paths actually trip.
- `src/lib/client.js` owns the shared client contract (envelope / X-Idempotency-Key /
  X-Operator-Ref / `novel-system-api-base` localStorage override).
- localStorage holds only UI preferences and read caches of backend truth
  (`wr-doc:*` is a write-through cache of author-drafts); business writes all go
  through `/api/v1` + `/api/v2` endpoints.
- Test/E2E fixture works come from `backend/tests/fixture_works.py` (`seed_fixture_works`,
  project ids `work-a`/`work-b` — keep these literal ids) + `backend/tests/fixture_runtime.py`
  (`seed_runtime_fixture`, also seeds the `PRJ_DEMO_CH001` runtime fixture). These are neutral
  placeholder works for tests only — the product runtime never seeds them.
- Contract-level E2E: `cd frontend-react; node scripts/run-smokes.mjs [BASE] [API]`
  (runs the acceptance suite, `smoke-phase2..7`, `smoke-ai-settings`, and `qa2-ui`, reseeding via `python tests/fixture_runtime.py` before each suite; uses
  frontend-react's own locked Playwright install). Defaults are `BASE=http://127.0.0.1:5174/` and a **separate
  seeded backend** `API=http://127.0.0.1:8009` — not the dev `:8000`. `scripts/verify_react_e2e.ps1`
  now orchestrates this end-to-end (isolated e2e sqlite under `.codex-run/e2e/`, seeded `:8009`
  backend + React `:5174`, full process-tree teardown) and is wired into both GitHub Actions
  (`scripts/verify_react_e2e.sh`) and `verify_release.ps1` as a default gate. **Gotcha**: a fresh `alembic upgrade head` aborts on migration `20260523_0036`'s
  legacy-backup guard unless `backups/style_reference_legacy_*.json` exists; the e2e lane satisfies
  it via that migration's `STYLE_REFERENCE_REPO_ROOT` test override pointed at a placeholder backup.
- Current user and engineering documentation is indexed in `docs/README.md`; dated plans and
  evidence describe their original run and are not the current runtime contract.

### Database Migrations
```powershell
cd backend
python -m alembic current
python -m alembic heads
python -m alembic upgrade head
```

A `database operation failed` response usually means the schema is stale — run `upgrade head` first.

Migration `20260914_0085` adds the nullable `expected_reader_emotion` / `story_time` columns to `snowflake_scene_plans` (2026-09-14 阶段 J: Ingermanson's on-stage cast, time-stamp and "what the reader should feel" scene columns; on-stage cast reuses `onstage_chars_json`; the book-level `narrative_stance` lives in the `book_brief` draft and reaches drafting through the design context's binding `Narrative stance` line; it is optional, and `snowflake_staleness.semantic_payload` treats any empty top-level value as absent, so a brief approved before this phase is not flipped back to pending_review by the first re-save that carries `narrative_stance: ""`). Migration `20260716_0073` **irreversibly** rewrites historical LLM-audit payloads (prompts, drafts, model output, provider error bodies) into bounded fingerprints, and `20260904_0083` irreversibly drops the tables of the retired feature clusters — back up an existing DB before upgrading past either. `GET /ready` performs the structural check (revision + required columns); the standalone preflight tool was removed.

### Author-State Reset
Wipes all project/snowflake/chapter data while preserving reference profiles and system config:
```powershell
cd backend
python -m novel_system.tools.reset_author_state           # dry-run
python -m novel_system.tools.reset_author_state --execute --yes
```

## Environment Variables

| Variable | Default | Purpose |
|---|---|---|
| `NOVEL_SYSTEM_DATABASE_URL` | `sqlite:///./novel_system.db` | SQLAlchemy DB URL |
| `NOVEL_SYSTEM_VECTOR_BACKEND` | `memory` | `memory` (deterministic, no ChromaDB — the code default in `settings.py:get_settings`, and `start-dev.cmd` / the Linux start scripts force it as well) or `chroma` (real ChromaDB; only exercised by the `chroma_integration` lane) |
| `NOVEL_SYSTEM_CHROMA_DIR` | `./.vector_store` | ChromaDB persistence path |
| `NOVEL_SYSTEM_LLM_ENABLED` | `false` | Enable real LLM calls |
| `NOVEL_SYSTEM_LLM_PROVIDER` | `openai_compatible` | Provider key resolved against the `services/llm_providers/` adapter registry (12 adapters: `openai_compatible`, `openai`, `anthropic`, `deepseek`, `zhipu_glm`, `gemini`, `qwen_dashscope`, `moonshot`, `minimax`, `doubao_ark`, `xai`, `ollama`) |
| `NOVEL_SYSTEM_LLM_BASE_URL` | `https://api.openai.com/v1` | Provider base URL |
| `NOVEL_SYSTEM_LLM_API_KEY` | — | API key for the provider |
| `NOVEL_SYSTEM_LLM_TIMEOUT_SECONDS` | `0` (no ceiling) | Per-call LLM response timeout. `0` = wait as long as the model needs (slow long-context tasks are normal work, not faults); connection setup keeps its own finite `LLM_CONNECT_TIMEOUT_SECONDS` so an unreachable endpoint still fails fast. A positive value re-arms a ceiling, as does a per-node `timeout_seconds` in `config/models.yaml`. Connectivity probes ("测试连接") are always finite and ignore this |
| `NOVEL_SYSTEM_ADMIN_TOKEN` | — | Admin endpoint auth token |
| `NOVEL_SYSTEM_CONFIG_SECRET` | — | Secret for encrypted config snapshots |
| `NOVEL_SYSTEM_AUTO_CREATE_TABLES` | `false` | If true, bypass Alembic and `create_all` tables on startup — **dangerous**: hides schema drift (see the schema-drift guard) |
| `NOVEL_SYSTEM_LLM_AUTO_CRITIQUE_ENABLED` | `false` | §8 opt-in: layer an independent LLM editor critic on top of the rule-based pass in `orchestrator.py` (after Best-of-N, before soft QC) |
| `NOVEL_SYSTEM_LLM_EVENT_EXTRACTION_ENABLED` | `false` | §2 opt-in: extract narrative events from finished prose (`prose_event_extractor.py`) |
| `NOVEL_SYSTEM_CHROMA_COLLECTION_PREFIX` | `novel_system` | Prefix for ChromaDB collection names |
| `NOVEL_SYSTEM_CORS_ORIGINS` | `…:5173/5174/5175/8081` | Comma-separated allowed CORS origins |
| `NOVEL_SYSTEM_EXPOSE_ERROR_DETAIL` | `false` | Whether the error envelope exposes `error.details` |
| `NOVEL_SYSTEM_LOCAL_ONLY` | `true` | Loopback-only: rejects non-loopback clients and forwarded headers. Setting `false` **requires** `NOVEL_SYSTEM_REMOTE_ACCESS_TOKEN` (`create_app` refuses to start otherwise) |
| `NOVEL_SYSTEM_REMOTE_ACCESS_TOKEN` | — | Shared access token when not local-only; the browser sends it as `X-Novel-Access-Token` (frontend default injectable via `VITE_NOVEL_SYSTEM_ACCESS_TOKEN`, runtime value lives in sessionStorage). Not user auth/RBAC |
| `NOVEL_SYSTEM_LLM_DAILY_TOKEN_LIMIT` / `…_MONTHLY_TOKEN_LIMIT` / `…_PROJECT_DAILY_TOKEN_LIMIT` / `…_DAILY_REQUEST_LIMIT` / `…_MAX_CONCURRENT_REQUESTS` | `0` (every fence off) | LLM quota family, all opt-in: `0` disables a fence, a positive value arms it and the accounting layer enforces it *before* provider dispatch. A single-author desktop install fences off nobody but the author, so nothing ships armed |
| `NOVEL_SYSTEM_LLM_DAILY_COST_LIMIT_USD` (+ `…_LLM_INPUT_COST_PER_MILLION_USD` / `…_LLM_OUTPUT_COST_PER_MILLION_USD`) | `0.0` (off) | Optional daily cost cap; a nonzero cap requires nonzero unit prices |
| `NOVEL_SYSTEM_LLM_RESERVATION_RECOVERY_TTL_SECONDS` | `3600` | TTL after which orphaned LLM accounting reservations are reclaimed |
| `NOVEL_SYSTEM_SCENE_TOKEN_BUDGET_MULTIPLIER` | `0` (disarmed) | Per-scene lifecycle budget: the end-to-end token ceiling is `N × single-shot baseline` plus finite business/provider attempt caps. This was the last hard fence that shipped armed; it now joins the fence family — `0` = no scene ceiling (finite sentinel budgets, the pre-dispatch CAS gate is a no-op, so a single scene run is never blocked mid-draft). Accounting is unchanged (the ledger + 成本看板 still record every token; the dashboard shows a disarmed scene as 不限). A positive `N` re-arms `N × baseline`, and the attempt caps fall back to `config/models.yaml` `retry_budget`. Only affects **newly** initialized scenes — a scene whose immutable `scene_budget_basis_json` already exists keeps it (expand it via the author topup) |
| `NOVEL_SYSTEM_SNOWFLAKE_INPUT_TOKEN_BUDGET` | `0` (use per-template value) | Snowflake-workspace prompt **input** budget override, in estimated tokens. `0` = use each template's declared `input_token_budget` (the whole family is `24000`, a measured number — see the comment above `snowflake_generate_book_brief` in `config/prompts.yaml`). Set a positive value to tighten it for a small-context local model. Over-budget payloads are shed by relevance via `services/snowflake_prompt_budget.py` — never the step contract, the author's adopted direction, or the focused members — and the shedding is recorded in the LLM audit summary (`prompt_budget_*` fields) plus surfaced to the author as `health.generation_notice` when the ladder still can't fit |
| `NOVEL_SYSTEM_SCENE_INPUT_TOKEN_BUDGET` | `0` (use per-template value) | Prompt **input** budget override for the scene / planning / chapter template families that go through `services/prompt_builder.py` (scene-run drafting + QC + near-final review, writer scene/chapter diagnosis + revision, deep review + passage patch, author structure-extract / proposal, scene blueprint + chapter architecture + character pressure). `0` = use `max(template.input_token_budget, RUNTIME_MIN_INPUT_BUDGETS[name])` — the measured family values `24000` (scene) / `8000` (planning + passage patch) / `30000` (chapter), see the comment block above `neutral_draft` in `config/prompts.yaml`; the code floor is what protects a live install whose DB prompts snapshot still carries the pre-estimator values. A positive value replaces both (tighten for a small-context local model); it does not touch the snowflake family, which has its own `NOVEL_SYSTEM_SNOWFLAKE_INPUT_TOKEN_BUDGET` |
| `NOVEL_SYSTEM_SCENE_STRUCTURE_BRIEF` | `true` | 2026-09-13 阶段 A: render the scene's structure (form, POV, crucible, Goal/Conflict/Setback or Reaction/Dilemma/Decision, cost, must-withhold) from the raw `SceneCard.writer_brief_json` keys as the `Scene Structure (Snowflake)` fact section of the drafting bundle, the scene-blueprint source snapshot and the near-final planning snapshot (`services/scene_structure_brief.py`). `false` is a rollback switch only — it removes the section and nothing else |
| `NOVEL_SYSTEM_SCENE_DESIGN_CONTEXT` | `true` | 2026-09-13 阶段 F: render the **confirmed** snowflake design around the scene (book logline, five-sentence spine + moral premise, the chapter's act / 灾 mark / goal from the chapter table, scene position, the POV character's sheet with values / storyline, a ≤300-char excerpt of their 视角故事, on-stage one-liners, what the previous scene ended on and the next opens on) as the compressible `Scene Design Context (Snowflake)` section right after the structure brief in the drafting bundle and the blueprint snapshot (`services/scene_design_context.py`; only `approved` / `stale` step versions count, `pending_review` never becomes a fact; the referenced `step_run_id`s go into `source_version_refs` so the bundle hash follows the design). It is background, not facts: the budget compresses it to essentials before any fact section and omits it as the last soft section; `hard_qc`-kind tasks never see it. `false` is a rollback switch only |
| `NOVEL_SYSTEM_CONTENT_SAFETY_MODE` | `review` | `review` or `audit` — content-risk triage mode on publication actions |
| `NOVEL_SYSTEM_SQLITE_FOREIGN_KEYS_ENABLED` | `true` | Enforce FK pragma on SQLite connections |
| `NOVEL_SYSTEM_CORS_ALLOW_CREDENTIALS` | `true` | CORS credentials flag |
| `NOVEL_SYSTEM_STYLE_REFERENCE_IMPORT_ROOTS` | — | Allowed filesystem roots for style-reference path imports |

## High-Level Architecture

### Backend (`backend/src/novel_system/`)

FastAPI application (`api/app.py`) with one router per domain area (`api/routes/`). Each route file maps to a service in `services/`. The `db/models.py` file contains all SQLAlchemy ORM models; `db/session.py` manages the engine. The authoritative list of mounted routers is the `include_router` calls in `api/app.py`; `api/routes/__init__.py`'s `__all__` now mirrors it one-to-one, enforced by the drift guard `backend/tests/test_routes_all_manifest.py`.

**Domain layers:**
- `services/projects.py` + `services/snowflake_workspace.py` — Snowflake Method planning pipeline
- `services/snowflake_planner.py` + `services/snowflake_steps.py` — step catalog, completeness gates, materialization rules
- `services/snowflake_workspace_llm.py` + `services/llm_task_runner.py` — LLM call orchestration. Two non-obvious contracts live in the first file: 「场景规划」(`scene_details`) full-table generation is **batched server-side** (`SCENE_DETAIL_BATCH_SIZE` scenes per call, `SCENE_DETAIL_MAX_BATCHES_PER_RUN` batches per request, resuming at the first scene that still has a missing field) because one call for a whole scene table always overflows `max_output_tokens`; and every task on this path goes through `services/snowflake_prompt_budget.py` for a real, enforced **input** budget (see the env var table). Progress, partial failures, and un-fittable payloads all report through `WorkspaceLLMResult.notice` → `health_json.generation_notice` → a warning toast in `ws-snow.jsx` — a half-finished or shed generation is never reported as a plain success
- `services/llm_client.py` + `services/llm_providers/` — multi-provider LLM client built on a **pluggable adapter registry** (`llm_providers/registry.py` + `presets.py`, one adapter module per provider, 12 in total). Edit provider behavior / default base URLs in `llm_providers/`, not `llm_client.py`
- `services/scene_generation.py` + `services/qc_engine.py` — scene pipeline and quality gates
- `services/style_reference/` — reference-book style subsystem (ingest → segment → extract → synthesize → inject → validate → materialize); replaced the legacy `reference_learning.py`. See "Style Reference subsystem" below
- `services/source_safety.py` + `services/reference_safety.py` — copy guardrails (protected source terms + n-gram copy detection over reference material)
- `services/vector_store.py` — ChromaDB abstraction; swapped for in-memory store when `NOVEL_SYSTEM_VECTOR_BACKEND=memory`
- `services/idempotency.py` + `services/hash_engine.py` — idempotency contracts for LLM calls and content hashing
- `services/orchestrator.py` (+ `bundle_builder.py`, `scene_execution.py`) — the scene-run pipeline (bundle context → generate → Best-of-N → optional auto-critique → QC). The **blueprint quality-floor v2 / anti-AI-taste** cluster hangs off it: `services/literary_quality.py` (21 weighted quality dimensions incl. `perception_filter` / `self_repetition` / `conflict_too_clean`; route `/api/v1/literary-quality`), `services/best_of_n_blind_eval.py`, `services/self_repetition.py` (cross-scene n-gram + semantic guard, reuses the style-reference plagiarism engine), `services/auto_critique.py` (Reflexion-style editor pass), `services/scene_criticality.py`
- Narrative-coherence / continuity overlay (backs the scene-run pipeline): `services/narrative_event_log.py` + `services/prose_event_extractor.py` (append-only event sourcing, populated by the 成稿中心 正史 extract flow), `services/canon_continuity.py`, `services/character_continuity.py` (deterministic pronoun/identity drift checks), `services/pov_knowledge_projection.py` (who-knows-what redaction). The theme/tension/foreshadow/arc/psychology/relationship/voice/drift advisory modules were removed in the 2026-09 subtraction
- **LLM cost cluster**: `services/llm_accounting.py` (durable accounting boundary around *every* provider POST — short reservation → dispatch → settlement transactions, no DB write txn held during network I/O; quotas fail before dispatch), `services/cost_aggregation.py` + `services/pricing.py` + `config/pricing.yaml` (centralized per-provider/model price snapshots, all placeholder rates marked `is_estimate: true`), `services/llm_audit.py` (audit rows store bounded fingerprints, not payloads — see migration `0073`), route `api/routes/cost.py`
- **Persistent jobs & crash recovery**: `services/scene_run_jobs.py` (cancellable persistent scene-run jobs) + `scene_run_checkpoint.py` + `scene_run_preflight.py`, `services/chapter_runner.py` (advanced-mode 运行本章 chapter jobs with polled progress), `services/background_recovery.py` (`run_startup_recovery` wired into the FastAPI lifespan — startup scan submits durable candidates; workers own the CAS so duplicate scans from multiple ASGI workers are harmless)
- **Canonical final-text lifecycle**: `services/canonical_manuscripts.py` + `services/final_text_gate.py` + `services/chapter_approval.py` — 成稿中心 final approval requires an explicit read-through confirmation of the *server-side* text, then a body-hash confirm, then project-level `approve-final`; reopening requires a reason and the server cascades revocation of downstream approvals. `services/archiver.py` and `services/narrative_position.py` support this layer
- `services/content_safety.py` — deterministic, auditable content-risk triage on publication: blocks only unattended publication of a few compound high-risk patterns; the author acknowledges exact finding codes on the exact publication action (dark themes stay writable, ordinary genre violence is advisory)
- `services/author_preferences.py` + `services/author_instructions.py` — scoped, prompt-safe author preference/instruction summaries shared by generation, review, and bundle construction

**Key data models** (all in `db/models.py`):
- `StoryProject` / `OutlinePlan` — top-level novel project and its outline
- `SnowflakeArtifact` / `SnowflakeStepRun` — per-step artifacts and run state for the 10-step snowflake
- `SnowflakeScenePlan` / `SnowflakeSceneTriageItem` — scene-level plans and quality triage
- `SnowflakeCharacterPlan` — per-character snowflake data
- `ChapterGoal` / `SceneCard` — materialized chapter/scene production units (created by structure materialization)
- `StyleReferenceBook` / `…Paragraph` / `…Run` / `…Extraction` / `…Finding` / `…Evidence` / `…Quote` / `…Profile` / `…InjectionBinding` / `…ValidationReport` / `…BannedTerm` / `…MetricEvent` / `…FindingFeedback` — the Style Reference subsystem's table family
- `NarrativeEvent` (append-only event-sourcing log — replay events up to a scene to reconstruct entity state), `VolumeSummary`, plus blueprint-v2 columns (`SceneCard.constraint_intensity`, `SceneRunState.criticality_level` / `candidate_dispersion_score`) — the causal/foreshadow/theme overlay on the snowflake pipeline
- `LlmCall` / `LlmCallAttempt` — durable LLM accounting rows (reservation → dispatch → settlement); payload columns hold bounded fingerprints since migration `0073`
- `ChapterRunJob` / `BackgroundRecoveryLease` — persistent chapter runs and the startup-recovery lease
- `AuthorPreferenceProfile` — scoped author preferences injected into prompts

**Configuration** lives in the project-root `config/` directory (not inside `backend/`):
- `config/models.yaml` — model profiles (`local_fast`, `quality_strong`, `dual_track`), task routing (task name → provider/model/temperature/response_format), and top-level `retry_budget` + `job_runtime` (lease/idempotency TTLs). The blueprint-v2 quality floor is config-driven here too: new task-routing entries (`scene_blueprint`, `character_pressure_blueprint`, …) and decoding penalties on `stylize` (`frequency_penalty` / `presence_penalty`, §7 anti-mean sampling)
- `config/prompts.yaml` — prompt templates with `system_prompt`, `task_prompt`, `structured_schema`, and `input_token_budget` (incl. the Scene Literary Blueprint v2 / Character Pressure Blueprint templates)
- `config/allowlists.yaml` / `config/hash_contract.yaml` — domain policy files
- `config/style_reference/` — Style Reference policy files (`banned_adjectives.yaml`, `extraction.yaml`, `injection_budget.yaml` incl. the v2 intensity / few-shot-window / continuity keys and `rag_*` keys, `input_thresholds.yaml`, `sensory_lexicon.yaml`, `tolerance_floors.yaml`, `feedback.yaml`, `function_words.yaml` (closed-class function-word groups for the voice signature), `voice_baseline.yaml` (generated "general Chinese fiction" quantile baseline over the golden corpus — rebuild with `python -m novel_system.services.style_reference.voice_signature build-baseline`), `anti_plagiarism_template.txt`, `prompts/`)
- `config/pricing.yaml` — per-(provider, model) price snapshots with `effective_at`, used by cost aggregation (shipped rates are placeholder estimates, `is_estimate: true`)

Runtime LLM config can also be stored in the DB (via `SystemConfigSnapshot`) and applied on top of env vars by `settings.py:get_settings()`.

**Gotcha — the DB snapshot wins over the repo file.** Once the 系统配置 UI has saved a category, `load_active_config_payload(category)` returns the *active DB snapshot* and the repo file is never read: editing `config/models.yaml` (or `prompts.yaml`) then has **no effect on a running install**, silently. Re-importing the whole file is not a safe workaround either — it clobbers the per-node provider/model routing configured through the UI. To change one field on a live install, patch the active snapshot and activate a new version; `tools/raise_llm_output_budget.py` does exactly that for `max_output_tokens` (dry-run by default, `--execute` to apply) and is the pattern to copy.

**LLM node registry** (`services/llm_node_registry.py`) defines the catalog of all LLM-calling nodes as `LLMNodeSpec` dataclasses (node_id, model, temperature, reasoning_level, api_mode). The system config UI routes each node to a specific provider at runtime. `config/models.yaml` provides task-level defaults; DB-stored routes override them.

**Response envelope**: all API responses use `{ok: bool, data: ..., error: {code, message, details}, request_id}` (see `api/response.py`). Frontend `client.js` parses this envelope and throws `ApiRequestError` with structured fields on failure.

**Request middleware**: every request gets `request.state.request_id` (a hex prefix) and `request.state.operator_ref` (from `X-Operator-Ref` header or `"operator"`). Mutating frontend calls pass `X-Idempotency-Key` and `X-Operator-Ref` headers for idempotency and audit trails.

**Author-action pattern** (`services/author_actions.py`): when the backend detects a missing prerequisite (e.g., LLM not configured, step incomplete), it returns an `author_action` dict that tells the frontend which view to navigate to and what button to show. This avoids hard-blocking the user while still guiding them.

**Style Reference subsystem** (`services/style_reference/`, route prefix `/api/v2/style-reference`): the reference-book style engine that replaced the legacy `reference_learning.py`. Pipeline: `ingest` (import + checksum + `assess_input_size` layer gating) → `segmentation/` (paragraph typing via heuristic + LLM classifier with anchor-set calibration) → `extractors/` (four layers — `language` / `narrative` / `scene` / `theme` — over 16 sub-dimensions; each finding requires ≥2 evidence spans and rejects banned vague adjectives, enforced by Pydantic + two-level retry) → `profile_synthesizer` (16 sub-profiles → `StyleProfile` + metrics baseline) → `injection.py` (A=System-prompt / B=Few-shot / C=RAG strategies with a `style_intensity` slider and per-`TaskType` defaults; binding scope resolves scene > character > project > global) → `validation/` (three concurrent checks — `quantitative` adaptive-tolerance + `semantic` critic-LLM + `plagiarism` n-gram; sync fast-path for QC gates, async polling otherwise) → `materialization` (profile → `ReviewItem` → style rules). `metrics.py` computes hard quantitative anchors (sentence length, sensory-word frequency, dialogue ratio) as pure functions reused across extract/validate/preview. Findings are `observation` or `forbidden_pattern` (anti-samples), distinguished by `finding_kind`. Anti-plagiarism is two-layer: prevention (fixed System-prompt red-line segment) + detection (8-gram / 12-char). **Phase 3 additions** (2026-08): `rag.py` — Strategy C is a real three-granularity (sentence/paragraph/scene) vector recall, with collections `style_ref_rag_{profile_id}_{granularity}` built at synthesize time, deterministic rerank, and no LLM on the inject hot path (the RAG regression is a deterministic **style-signature A/B** in `tests/test_style_reference_rag.py` on the `memory` backend — content-independent signatures, not raw text; the real-Chroma variant is a `chroma_integration` test); `finding_feedback.py` — per-operator 👍/👎 votes recalibrate a finding's confidence ±1 tier off a frozen `base_confidence` (policy in `config/style_reference/feedback.yaml`); scene/character binding scopes (`BindingScope` PROJECT/SCENE/CHARACTER); and `cleanup.py` `purge_derived_data` — the library-delete (删书) cascade that manually deletes ~10 derived tables in FK-reverse order plus `ReviewItem` rows and each profile's RAG index (SQLite has no `ON DELETE CASCADE`), deliberately keeping `…MetricEvent`. Authoritative design: `docs/style_reference_module_design_v1.1.md`; progress log: `docs/style-reference-progress.md`.

**2026-09 style-imitation v2** (spec: `docs/style-imitation-v2-plan-2026-09-05.md`, §5 is the completion log; branch `feature/style-imitation-v2`) targets voice-level imitation, narrative-layer transfer, and cross-scene consistency — explicitly *not* an author discriminator / "how alike" metric, multi-work modelling, Best-of-N recovery, or a long-form continuation loop. Everything below degrades to a no-op for older data: a profile without the new keys, a bundle without the new sections, or a yaml without the new keys simply renders nothing extra.
- **Synthesis actually fits.** `style_ref_synthesize_profile.input_token_budget` is `39000` (measured: a full 16-sub-dimension × (6 observation + 2 forbidden_pattern) × 120-char payload is ≈31.8k under `_estimate_synthesis_input_tokens`; the old `5000` squeezed statements to ~56 chars or failed outright). `_fit_synthesis_payload_to_budget` degrades globally — drop optional metrics → cap statements at 80 then 56 chars → rotate out findings by confidence / evidence — but always keeps ≥1 `observation` + ≥1 `forbidden_pattern` per sub-dimension. The payload carries run-scoped `anchor_quotes` (≤2 × ≤60 chars per sub-dimension, mechanism anchors that must never be echoed into output) and `voice_habits`. `SynthesizeError` is now also a `DomainError` → **409 `STYLE_REFERENCE_SYNTHESIZE_FAILED`** with `details.reason_code ∈ {budget_unfit, empty_profile, source_overlap, text_integrity, llm_failed}` plus an `author_action`.
- **New `profile_json` keys, frozen into the runtime contract** (`runtime_contract._FROZEN_PROFILE_JSON_KEYS`): `voice_signature` (`{version, features, habits, deliberate_repetition}`) and `narrative_guidance` (≤8 deterministic lines derived from `narrative_patterns` ∪ `narrative.*` forbidden statements, source-overlap filtered; a forbidden statement keeps its polarity through a `避免：` prefix unless it already starts with a full negation word — single characters such as 不/无/别 do not count, so 「不断插入旁白」 still gets the marker). `anchor_quotes_used` is audit-only and not frozen.
- **Voice signature** (`services/style_reference/voice_signature.py`, pure functions, content-safe — closed-class words and punctuation only): function-word group densities + top words (`config/style_reference/function_words.yaml`), sentence-final particles, punctuation per 1k chars, sentence-length cadence (mean/std/p10/p90, short-sentence runs, lag-1 autocorrelation), paragraph shape, dialogue-lead placement and verbs, four-character-phrase and reduplication density, person, char-level TTR / bigram hapax — 52 features. `compute_voice_signature(texts)` / `compute_voice_signature_for_text(text)`; `render_voice_habits(features, baseline)` turns them into ≤12 directional Chinese habit lines **with no numbers** against `voice_baseline.yaml`; `deliberate_repetition=true` only when reduplication or short-sentence runs sit ≥ the baseline's literal block-level p85 (the 1/√n band narrowing applies to drift z-scores, never to this flag). Ingest stores `stats_json.voice_signature`, synthesis stores `profile_json.voice_signature`. If sentence splitting changes again, rebuild the baseline (`build-baseline`) and regenerate the golden expected files.
- **Injection & intensity semantics** (`injection.py`, `config/style_reference/injection_budget.yaml`): `intensity ∈ [0,100]` drives two things for *every* strategy (A / B / C / MIXED): the abstract-block total `total(i) = 900 + (2400 − 900)·i/100` chars (`intensity_min_total_chars` → `system_prompt_max_tokens`, split metric 0.15 / voice 0.20 / positive 0.45 / forbidden 0.20) and the few-shot window count `k(i) = round(3 + (10 − 3)·i/100)` (`few_shot_k_min` → `few_shot_k`; 2026-09-12: k max 8→10, and a binding whose `config_json` has no `intensity` now defaults to **100**, `_DEFAULT_INTENSITY`). **2026-09-09 exemplar-first (样例优先, Step 1 of the post-v2 plan; see `docs/style-exemplar-first-2026-09-09.md`)**: the reference passages are the primary signal. Prefix order is now `few_shot → rag → voice ([声音特征], rendered from voice_signature.habits) → positive → forbidden → metric → anti_plagiarism` (samples first, quantitative distribution last). Few-shot samples are **contiguous windows of up to 60 paragraphs / ≤4,000 chars each** (block ≤40,000 since 2026-09-12; measured on the 鲁迅 golden corpus, 904 paragraphs / 66k chars: 3 windows ≈11.5k chars at intensity 0, 7 ≈22k at 50, 10 ≈28.5k at 100, ≈0.2 s per render), presented in book order under a header that tells the model to write *in this author's hand* (diction, imagery, sentence shapes, pauses, narrative stance, dialogue handling) and forbids only reusing the samples' characters / places / events / sentences; the untrusted-data boundary stays but its preamble no longer calls the samples "data only". Windows **rotate per scene**: `style_prompt_injection` seeds `InjectionService.few_shot_seed` with the `scene_id`, so every node of one scene (style_draft, soft_qc, near-final rewrite *and* the near-final acceptance review, which now also receives the prefix) sees the same windows while different scenes see different ones (deterministic shuffle inside the top `k × few_shot_rotation_pool_multiplier` candidates; disabled while drift correction is active; the preview endpoint is unseeded). Ranking is still (scene paragraph-type match, distinctiveness vs. baseline — scored on the first `few_shot_affinity_scan_chars` of the window — completeness). `fit_fragments_to_input_budget` sheds few-shot **whole windows from the end** (keeping ≥1) before touching a single abstract line (`shed_few_shot_windows_preserve_abstract_v1`); the style-pass templates (`style_draft`, `scene_literary_rewrite`, both style patches, `soft_qc`, `near_final_acceptance_review`) therefore have their own `STYLE_PASS_INPUT_TOKEN_BUDGET = 64000` floor in `prompt_builder.py`, and `style_draft` / `stylize` / `style_patch` decoding penalties are 0 (they flattened deliberate repetition). Truncation is whole-line only — orphan headings are dropped, never a half sentence. Quantitative guidance is softened (`_soften_quantitative_guidance` strips numbers / absolute quantifiers, including bare decimals, ratios, ranges and `/千字`; a line left with ≥2 numeric tokens or no mechanism residue is dropped) instead of deleted; only claims pointing the *opposite* way from the frozen baseline are dropped — `short_sentence_ratio` / `long_sentence_ratio` use their own sentence-fraction bands (0.20–0.40 / 0.15–0.35) and a marker group conflicts only when a determinable metric disagrees and none agrees; the 概述 line is softened clause by clause. The runtime contract freezes the whole book's **paragraph root hash** (`book.paragraph_root_sha256` + `paragraph_count`, `runtime_contract.compute_paragraph_root`, hashes only) — while it matches the live paragraph table any paragraph of the book may enter a window — plus each quote parent's ±`few_shot_contract_neighbour_span` (default 2) same-book neighbours as the fallback: if the root no longer matches (any paragraph edited / added / removed) windows may only expand inside the frozen neighbours, and a tampered neighbour still fails its SHA-256 and degrades that side; old contracts without a root take the fallback path unchanged. Multi-layer: total ×(1 + 0.35·(layers − 1)) capped at ×1.7; few-shot / RAG samples come from the most specific layer and are no longer discarded; the same profile renders once across scopes; layered audit `render_stats` are recomputed on the merged, capped fragments (a layer that did not render inherits nothing), and `render_preview` honours `context_text` / `drift_ptype_priority` like the binding path. Strategy C calls the idempotent `ensure_rag_index` before rendering and queries by the profile's representative signature plus a scene paragraph-type filter (the styled previous text only when the caller marks it styled); empty recall audits `rag_outcome=unavailable`. The preview endpoint returns `stats {positive_lines, forbidden_lines, metric_lines, voice_lines, few_shot_windows, few_shot_chars, total_prefix_chars}` — the UI intensity readout consumes exactly this.
- **2026-09-12 style-first drafting (风格直起, Step 2 of the post-v2 plan; spec `docs/style-first-draft-plan-2026-09-12.md`; user directive: maximal imitation at every level, never conservative defaults).** `binding.config_json.draft_mode ∈ {style_first, neutral_first}` (default `injection_budget.yaml: draft_mode_default: style_first`) is frozen into the runtime contract as top-level `draft_mode` at bundle build (most specific layer wins; old contracts without the key = `neutral_first`); `runtime_contract.effective_draft_mode(bundle)` / `is_style_bound(bundle)` are the single deference condition, and `neutral_first` is the reading control group whose behaviour is byte-identical to before. Under `style_first` the **`neutral_ready` step slot stays** (checkpoint order, `stage="neutral_draft"` row, attempt `step="neutral_draft"`, pointers, ledger keys are unchanged — `_validate_advance` only allows `bundle_ready → neutral_ready → hard_qc_ready`) but its content changes: `generate_neutral_draft` builds the new `style_first_draft` template (64000 budget, `drafting` section kind, `[STYLE_REFERENCE]` prefix, dispatched under the **`style_draft` node route** — no new node to configure), writes the first draft in the author's hand, records `content_source=style_first_draft` + notice `STYLE_FIRST_DRAFT` (info) + the styled-draft gate (stage `neutral_draft` is now legal in `STYLED_DRAFT_GATE_STAGES`), and its single deterministic repair re-injects the prefix. `style_draft` (v10) then sees the source labelled `First Draft (already in the reference author's hand)` and **refines** (keep what already sounds like the author, move the rest one step closer, never neutralise) instead of recomposing; a rejected refine falls back to the first draft (`content_source=first_draft_fallback`), which `bundle_builder.latest_styled_draft_for_scene` may use as the next scene's voice anchor (a style-first neutral row is not treated as neutral text). Numeric length bands are widened by `style_first_length_slack` (0.5: 1200–1800 → 600–2700) through the `_LENGTH_BAND_SLACK` contextvar that every band parser reads — the author's scale wins inside the hard range. **House-taste gates defer** when `style_bound`: the anti-template / de-template gate becomes advisory-only (`_defer_house_taste_gate`, findings kept as `advisory_findings`; only the reference-derived `style_anchor_audit` and the safety gate can still trigger a repair, and `_assess_de_template_rewrite(house_taste_deferred=True)` skips the house-dimension regression checks), the rule-based auto-critique never issues a patch (`skip_critique`), `near_final._apply_scene_near_final_gates(style_bound=True)` skips the model-voice word list and the ending-action heuristic, `final_text_gate` computes but does not apply the three literary promotion thresholds (`house_taste_thresholds=deferred_to_reference`), and `_literary_freshness_budget` drops the hardcoded `avoid_false_clarity` / `avoid_summary_endings` lists and the "end on a hard action" clause (cross-scene template / image-field / n-gram reuse checks stay; `lifetime_banned_expressions` is skipped when the profile marks deliberate repetition). Hard gates (facts, must-includes, forbidden text, 12-char plagiarism, banned terms) never defer. Prompts touched: `style_first_draft` (new), `style_draft` v10, `hard_qc` v3 (manner is never a hard violation), `soft_qc` v5, `near_final_acceptance_review` v4 (the summary / atmospheric-ending veto only applies without a style block); `chapter_near_final_review` now receives the prefix via the chapter's first scene (live bindings; chapter bundles have no frozen contract). Workbench: `generation_summary.draft_mode`. Tests: `tests/test_style_first_draft.py`.
- **2026-09-12 structure following (结构跟随, Step 2 Track B).** Synthesis now writes two more deterministic `profile_json` keys (frozen in the contract): `structure_card` (`services/style_reference/structure.py::compute_structure_card` — chapters split at title-shaped paragraphs via `segmentation.heuristic.is_title_paragraph`; per-chapter chars / paragraphs / dialogue share / opening & closing type + ≤150-char excerpts; book-level chapter count, chapter-length and paragraphs-per-chapter quantiles, paragraph-type shares, opening/closing type distributions, person from the voice signature, ≤3 chapter-opening and ≤3 chapter-ending samples spread over the book; no markers → one chapter) and `planning_guidance` (≤10 `标签：陈述` lines from `scene.*` / `theme.*` observation findings, source-overlap filtered). `render_structure_card` produces a numeric Chinese `[结构画像]` block (planning needs scale) plus 「章首样例」/「章尾样例」 wrapped in `secure_reference_block(kind="structure_samples")` (samples only when the book's cloud policy allows, same rule as few-shot); `render_planning_guidance` produces `[场景手法]`. Old profiles without the keys render nothing. Consumers: `scene_blueprint._source_snapshot` (inline digests `style_structure_card` / `style_planning_guidance`, rendered as `## Style Reference — Structure Card` / `## Style Reference — Scene Craft`; `_validate_blueprint_payload` accepts 「无」 for `ending_action` / `anti_summary_rule` only when the snapshot carries a style reference), chapter planning (`chapter_planning_context` slot `style_reference` → `chapter_plan_llm` renders it for architecture / candidates / fill / review), snowflake `scene_list` / `scene_details` (`prompt_payload["style_reference_structure"]`, shed by `snowflake_prompt_budget` rungs `drop_style_reference_samples` then `drop_style_reference_structure` before any story material), all resolved through `services/style_reference/planning_context.resolve_project_style_reference` (project + global layers, task_type `scene_generation`). Planning templates bumped: `scene_blueprint` v4, `chapter_story_architecture` v3, `chapter_scene_plan_candidates` v2, `chapter_scene_plan_fill` v3, `chapter_plan_review` v2, `snowflake_generate_scene_list` v6, `snowflake_generate_scene_details` v7. Tests: `tests/test_style_reference_structure.py`.
- **Bundle sections** (`context_budget.SECTION_SPECS`): `style_narrative_guidance` ("Style Reference — Narrative Mechanisms", merged / deduped `narrative_guidance` across contract layers, ≤8 lines) is the *only* style section `neutral_draft` sees, and `scene_blueprint._source_snapshot` injects it as well; `previous_scene_voice_anchor` (tail ≤`continuity_anchor_max_chars`=900 of the same chapter's previous scene latest `style_draft/de_template/style_patch/style_salvage` draft — skipping a `style_draft` row that is really the approved neutral fallback (AttemptTracker `content_source=approved_neutral_fallback`, or content equal to the neutral draft) — else the previous chapter's last scene, else absent) and `style_drift_calibration` (≤`drift_calibration_max_lines`=3 lines from the chapter's latest `style_drift_observed` MetricEvent via `style_continuity.latest_drift_calibration`) are `style_draft`-only (`NEUTRAL_DRAFT_STYLE_SECTIONS`). `style_draft` keeps facts, causality, ending function, and must-includes immutable but may re-order sentences, pauses, information release, and paragraph selection; with no binding it does a natural de-neutralizing rewrite. `soft_qc` receives the same `[STYLE_REFERENCE]` prefix (`qc_engine._inject_style_reference_prefix`).
- **Styled-draft gate** (`qc_engine.run_styled_draft_style_gate`, `STYLED_DRAFT_GATE_STAGES = {style_draft, soft_qc, near_final_rewrite}`): the neutral-draft gate keeps only deterministic n-gram plagiarism (Q0); every styled output that can become final text — the `style_draft` and the `near_final_rewrite` (step `scene_literary_rewrite`, which used to go straight to archive) — is checked for plagiarism (Q0 `style_plagiarism` → the existing human-review escalation, never soft-acceptable) and frozen `banned_terms` (Q2 `reference_banned_term_replicated` → soft_qc asks for review); quantitative validation is diagnostic only. A plagiarism-rejected rewrite is dropped by `orchestrator._ensure_near_final_subcheckpoints`: the already-gated source draft becomes the final generation with `near_final_skip_reason=rewrite_rejected_style_plagiarism`, a Q2 warning `near_final_rewrite_rejected_style_plagiarism` (read from the near-final payload, so strict mode stops at `quality_warning_pending_acceptance`), and the run-state draft pointers reset to the source row so adopt-current cannot pick the rejected text. A gate exception is never treated as "no binding": it yields verdict `unavailable` (error type only), a non-blocking Q2 `reference_style_gate_unavailable` issue that asks for review, and the `STYLE_GATE_UNAVAILABLE` notice. Every decision writes a `styled_draft_gate_decided` MetricEvent.
- **Notices**: `scene_generation` records `notices=[{code, severity, message}]` on the latest style_draft attempt (and on the `scene_literary_rewrite` attempt for a gated rewrite) — `STYLE_DRAFT_FALLBACK_NEUTRAL`, `STYLE_INJECTION_MISS`, `STYLE_INJECTION_DEGRADED`, `STYLE_PLAGIARISM_HIT`, `STYLE_BANNED_TERM_HIT`, `STYLE_GATE_UNAVAILABLE` — with `hit_count` being the gate's true total rather than the length of the ≤8-item evidence list; `api/routes/scenes.py` `_attach_style_notices` surfaces them on `run/full` and the workbench payload **scoped to the current run's bundle** (an unresolvable bundle leaves the payload unchanged rather than showing an earlier run's notices); the same outcome lands in `_style_reference_runtime_audit` (hashes and counts only, never text).
- **Cross-scene drift** (W6, `style_continuity.observe_style_drift`, called from `scene_archive_effects._detect_and_store_style_drift` at archive time): z-scores of the final text's voice signature against the frozen `voice_signature.features` + `metrics_baseline`; features with |z| ≥ 1.5 yield ≤3 directional calibration lines and a `drift_ptype_priority`, written as MetricEvent `style_drift_observed` (`target_kind="scene"`, `context={chapter_id, scene_seq, features, deviations, calibration_lines, drift_ptype_priority}`). Until events exist, `latest_drift_calibration` returns `[]` and the section is simply absent. The freshness budget (`bundle_builder._literary_freshness_budget`) exempts pure function-word / punctuation n-grams and sets `preserve_reference_repetition: true` when any layer's profile marks `deliberate_repetition` (flag only — the explanatory wording lives in the `style_draft` template, so no target-voice directive reaches `neutral_draft`).
- **Input fidelity**: `split_sentences` keeps closing quotes with their sentence and splits ASCII `.` only before whitespace / EOL; the heuristic classifier treats a paragraph as dialogue only when quoted text is ≥ half of it or led by `说：/道：/问：` (single-character speech verbs are matched only after stripping `function_words.yaml` `speech_verbs.exclusions` such as 知道 / 应该 / 道理, so a quoted term inside narration is not dialogue), and short paragraphs inherit the previous type instead of defaulting to `transition`. Both import routes pick the LLM vs. heuristic classifier through `_get_llm_client_and_enabled()` per cloud policy and record `stats_json.classification.fallback_to_heuristic`. Because sentence splitting and the heuristic changed, the golden `tests/golden/style_reference/expected/*.json` were regenerated (`regen_expected.py`; once for the splitter / heuristic change and again for the speech-verb exclusion fix) — regenerate again, and say why in the commit message, whenever either changes.
- **Frontend** (`ws-styleref.jsx`): only the `scene_generation` task card remains; the intensity readout consumes the preview endpoint's `stats` (`computeIntensityReadout`, no local formula); the dimension selector is built from `profile_json.sub_dimensions`; imports list txt / md only; a duplicate same-scope binding warns that it will shadow the existing one.
- **Prompt sync rule.** Any edit to `config/prompts.yaml` (`style_ref_*`, `neutral_draft`, `style_first_draft`, `style_draft`, `hard_qc`, `soft_qc`, `near_final_acceptance_review`, `scene_blueprint`, `style_length_patch`, `style_salvage_patch`, …) is invisible to an install whose 系统配置 UI has ever saved a prompts snapshot until you run `cd backend; python -m novel_system.tools.sync_prompt_templates` (dry-run) and then `--execute` — see the DB-snapshot gotcha above. The tool now covers **every** template by default (it used to cover only `snowflake_*`; `--prefix snowflake_` restores that scope, `--template` names one, `--all` is kept as a no-op synonym); UI-edited prompts whose `version` did not move are preserved unless `--force-text`.

### Key Architectural Concepts

- **Snowflake Method pipeline**: The primary authoring flow. Author progresses through 10 ordered steps (reader positioning → one-line summary → one-paragraph summary → character summaries → one-page synopsis → character backstories → long outline → character bibles → scene list → scene planning). Each step produces a `SnowflakeStepRun` with draft JSON. Steps 1, 2, 3, 9, 10 are hard gates for structure materialization; others produce warnings.
- **Structure Materialization**: `POST /api/v2/projects/{id}/snowflake-workspace/materialize` converts approved `SnowflakeScenePlan` rows into `ChapterGoal` and `SceneCard` records. Proactive scenes get `Goal/Conflict/Setback` written to `SceneCard.writer_brief_json`; reactive scenes get `Reaction/Dilemma/Decision`. **Gotcha (fixed 2026-09-13, 阶段 A)**: `writer_briefs.normalize_scene_writer_brief` only keeps the 13 `writer_brief_v2` keys, so these snowflake keys never survived into the drafting bundle / blueprint / preflight — the writer saw only unlabeled `Beats`. `services/scene_structure_brief.py` now reads the raw keys (any scene whose brief carries a three-beat trio or a crucible, snowflake-materialized or hand-filled in 章节编排) and injects a labeled `Scene Structure (Snowflake)` fact section right after `scene_card` in `bundle_builder.py`, `scene_blueprint._source_snapshot` and `near_final._source_snapshot`; it is never compressed or omitted by the context budget. The drafting / planning / QC templates (`neutral_draft` v7, `style_first_draft` v2, `scene_blueprint` v5, `hard_qc` v4, `soft_qc` v6, `near_final_acceptance_review` v5) say what to do with it: proactive = Goal early → escalating Conflict → the designed Setback, reactive = Reaction felt → Dilemma → committed Decision; the ending outcome, POV, cost and must-withhold are bundle facts for `hard_qc`, scene-shape gaps are `scene_shape` patch guidance for `soft_qc`. `scene_run_preflight` reports `SCENE_STRUCTURE_INCOMPLETE` (missing beats) for structured scenes instead of the v2 `SCENE_LITERARY_INTENT_INCOMPLETE`. Tests: `tests/test_scene_structure_brief.py`. **阶段 F (2026-09-13, second-round evaluation)**: the rest of the confirmed design (02/03/04/06, the chapter table, neighbouring scene rows) now reaches drafting and the blueprint through the compressible `Scene Design Context (Snowflake)` section (`services/scene_design_context.py`, env `NOVEL_SYSTEM_SCENE_DESIGN_CONTEXT`; prompts `neutral_draft` v9, `style_first_draft` v4, `scene_blueprint` v7), and materialization no longer writes boilerplate as facts — `must_withhold`, `hook`, `expected_reader_emotion` and unplanned beats / crucible stay empty (the brief prints 未规划), `must_include_text` no longer falls back to the scene summary, `exit_change` still falls back to the author's setback / decision but never to a canned sentence; `_scene_card_resync_patch` keeps the same recipe. Tests: `tests/test_scene_design_context.py`. **阶段 G (让回溯便宜, same day)**: `snowflake_staleness.FIELDS_CONSUMED` now lists every direct-parent edge (01→02 minus `safety_rules`, 03→04/05, 04+05→06, 05→07, 04+06→08, 07→09, 09→10) and an upstream *absent* from a step's table means "not consumed, never stale" (an unknown step still degrades to edge-level); accepted-stale rows stay in the `_mark_downstream_stale` candidate set so 「已复核」 is re-evaluated on the next upstream change; a 09 re-approval marks only the scene plans whose content projection (`snowflake_staleness.scene_row_content`, keyed by `row_uid` or legacy `scene_id`) changed, and `_sync_scene_plans(approved=False)` resets only rows whose content changed (an unchanged row keeps its status and 复核 trace, so 「AI 补全这一场」 no longer un-approves the whole table); the minted `row_uid` / `scene_id` stamping into `draft_json` is now persisted via `flag_modified` (in-place mutation made SQLAlchemy skip the write). `project_runtime_invalidation` is **advisory** (`scope="advisory"`, nothing mutated) for 01/02/03/05 — re-approving a book-level step no longer stales every draft / QC / final and resets every run state — and scoped for `scene_list` (changed rows) and `long_synopsis` (scenes in changed chapter rows); 10 and the character steps were already scoped. The workspace step payload carries `revised_after_approval` (latest version is `pending_review` while an approved/stale version exists): the React sync layer no longer auto-approves such a step 700 ms after a keystroke or on hydrate — `SnowSync.needsReconfirm` / `approveStep` and the 「已改动 · 待重新确认」 pill / list marker hand the decision back to 「确认本步」, so the downstream stale cascade fires when the author confirms, not while typing. Tests: `tests/test_snowflake_cheap_backtracking.py`, the 阶段 G blocks of `ws-snow-sync.test.jsx`.
- **Scene Triage**: Before materialization, each scene plan is scored and assigned a triage status (`qualified`, `needs_fix`, `rewrite`). The `suggest` endpoint uses LLM to recommend triage decisions. **Rule layer (2026-09-13 阶段 B)**: `snowflake_steps.diagnose_scene_detail` only flags *missing* required beats and *placeholder* content (`placeholder_<field>` — empty, equal to an editor hint / placeholder / `SCENE_FIELD_EXAMPLES` repair example, or containing 待补/TODO/TBD/占位); **阶段 H (2026-09-13)** demoted everything else to `advice` lines (`建议：…`, also appended to `fix_steps`) that never change score or status: the generic-phrase table, a missing `cost_requirement` (a project addition, not one of the method's beats — Ingermanson's own scene sketches now `pass`), the keyword heuristics (escalating conflict, costly setback, two-cost dilemma, decision → next goal), and at step level the 但/却/cost marker checks and `_looks_generic` flags for 01/02/03/05/09 and the per-character `_pressure_too_soft` flag (which also read top-level keys the bible normalizer had moved into `psychological_profile`, so every bible character was permanently "too soft"; it now reads the nested profiles and only nags lead roles about empty sheets — minor characters may stay sparse). The rule-derived `rewrite` recommendation is a materialization *warning* (`triage_unreviewed_rewrite`), not a blocker; the execution contract's `cost_requirement` is always advisory. The five-slot synopsis steps keep positional empty slots and never truncate (`_coerce_positional_list`). Prompts `character_sheets` v5 / `character_synopses` v5 / `character_bibles` v4 / `scene_details` v10 say leave-empty-do-not-invent (only the protagonist and antagonist must be complete), and `UPSTREAM_STEPS_HOW_TO_USE` tells the model an unconfirmed draft's blanks are the author's, not gaps to fill — Ingermanson's own Goldilocks scene sketches are the golden fixtures (`tests/test_snowflake_method_contract.py`). Same batch: `_looks_generic` takes a per-field `min_chars` (no more blanket 28-char threshold); the logline contract is ≈40 Chinese characters everywhere (catalog, guidance, prompt v3, FE meter); `short_synopsis` is exactly five paragraphs and `one_paragraph_summary` exactly five sentences — extra items raise `StructuredCountMismatch` → one automatic regeneration with the rejection reason, then a 409 (`count_mismatch`), never silent truncation (coach patches drop the key instead); scene type defaults to `proactive` (no row-parity alternation) and the scene-list prompt v7 / guidance state Ingermanson's rule (after a setback: switch POV thread, next proactive scene, or a reactive scene only when the next goal is not obvious; reactive scenes are a minority); materialization stamps the 主角 from character sheets as `protagonist_hint` / `protagonist_character_id` on every scene brief (阶段 L: the 04 draft's explicit `protagonist_character_id` wins, the first 主角-role character is only the fallback) and prompt v8 / the structure brief say setbacks are measured against the protagonist, not the POV character. **Reactive rendering (2026-09-13 阶段 C)**: `SnowflakeScenePlan.rendering_mode` (`full` | `summary`, migration `20260913_0084`, `RENDERING_MODES` in `snowflake_steps.py`) lets step 10 mark a *reactive* scene as 「概述两段」; proactive scenes are always `full` (coerced in `_apply_scene_patch`, ignored by the scene_details sanitizer, omitted by the React `canonFromFE`). Materialization and resync stamp `writer_brief_json.rendering_mode` and give a summary scene the numeric band `SUMMARY_LENGTH_BAND = "200-500"` (so the existing numeric length machinery enforces it; switching back to `full` returns the card to `medium`); the structure brief renders a `Rendering mode: summary` line and `neutral_draft` v8 / `style_first_draft` v3 / `scene_blueprint` v6 / `snowflake_generate_scene_details` v9 tell the model to write it as two or three paragraphs of narrative summary. `snowflake_staleness.semantic_payload` strips a default `rendering_mode: full` from scene rows so pre-C drafts don't flip to pending on the first autosave; the chaptering rhythm report weights summary scenes 0.5 (`weighted_scene_counts`, `summary_scene_count`). **阶段 I (2026-09-14)** added Ingermanson's third option `skip` (`RENDERING_MODES = ("full", "summary", "skip")`, reactive only): the beat is still designed, materialization creates no `SceneCard` for it, `_scene_card_resync_patch` sends an already-materialized card to the trash (`trashed_flag=1`, brief `skipped_by_plan`) and takes it back when the mode changes again, the chaptering rhythm weighs it 0 (`skipped_scene_count`), and the following scene's `Scene Design Context` carries the skipped Reaction / Dilemma / Decision in full. Same phase: a scene may carry the *other* trio as follow-up beats (Goldilocks scenes 1 / 8 / 13) — the structure brief renders `Follow-up beats` + `Scene ends on`, `_beats_from_detail` appends them after the primary trio, and `neutral_draft` v10 / `style_first_draft` v5 / `hard_qc` v5 / `scene_blueprint` v8 / `snowflake_generate_scene_details` v11 know the scene then ends on the last follow-up beat; the setback field may describe a costly (mixed) victory when losing would end the story. Tests: `tests/test_snowflake_rendering_mode.py`, `tests/test_migration_0084_scene_plan_rendering_mode.py`. **Method fidelity (2026-09-13 阶段 D)**: step 04 character sheets carry Ingermanson's two storyline columns (`one_sentence_summary` / `one_paragraph_summary` — the React form fields 「一句话故事线」/「一段话故事线」 now round-trip them, and `canonFromFE` omits the keys when an older local cache lacks them so a server-generated storyline is never cleared) and `values` are one statement per element phrased 「没有什么比___更重要」 (the form stores the middle text one per line and wraps/strips the phrase on the way up/down; prompt `snowflake_generate_character_sheets` v4 asks for 2–3 in tension). Step 06 `synopsis` has a sixth prefixed line 「视角故事：」 (the whole book retold from that character's POV — the book's step 5; prompt `snowflake_generate_character_synopses` v4; unprefixed prose hydrates into that field, no longer into 「信念」). **阶段 K (2026-09-14)**: chapters come *after* scenes — `snowflake_generate_long_synopsis` v6 no longer hard-codes a chapter count and returns `chapters: []` unless the draft already has a table (the 07 `chapters` editor field is `optional`, so an empty table is not a missing field); `SnowflakeChapteringService.propose_from_scenes` (`POST …/chapter-plan/propose`, body `target_chapter_count` / `scenes_per_chapter` / `replace`) splits the ordered scene list into chapters — each 灾 scene closes its chapter (hinges outrank the target count), chapter count from the project setting or ≈3 scenes per chapter, ≥1 chapter per act — assigns the scenes, mirrors the rows into the latest 07 draft, and the 章节编排 panel offers 「按场景生成 / 重排章表」 (the preview's `SNOWFLAKE_CHAPTER_PLAN_EMPTY` is no longer a dead end); the five expansions are now about a page each (600–1000 chars, `chapter_planning_context` canon budget 5200). Step 07 `long_synopsis.paragraphs` is again the book's step 6 — exactly five page-length expansions of the five short-synopsis paragraphs (`LONG_SYNOPSIS_PARAGRAPHS`, same `StructuredCountMismatch` retry/409 contract as step 05; normalization pads to five but never truncates) while `chapters` stays the chaptering truth; `canonFromFE("outline")` no longer overwrites `paragraphs` with per-act chapter lines, `feFromCanon` treats a legacy chapter-line mirror as empty slots, and `parse_outline_chapters` / the FE fallback only recognise real `NN 章名：` lines (prose never fabricates chapters); prompts `snowflake_generate_long_synopsis` v5 and `snowflake_generate_scene_list` v8 (scenes are traced to the five expansions, chapters give membership only); `snowflake_staleness.FIELDS_CONSUMED` now lists `chapters` next to `paragraphs` as a scene-list input (the mirror used to carry chapter edits implicitly), and the chapter-planning canon budget for `long_synopsis` is 3200 chars. One-time effect on pre-D projects: the first 07 save after upgrading replaces the chapter-line mirror with empty expansions, so step 09 flips to `stale` once and the author accepts it. Post-draft triage: `near_final_acceptance_review` v6 returns a required `scene_story_check {crucible_identified, shape_landed, verdict yes|no|maybe, note}` (Ingermanson's Yes/No/Maybe); `near_final._normalize_scene_story_check` normalizes it, it rides on the review `AttemptTracker.details_json`, is surfaced by the workbench near-final summary and per scene by the catalog (`story_check`, `CatalogService.story_checks`, one batched query per chapter) and rendered as a non-blocking badge in the 成稿中心 结构 tab — it never changes `pass_flag`. Tests: the 阶段 D section of `tests/test_snowflake_method_contract.py`, `test_scene_story_check_is_normalized_recorded_and_surfaced` in `tests/test_near_final_engine.py`, and the 阶段 D block of `frontend-react/src/ws-snow-sync.test.jsx`. **Backtracking UX (2026-09-13 阶段 E)**: the workspace step `artifact` now carries `input_refs` (the upstream `step_run_id`s consumed when the version was written); `SnowSync.upstreamChanges(workId, feKey)` compares them with each upstream's current `stepRunId` and fetches `history?include_draft=true` to show a consumed-version vs current-version diff (`S2UpstreamDiff`), the stale banner offers 「按新上游重新展开」 (`structuredGenerate({source: "fe_restale_regen"})`, the step returns to 进行中 for re-confirmation) and 「已复核」 calls `accept-stale` first when the backend marked the step stale (`SnowSync.acceptStale`; a failed call leaves the local map untouched). Backend `status == stale` (not yet accepted) is the **only** 需复核 signal (E3 step 2, same day): the local `revs` / `confirmRevs` map, its content-signature bump effect and the `book_brief.fe_meta.revs` write-through were removed — `s2StaleMap(health)` lists backend-stale steps and `s2UpstreamDrift(health, key)` names the upstream steps whose current `step_run_id` differs from the step's `input_refs` (a direction hint and the diff input, never a second staleness algorithm); approve and accept-stale responses carry the whole workspace and `captureWorkspaceHealth` refreshes every step's health so downstream stale shows without a re-hydrate; `accept_stale_step` re-snapshots `input_refs_json` / `consumed_input_sigs_json` to the current upstream versions (「仍然有效」 is said about the upstream as it is now). Hydration maps backend `stale` to FE state `done` (the flag lives in `health.beStatus` / `staleReason`). The 验收门 is explicitly advisory (copy says 验收未过也可确认本步，后端闸门仍会守住依赖; no lock icons), the coach textarea owns ⌘↵ (`stopPropagation`, window handler skips `.sf-coach-input`), and the step-10 coverage cells count slots by the 09 scene type (`s2PlanState(plan, type)`). Docs: `docs/snowflake-method-contract.md` is the step-by-step contract against Ingermanson (indexed from `docs/README.md`, README and the operator manual; the two dangling links to deleted docs were removed and `tests/test_docs_links.py` guards relative links). Tests: `tests/test_snowflake_phase_e.py`, the 阶段 E blocks of `ws-snow-sync.test.jsx` / `ws-snow.test.jsx`. **阶段 M (2026-09-14, frontend only)**: the 09 board is Ingermanson's movable spreadsheet — `snow-scene-insert-{i}` inserts a fresh row after row *i* (inheriting line / POV / place), rows are HTML5 drag-and-drop reorderable through the pure `s2ReorderScenes(list, from, to)`, rows are keyed by scene id, and a read-only chapter header row (`snow-scene-chapter-{i}`) is rendered before the first scene of each chapter from the hydrated `chapter_title` (hidden when it is only the `chapter_id` fallback; `chapter` never goes up with the 09 PATCH — chapters are edited in the 章节编排 panel). Step 10 gained `hook` / `exit_change` inputs (`snow-plan-hook` / `snow-plan-exit-change`; the columns always existed on the scene card). `captureTriage` stores the workspace's `triage_items` per work keyed by the 09 `row_uid` (mapped through the `scene_list` draft's `scene_id`; author `effective_status` outranks `recommended_status`) and `SnowSync.triageItems(workId)` seeds the step-10 triage state on hydrate, so a saved triage survives a reload. 「略过此步」 now calls `SnowSync.skipStep(workId, feKey, reason)` → `POST …/steps/{beKey}/generate {skip: true, skip_reason}` (the view refuses essential steps 01/02/03/09/10 up front and requires a reason; a server rejection leaves the local state untouched) — it used to be a local-only marker the backend never saw, so the materialization gate stalled silently. Tests: the 阶段 M blocks of `ws-snow-sync.test.jsx` / `ws-snow.test.jsx`.
- **Vector Backend Split**: Windows tests always use `memory` backend; real ChromaDB only runs in Linux/WSL and is gated by the `chroma_integration` pytest marker. `conftest.py` applies this automatically.
- **Style Reference (style imitation)**: Profiles are *abstract* (layered rhythm/syntax/imagery/narrative dimensions + anti-clone `forbidden_pattern`s) — the system must never copy source text, characters, settings, or signature imagery. See the "Style Reference subsystem" above for the ingest→extract→inject→validate pipeline; `services/source_safety.py` + `services/reference_safety.py` enforce the copy guardrails.
- **Dual-stack Pagination**: API responses support both `page`/`page_size` (offset) and `cursor`/`limit` (cursor-based) patterns via `services/pagination.py`.
- **Test Isolation**: `conftest.py` auto-creates an isolated SQLite DB per test in `tmp_path`, resets the engine, and auto-skips `chroma_integration`-marked tests on Windows. No shared test state between tests.
- **Snowflake Assistant Turns**: `SnowflakeWorkspaceAssistantService` stores conversational coaching turns per step, enabling LLM-guided iterative refinement of snowflake drafts without losing context.
- **Schema build split**: tests build the schema via `Base.metadata.create_all`, but dev/prod build it via Alembic migrations (`auto_create_tables` defaults to `False`). A model change without a matching migration therefore passes CI but 500s at runtime — the `test_metadata_isolation.py` drift guard (see Backend commands above) is the tripwire. Adding a column/index means writing a migration *and* keeping the ORM `__table_args__` in sync.
- **Scene-run orchestration & quality floor**: `services/orchestrator.py` runs a scene end-to-end — bundle context → generate (single candidate in production; the multi-candidate selection machinery is only reachable when tests override `_best_of_n_count`) → optional LLM auto-critique → QC gates — scored by `literary_quality.py`'s 21 dimensions and guarded against repetition by `self_repetition.py`. This is the "blueprint quality floor v2" layer on top of raw scene generation.
- **Narrative event sourcing**: `NarrativeEvent` is an append-only log treated as the single source of truth for story state; entity state at a given scene is reconstructed by replaying events up to that point (`narrative_event_log.py`, populated from prose by `prose_event_extractor.py` when event extraction is enabled).
- **Durable LLM accounting & quotas**: every provider POST goes through `llm_accounting.py`'s reservation → dispatch → settlement transactions; daily/monthly/per-project token, request, concurrency, and cost quotas are checked *before* dispatch, and no DB write transaction is held while waiting on the network. Orphaned reservations are reclaimed after a TTL. **Every quota is opt-in and ships disabled** (`0` = no ceiling) — with none armed the pre-dispatch gate short-circuits before running any counting scan. Accounting itself is unconditional: the ledger and the 成本看板 usage readings do not depend on a fence being armed.
- **Startup background recovery**: the FastAPI lifespan runs `run_startup_recovery` to re-submit durable scene/chapter/style/validation jobs after a crash; persistent leases + worker-owned CAS make duplicate scans harmless. Scene runs are cancellable and checkpointed.
- **Canonical final-text lifecycle**: chapter/project final approval is a deliberate multi-step gate (read-through confirm of server text → body-hash confirm → `approve-final`); the catalog cannot fake `approved`, and reopening needs an auditable reason with server-side cascade revocation of downstream approvals.
- **Local-only network boundary**: the backend defaults to `NOVEL_SYSTEM_LOCAL_ONLY=true` (loopback clients only, forwarded headers rejected); remote access requires an explicit shared token — there is no user auth, RBAC, or tenant isolation.
- **Demo vs. real data**: retired. There is no product demo work and no fake/offline generation — the 「潮汐档案」/「盐镇来信」 seeds and all offline stub clients were removed. Neutral placeholder works survive only as test fixtures (`backend/tests/fixture_works.py`, ids `work-a`/`work-b`). Every generation path is fail-closed: no live LLM → 409/502 + `author_action`, never canned output.
