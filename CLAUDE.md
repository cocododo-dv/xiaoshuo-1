# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Product Scope

Single-machine, single-author long-form novel writing system (Chinese-language product). The root `README.md` (Chinese) is the product-truth entry point and `docs/README.md` indexes all documentation — the operator manual documents only the React workbench; dated plans/evidence describe their original run, not the current contract. Product mainline: create work → 10-step snowflake ideation → materialize chapters/scenes → per-scene AI drafting or manual writing → author review promotes authoritative text → final approval in 成稿中心. Explicitly *not* a multi-user SaaS, HA service, or automatic publication arbiter.

**2026-09 subtraction (batch 1)** removed, wholesale: the legacy Vue frontend (`frontend/`), the outcome-governance / blind-evaluation / hidden-benchmark cluster, the knowledge-promotion + versioning + vector-alias / reindex / verify layer and its 知识/索引/域 consoles, the interop (bundle-worksheet) center, the 长篇控制塔 backend (anchors, chapter contracts, audits, structure guidance), the legacy `style_profile` extractor and its six rule tables, the advisory narrative overlays (theme anchor, tension curve, foreshadow lifecycle, character arc/psychology, relationship matrix, voice fingerprint, POV coloring, style-drift guidance, reverse causal skeleton, work profiles), chapter runtime backfill/manual-hold, project backtrack items, scene quality contracts / auto-rewrite, author structure extraction / discovery drafts, writer-review LLM diagnosis nodes, writer-room / author-desk, and most operator tools (only `reset_author_state`, `db_backup`, `sync_prompt_templates`, `raise_llm_output_budget`, `chroma_smoke` remain, plus `refresh_style_reference_books` (2026-09-14) and `purge_style_reference_books` (2026-09-23 风格参考 v3)). A few thin v1 primitives were deliberately kept because the test suite builds on them even though React never calls them: `POST/GET /api/v1/projects|chapters|scenes`, `GET /api/v1/projects/{id}/dashboard`, the v1 `snowflake` route, `POST …/chapters/{id}/run` (sync), and `POST /api/v1/scenes/{id}/run/full`. Migration `20260904_0083` drops their tables irreversibly. Do not re-introduce these; batch 2 (simplify approvals, accounting, system config) and batch 3 (scene pipeline rewrite) are planned next. **Demo data / fake generation are retired**: there is no product demo work, no seeded 「潮汐档案」/「盐镇来信」, and no offline/deterministic stub generation. Every generation node is fail-closed — with no live LLM configured it returns a 409/502 with an `author_action` (never canned prose). The old demo seed lives on only as neutral **test fixtures** under `backend/tests/fixture_works.py` (works `work-a`/`work-b`) + `backend/tests/fixture_runtime.py` (`seed_runtime_fixture`), consumed by tests and the contract-E2E lane; the production runtime never seeds them.

## Common Development Commands

### Full Stack Lifecycle (Windows)
- Start full stack: `.\start-dev.cmd` (runs backend + frontend, opens browser)
- Stop full stack: `.\stop-dev.cmd`
- Restart full stack: `.\restart-dev.cmd`
- Reset runtime DB/artifacts but keep LLM config: `.\reset-runtime-keep-llm.cmd` (→ `scripts/reset_runtime_keep_llm.ps1 -StopServices`; distinct from the Python `reset_author_state` tool below)

On Linux use the shell equivalents: `scripts/start-all-linux.sh` / `scripts/stop-all-linux.sh` (one-shot backend + React frontend, health-probed, logs + pidfiles under `.codex-run/`), or the per-leg `scripts/start-backend-linux.sh` / `scripts/start-frontend-linux.sh` (all safe to re-run — each leg stops its own previous instance first; shared helpers in `scripts/lib/dev-lifecycle.sh`). The backend leg runs `alembic upgrade head` with **`backend/.venv/bin/python`** (not system python) and forces `NOVEL_SYSTEM_VECTOR_BACKEND=memory`; the frontend leg resolves Node in the order `NOVEL_SYSTEM_NODE_BIN` (a `bin/` dir) → nvm at `~/.nvm` (falling back to `nvm use 16` only when no default is on PATH) → `~/.local/node/bin` (plain tarball install) → `node` already on `PATH`, failing with a clear message if none is found, and always preloads `frontend-react/crypto-polyfill.cjs` via `NODE_OPTIONS --require` (a no-op on Node ≥ 18; needed on Node 16, the ceiling of the old CentOS 7 / glibc 2.17 host, which lacks the global WebCrypto Vite 6 needs). `start-all-linux.sh` fails fast if a leg's process exits during startup and prints the tail of that leg's log. CI (`.github/workflows/ci.yml`, Ubuntu + Node 22) installs locked frontend dependencies with `npm ci`, runs unit/build gates, and uses `scripts/verify_react_e2e.sh` for an isolated migrated backend + React browser contract lane.

Default addresses: the **React frontend** `http://127.0.0.1:5174` (what `start-dev.cmd` auto-opens) and the backend `http://127.0.0.1:8000`. `start-dev.cmd` (→ `scripts/dev.ps1`) brings up the backend + React frontend in one shot: it runs `alembic upgrade head` (**no demo seed** — the production launcher never injects demo works), forces `NOVEL_SYSTEM_VECTOR_BACKEND=memory`, auto-generates a `NOVEL_SYSTEM_CONFIG_SECRET`, and writes pid/url files under `.codex-run/` (`backend.url`, `frontend.url`, `frontend-react.url`). If port 8000 is busy it scans upward and records the chosen URL in `.codex-run/backend.url`. Backend readiness is probed at `GET /ready` (a 90s timeout — if migrations are stale the probe fails and the browser never opens). The backend also exposes `GET /live` (process liveness only) and `GET /ready` (adds DB connectivity + migration revision + required-structure checks) — deployment probes should use their distinct semantics.

### Backend (Python 3.12 / FastAPI)
Located in `backend/`. The interpreter is the **uv-managed locked venv**, never a system / Anaconda Python: `cd backend; uv sync --locked --extra dev` creates `backend\.venv\Scripts\python.exe` on Windows — what `start-dev.cmd` (`scripts/dev.ps1`), `scripts/verify_windows.ps1` and `scripts/verify_react_e2e.ps1` hard-require (each throws "Locked backend Python is missing" otherwise) — and `backend/.venv/bin/python` on Linux (what the Linux launchers use). `backend/.venv-wsl` (`UV_PROJECT_ENVIRONMENT=.venv-wsl uv sync --locked --extra dev --extra chroma`) is the separate Chroma-capable env that `scripts/verify_wsl_strict.sh` prefers (it falls back to `.venv`, then to a sibling worktree's venv, then `python3`); the default env deliberately excludes Chroma. `pyproject.toml` sets `pythonpath=["src"]` and `testpaths=["tests"]`, so pytest/alembic must run **from `backend/`** with paths relative to it — and the PowerShell working dir resets to repo root between calls, so always `cd backend` in the same command. Ruff is correctness-only (`select = ["E9", "F63", "F7", "F82"]`, run as `ruff check src tests`): no formatter or style rules are enforced, so never reformat files you did not otherwise change.

CI installs `requirements.lock` with `pip --require-hashes`; `uv.lock` is the canonical cross-platform resolution. After changing `pyproject.toml`, run `uv lock --python 3.12` and then `uv export --locked --extra dev --no-emit-project --format requirements-txt --output-file requirements.lock` (the `dev` extra only — `--all-extras` would pull the `chroma` extra and its ~50 transitive packages into the hashed lock that every CI job installs), then the same export again with `--extra dev --extra chroma --output-file requirements-chroma.lock` (the **Backend Chroma Integration** CI job and the WSL lane install that one), and review all three files. `tests/test_dependency_lock.py` pins this contract: `uv.lock` must match `pyproject.toml`'s direct dependencies, `requirements.lock` must match the uv resolution and contain `--extra dev` but neither `--all-extras` nor `chromadb`, and `requirements-chroma.lock` must be committed with `--extra chroma` and `chromadb==1.5.9`. To lift one flagged package without touching `pyproject.toml`, use `uv lock --python 3.12 --upgrade-package <name>` and re-export. The root is not an npm package; only `frontend-react/` owns a Node lockfile (the same test asserts there is no root `package-lock.json`).

```powershell
cd backend
.\.venv\Scripts\python.exe -m pytest                                          # all non-Chroma tests (~2,960 — minutes, not seconds)
.\.venv\Scripts\python.exe -m pytest tests/test_snowflake_workspace_v2.py     # single file (path is relative to backend/)
.\.venv\Scripts\python.exe -m pytest -k "test_materialize"                    # by name pattern
.\.venv\Scripts\python.exe -m pytest -m "not chroma_integration"             # exclude Linux-only tests
.\.venv\Scripts\python.exe scripts\pytest_shard.py --shard-index 0 --shard-count 4 -- -q -m "not chroma_integration"   # one CI shard
```

On Linux the same commands are `backend/.venv/bin/python -m pytest …` (the full suite takes many minutes on a small box — run it in the background). `backend/scripts/pytest_shard.py` is the deterministic file-level sharding CI and `verify_windows.ps1` use (sorted `tests/test_*.py`, index modulo count); four shards passing is equivalent to the full non-Chroma suite. **Two pytest sessions running at the same time must each pass a distinct `--basetemp`** (a per-session temp dir): they otherwise rotate and delete each other's shared `/tmp/pytest-of-<user>` dirs and fail with spurious `could not create numbered dir` / `unable to open database file` errors.

Tests marked `@pytest.mark.chroma_integration` require real ChromaDB and are auto-skipped on Windows by `backend/tests/conftest.py`. Run them via WSL:
```
wsl -d Ubuntu-24.04 bash -lc "cd <wsl-path> && bash scripts/verify_wsl_strict.sh"
```
The other declared marker, `consistency_validation` (blueprint §17 Action B recall/precision), is **not** auto-skipped and runs in the default Windows suite.

Full Windows CI lane: `powershell -ExecutionPolicy Bypass -File scripts/verify_windows.ps1` (pytest `-m "not chroma_integration"` + the **React mainline** `frontend-react` `npm test` (vitest) + build). Full release lane (Windows CI → **React mainline contract E2E** (`scripts/verify_react_e2e.ps1`, see below) → WSL strict Chroma): `scripts/verify_release.ps1`.

GitHub Actions (`.github/workflows/ci.yml`) runs on every PR and on pushes to `main` / `master` / `codex/**` (a feature branch gets CI only through its PR): **Backend Quality Gates** (`ruff check src tests`, `pip check`, hashed `pip-audit`, `tests/test_service_architecture.py` + `tests/test_dependency_lock.py`), **Backend Tests** (pytest `-m "not chroma_integration"`, 4 shards via `backend/scripts/pytest_shard.py`), **Backend Chroma Integration** (installs `requirements-chroma.lock`, runs `novel_system.tools.chroma_smoke` then `-m chroma_integration`), **Frontend Tests (React mainline)** (`frontend-react`: `npm ci` → `npm audit --omit=dev --audit-level=high` → vitest → build — a high-severity advisory in a production dependency fails CI), and **React Contract E2E** (fresh migration + isolated seeded backend + real Chromium). WSL strict Chroma remains local in `verify_release.ps1`; React contract E2E runs both in CI and in that local release lane. `.github/pull_request_template.md` still lists checkboxes for clusters removed in the 2026-09 subtraction (Index / Knowledge Console, Interop Center, chapter runtime backfill, `/api/v1/knowledge-entries` …) — treat those items as void.

**Schema-drift guards** (`backend/tests/test_metadata_isolation.py`) — the most important non-obvious gotcha. Historical Alembic revisions are frozen explicit DDL and may not import the live application ORM. The test suite builds its schema via `Base.metadata.create_all`, while dev/prod use Alembic `upgrade head` (`auto_create_tables` defaults to `False`); the guard builds it **both** ways and diffs tables/columns/named-indexes. An ORM change without a matching migration must fail here instead of surfacing later as `OperationalError: no such column`. If it fails: write the missing migration, or declare a migration-only index in the model's `__table_args__`. Run: `cd backend; python -m pytest tests/test_metadata_isolation.py`.

**Other drift guards** that fail the suite (know them before you add code):
- `tests/test_service_architecture.py` — no import cycles inside `services/` or across the whole package (a new `import` between two services can close a cycle — e.g. `snowflake_triage.py` must stay a leaf), `services/` and `api/` never import `tools/`, project ownership must come from relation columns (never parsed from ids shaped like `*_CH_*`), and the Alembic bootstrap must not read DB-backed runtime config.
- `tests/test_api_openapi_contract.py` — every versioned operation documents the standard success / error envelopes. `api/openapi_contract.install_api_openapi_contract` does this centrally in `create_app`, so a new route only has to return through `api/response.ok` / `error`; `/live` and `/ready` are the only un-enveloped paths. Mutating routes go through `api/mutations.py` (`execute_with_idempotency`), which rejects a missing `X-Idempotency-Key` with 400 `IDEMPOTENCY_KEY_REQUIRED` and replays / rejects same-key requests (`tests/test_idempotency_contract.py`).
- `tests/test_prompt_template_contracts.py` — the table-driven contract over `config/prompts.yaml`: every template has `version` (`YYYY-MM-DD.vN`), `input_token_budget`, `system_prompt` / `task_prompt` and a `structured_schema`; every array whose members are objects declares their `properties` (a schema-enforcing relay decodes property-less members as `{}`; the snowflake collection templates are checked after `enrich_structured_schema`); score fields declare `minimum` / `maximum` (an open `scores` object — `additionalProperties` may only be a boolean in this schema subset — needs the prompt to state the scale); key templates keep their behaviour phrases. Still bump `version` on every prompt edit (`sync_prompt_templates` keys on it), but never pin a version string in a test.
- `tests/test_routes_all_manifest.py` (routes `__all__` mirrors `include_router`), `tests/test_schema_contract_revision.py` (`CURRENT_SCHEMA_REVISION` equals the single Alembic head), `tests/test_docs_links.py` (relative `.md` links in the root README, `docs/README.md`, the operator manual and the method contract must resolve), `tests/test_fixture_import_boundary.py` (the fixture-import endpoint must stay gone), `tests/test_style_reference_golden.py` (regenerate `tests/golden/style_reference/expected/*.json` with `regen_expected.py` whenever sentence splitting or the heuristic classifier changes, and say why in the commit).

**Test conventions** (`tests/conftest.py`): every test gets its own sqlite under `tmp_path` built with `create_all`, the `memory` vector backend, import roots limited to `tmp_path` + `tests/`, `NOVEL_SYSTEM_SCENE_TOKEN_BUDGET_MULTIPLIER=5` (the suite still assumes the *armed* scene budget; the product's disarmed default is covered explicitly in `tests/test_scene_token_budget.py`) and `NOVEL_SYSTEM_ENABLE_FIXTURE_IMPORT=true`. A fake LLM client must inherit `tests/accounted_llm_fakes.AccountedGenerateMixin` so the accounting hook wraps its `generate` — a bare object with a `generate` method bypasses the ledger and breaks the accounted paths. Style-reference fakes: `build_fake_paragraph_classifier()` (also the `fake_paragraph_classifier` fixture); the learn / check job tests carry their own accounted fakes; shared seeding helpers (book + paragraphs, run + findings + evidence, v3 profile with a dimension card, binding, windows) live in `tests/style_reference_factories.py`; route tests import a book through `tests/style_reference_route_helpers.import_book`, which installs the fake classifier and waits for `ready` under the strict-LLM classify job. A pytest process refuses to open the repository's own `backend/novel_system.db` (`db/session.refuse_repository_database_under_pytest`). `NOVEL_SYSTEM_STYLE_REF_LOCAL_CORPUS=<path/to/book.txt>` opts `tests/test_style_reference_local_corpus.py` into a real (possibly copyrighted, never committed) local book; without it that file skips.

### Frontend (React, primary) — `frontend-react/`
The production frontend is the maintained Vite + React 18 「潮汐工作台」 in
`frontend-react/`. `start-dev.cmd` serves it on `http://127.0.0.1:5174` and opens it
by default (landing view is `主页`, not the snowflake view). The former 「流程」 flow-map view was merged into 主页 on
2026-09-16: the per-chapter progress spine + scene counts are derived from the catalog by the pure `ws-home-derive.js`
(tests `ws-home-derive.test.js` / `ws-home.test.jsx`), and `#flowmap` is a `WS_VIEW_ALIAS` redirect to `home`.

```powershell
cd frontend-react
npm ci             # exact versions from package-lock.json
npm run dev        # http://127.0.0.1:5174
npm run build
```

Architecture rules:
- **Store layer only**: views keep the prototype's store contracts. The older stores are **runtime
  globals attached to `window`** via `Object.assign(window, {...})` at the bottom of their kebab-case
  files — `WsWorks` (`ws-works.jsx`), `WsCatalog` + `WsTrashStore` (`ws-catalog.jsx`), `WsReview`
  (`ws-review.jsx`), `SnowSync` (`ws-snow-sync.jsx`), `WsSnowflake` (`ws-snow.jsx`), the `LIB_*`
  library data (`ws-library-data.jsx` / `ws-library-edit.jsx`) and the writer-side `WrDocs` /
  `WrDocVersions` / `WrRecovery` (`wr-doc-store.jsx`) — grep `window.WsWorks`, not an ES import.
  The newer stores are plain ES exports: `WsManuStore` (成稿中心, `ws-manuscripts-store.jsx`),
  `WsCost` (`ws-cost.jsx`), `WsAiProviders` (`ws-ai-providers.jsx`), `WsLibrary` (`ws-library.jsx`),
  `WsTrash` (`ws-trash.jsx`) and the 风格 view's `ws-styleref-store.js` (its window globals were
  removed in the 2026-09-23 v3 rebuild). The window seam is legacy to be *consumed*, not extended:
  `tooling-independence.test.js` fails when a new ESM-only module writes to `window` (see the guard
  tests below). Stores are API-backed with sync in-memory caches (optimistic write + rollback /
  refetch-on-failure). The navigation model — writer/advanced mode gating `WS_NAV_GROUPS`, view labels,
  project-scoped views and the aliases (`WS_VIEW_ALIAS`: `deepdesk → writer`, `flowmap → home`) — is the
  pure data module `ws-nav.js`, shared by the rail (`ws-rail.jsx`) and the ⌘K palette; `ws-app.jsx`
  keeps only routing, the lazy `import("./ws-*.jsx")` literals and `<ViewReady>`.
- **Shared UI layer (2026-09-21 whole-frontend refactor, identity kept: ivory paper / ink / crimson /
  serif headings)**. Use these instead of per-view look-alikes:
  tokens in `styles.css` (semantic `--accent/--ok/--warn/--danger/--info` each with `-wash` and `-ink` —
  `-ink` is the only text colour allowed on a `-wash`; `--on-accent`, `--scrim`, `--z-*` stacking scale,
  `--fs-*` type scale, `color-scheme` per theme, Windows-safe font stacks; the sepia theme lives here too);
  `ws-ui.jsx` + `ws-ui.css` (imported right after `styles.css`): `PageHeader`, `Segmented` (radiogroup,
  `.seg/.seg-btn`, per-option `data-value`), `Tabs`, `Tag`, `Notice`, `EmptyState`, `StatTile`,
  `SectionLabel`, `Spinner`, `IconButton`, `CloseButton`, the `data-tone` resolver (defaults via
  zero-specificity `:where()`), `button{color:inherit}` and the global `.btn:disabled`;
  `ws-dialog.jsx` (`WsDialog` — focus trap / Esc stack / backdrop veto / `portal={false}`, StrictMode-safe
  `useFocusTrap`, `isImeComposing`); `ws-notify.jsx` (`WsToastHost` mounted once in App, `wsToast`,
  `wsConfirm` → `Promise<boolean>`; `storeAlert` routes there and falls back to native `alert`/`confirm`
  when no host is mounted, which is what unit tests see); `ws-prefs.js` (one schema for every
  `ws_tweaks_v1` key, used by the 「排版与舒适度」 panel and 设置·外观); `ws-labels.js` (chapter / scene
  labels and the one chapter-state vocabulary shared by 主页 and 成稿中心, human labels for review sources,
  accounting statuses and LLM nodes); `lib/platform.js` (⌘ vs Ctrl). The megafiles are split behind
  their old entry modules, which keep re-exporting every name tests import: `ws-snow-*` (model / hooks /
  scaffolds / scenes / coach / rail / chrome …), `ws-writer-*`, `ws-scene-*`, `ws-author-*`,
  `ws-manuscripts-*`, `ws-styleref-*` (store module `ws-styleref-store.js`), `ws-library-*`,
  `ws-settings-ai-*`. `screens.css` is retired: its live rules moved into per-view files imported at its
  old cascade position (`ws-scene.css`, `ws-manuscripts.css`, `ws-settings.css`, …). Event semantics that
  changed: `ws:work-changed` fires only when the active work switches or the list membership changes
  (derived word / stat updates fire `ws:work-stats-changed`); `ws:recovery-open` opens the 同步与恢复
  center, which lives in the rail footer (there is no floating button any more); `ws:settings-tab`
  (view intent, detail `"ai"` …) opens a 设置 tab; `ws:snow-chapter-plan` is only SnowSync's status
  broadcast — opening the 整理为章节结构 panel is a callback, never that event. Component and file names
  in the dated 阶段 T–Z bullets below predate this split (e.g. `S2AiBar` / `S2Coach` now live in
  `ws-snow-coach.jsx`, the 章节编排 rows in `ws-author-detail.jsx`): grep the name. The UI that read
  the chapter-level tension / threads fields (`ArrTensionCurve`, `ws-author-loom.jsx`, the 故事弧线 /
  线索织布机 lenses) is deleted; only the store / API fields remain as a subtraction candidate.
- **Store unit tests** (vitest + jsdom, `src/**/*.test.{js,jsx}`, 71 files / ~950 tests, ≈3.5 min on the 2-CPU Linux host;
  `vitest.config.js` sets `globals: true`, `restoreMocks` / `clearMocks`): cover the optimistic-write +
  rollback/refetch contract per store — `ws-works`, `ws-catalog` (incl. `WsTrashStore`),
  `ws-review`, `ws-snow-sync`, and the newer `ws-ai-providers` / `ws-chapter-run` / `ws-cost` /
  `ws-scene-run` / `ws-manuscripts` / `wr-doc-store` / `wr-canonical-control` /
  `wr-content-safety-review` / `wr-recovery-center` suites.
  They `vi.mock("./lib/client.js")` and route `apiGet` by URL through the shared
  `src/test-helpers.js` `installApiRouter`; each store loads in isolation via `vi.resetModules()` +
  dynamic import (the active work falls back to a `__loading__` placeholder until a seeded
  `/api/v2/projects` resolves with a real `project_id`). Run `npm test` (or the `verify_windows.ps1`
  React gate / GitHub CI). Keep tests falsifiable — verify rollback/alert paths actually trip.
- `src/lib/client.js` owns the shared client contract (envelope / X-Idempotency-Key /
  X-Operator-Ref / `novel-system-api-base` localStorage override). The build-time API base is
  `VITE_NOVEL_SYSTEM_API_BASE` (default `http://127.0.0.1:8000`; the E2E lanes inject the `:8009`
  backend through it); views branch on `ApiRequestError.code` / `details`, never on message text.
- **Frontend guard tests** (they fail `npm test`, not just lint): `tooling-independence.test.js` —
  Playwright is pinned to an exact version in `frontend-react/package.json`, the static ESM graph has
  no cycles, **new ESM-only modules must not write to `window`** (a regex guard — even `window.x ===`
  reads as a write), and every module that uses the icon object `I` imports it from `icons.jsx`
  (文学质量 / 成本看板 once crashed on open because a test injected `globalThis.I`);
  `design-guard.test.js` — fails on `var(--x)` with no definition, animation names with no (or a
  duplicated) `@keyframes`, a stylesheet not imported exactly once by `main.jsx`, CSS rules whose classes
  no source / test / smoke references, raw colours in CSS or JSX outside the token file, `--tone` set by
  anything but `[data-tone]` or `:where()`, a pigment used as text on its own `-wash`, informative text on
  `--ink-4`, `font-size` below 11px or off the `--fs-*` scale, and ratchets JSX `<style>` injections,
  `z-index` literals above 40 and off-scale breakpoints (its `KNOWN_*` / `MAX_*` lists only shrink); `build-chunking.test.js` — views
  are `React.lazy` routes (retryable `lazyNamed` from `ws-lazy.jsx`, the `import("./ws-*.jsx")` literals
  stay in `ws-app.jsx`) with a mount handshake for cross-view intents
  (`ws-view-intents.js`), `main.jsx` must not preload business modules through side-effect imports,
  the snowflake and author routes must both assemble `SnowSync`, and the production build only splits
  `vendor` (`build-chunks.js`; domain chunks are decided by the lazy routes, not by manual chunking);
  `frontend-boundary-contract.test.js` — `WS_VIEW_ALIAS` redirects are `replace`-only in browser
  history, fonts are served from the app itself and `index.html` carries the baseline
  `Content-Security-Policy` meta (add no external hosts); `runtime-truth-contract.test.js` — no copy
  or fallback that points at the retired demo seed, and 设置 must not describe a cache clear as a
  server-side reset. `main.jsx`'s CSS import order carries cascade semantics — `styles.css` then
  `ws-ui.css` first (design-guard pins that), view files after; reorder only with a visual regression.
- localStorage holds only UI preferences and read caches of backend truth
  (`wr-doc:*` is a write-through cache of author-drafts); business writes all go
  through `/api/v1` + `/api/v2` endpoints.
- Test/E2E fixture works come from `backend/tests/fixture_works.py` (`seed_fixture_works`,
  project ids `work-a`/`work-b` — keep these literal ids) + `backend/tests/fixture_runtime.py`
  (`seed_runtime_fixture`, also seeds the `PRJ_DEMO_CH001` runtime fixture). These are neutral
  placeholder works for tests only — the product runtime never seeds them.
- Contract-level E2E: `cd frontend-react; node scripts/run-smokes.mjs [BASE] [API]`
  (runs the acceptance suite, `smoke-phase2..7`, `smoke-ai-settings`, and `qa2-ui`, reseeding via `python tests/fixture_runtime.py` before each suite; uses
  frontend-react's own locked Playwright install). Defaults are `BASE=http://127.0.0.1:5174/` and a **separate
  seeded backend** `API=http://127.0.0.1:8009` — not the dev `:8000`. `scripts/verify_react_e2e.ps1`
  now orchestrates this end-to-end (isolated e2e sqlite under `.codex-run/e2e/`, seeded `:8009`
  backend + React `:5174`, full process-tree teardown) and is wired into both GitHub Actions
  (`scripts/verify_react_e2e.sh`) and `verify_release.ps1` as a default gate. The Linux / CI lane is
  `NOVEL_SYSTEM_PYTHON=$PWD/backend/.venv/bin/python bash scripts/verify_react_e2e.sh` (it and the
  per-suite reseed default to a bare `python` on `PATH`; run dir `.codex-run/e2e-linux/`, ports
  overridable via `PLAYWRIGHT_BACKEND_PORT` / `PLAYWRIGHT_REACT_PORT`, needs `npx playwright install
  chromium` once). **Gotcha**: a fresh `alembic upgrade head` aborts on migration `20260523_0036`'s
  legacy-backup guard unless `backups/style_reference_legacy_*.json` exists; the committed
  `backups/style_reference_legacy_placeholder.json` satisfies it for every checkout, and the ps1 lane
  additionally points that migration's `STYLE_REFERENCE_REPO_ROOT` test override at a shim so the
  throwaway e2e DB never touches the real `backups/`. **Gotcha for any ad-hoc browser probe**:
  never point it at `:5174` while the author's live stack may run — `start-all-linux.sh` kills whatever
  holds 5174, and the live frontend's default API base is the real `:8000` backend. A localStorage
  `novel-system-api-base` override alone does not help: `client.js` ignores a loopback override unless
  `novel-system-api-base-default` is also set. Run probes on their own port (e.g. 5175 with
  `VITE_NOVEL_SYSTEM_API_BASE` pointing at a DB copy), set both keys, and abort `:8000` in the browser
  context (`context.route(/127\.0\.0\.1:8000/, r => r.abort())`).
- Current user and engineering documentation is indexed in `docs/README.md`; dated plans and
  evidence describe their original run and are not the current runtime contract.

### Database Migrations
```powershell
cd backend
python -m alembic current
python -m alembic heads
python -m alembic upgrade head
```

A `database operation failed` response usually means the schema is stale — run `upgrade head` first.

The current head is whatever `alembic heads` and `CURRENT_SCHEMA_REVISION` say (`20260923_0091` as of 2026-09-23). Back up before an irreversible migration **with the services stopped**: `python -m novel_system.tools.db_backup --backup <src.db> <dst.db>` (also `--verify PATH` / `--restore BACKUP DST`; writes a sidecar manifest, checks SHA-256, SQLite integrity and foreign keys; `scripts/db_backup_drill.{ps1,sh}` rehearse it on a copy). Never plain-copy `novel_system.db` while the backend runs — it is WAL-mode, so committed rows may still sit in `novel_system.db-wal`. `*.db` is gitignored everywhere, including `backups/`.

**Adding a migration means bumping `backend/src/novel_system/db/schema_contract.py:CURRENT_SCHEMA_REVISION` to the new head.** The older `tests/test_migration_*.py` files that upgrade to `head` and assert `alembic_version` (0077 / 0080 / 0081 / 0082 / 0085 / 0086) read `CURRENT_HEAD` from `schema_contract.CURRENT_SCHEMA_REVISION` since 2026-09-23, so they follow the bump; a new migration test should upgrade to its own revision explicitly (0078 / 0079 / 0084 and 0087 onwards do). `GET /ready` compares the live `alembic_version` with that constant and every launcher (`start-dev.cmd`, `start-all-linux.sh`, the React contract E2E lane) waits on `/ready`; migration `20260913_0084` shipped without the bump, so every install migrated to the new head reported `schema_revision_mismatch` forever and CI's E2E job went red twice while the unit suites stayed green. `tests/test_schema_contract_revision.py` now pins the constant to the Alembic head, so a forgotten bump fails the suite instead of the launchers.

Migration `20260923_0091` (风格参考 v3 cleanup) drops `style_reference_finding_feedback`, `style_reference_validation_reports` and `style_reference_findings.base_confidence` (the downgrade recreates the structure, not the data). Migration `20260923_0090` creates the v3 tables `style_reference_jobs` / `style_reference_windows` / `style_fidelity_readings` / `style_reference_scene_windows` (reversible). Migration `20260920_0089` adds the nullable `snowflake_chapter_plans.catalog_chapter_id` (2026-09-20 阶段 Y: a chapter's catalog id is pinned on its chapter plan instead of being derived from its position; the backfill pins a live chapter plan only when all its live scene plans point at one existing catalog chapter of the project and no second plan claims it; reversible; see the 阶段 Y bullet under Key Architectural Concepts). Migration `20260917_0088` adds `turn_kind` (backfilled `chat`) / `candidates_json` / `brief_delta_json` / `adoption_json` to `snowflake_assistant_turns` (2026-09-17 阶段 U: 「先看 3 个方向」 is a `candidates` turn of the coach log, each turn keeps the brief delta it caused, and a generate that followed a turn's direction marks it `adoption_json`; see the 阶段 U bullet under Key Architectural Concepts). Migration `20260916_0087` creates `snowflake_direction_briefs` (2026-09-16 阶段 T: the author's per-step intent brief distilled from the coach — one row per (project, step), unique index `ix_snowflake_direction_briefs_step`; see the 阶段 T paragraph under Scene Triage / Snowflake Assistant below). Migration `20260915_0086` adds the nullable `exception_reason` column to `snowflake_scene_plans` (2026-09-15 阶段 N: the author's stated reason for a scene that deliberately breaks the trio rule — Ingermanson's own 「冲突：无」 closing lesson and trio-less wrap-up scenes; with a reason the rule layer stops counting the missing beats as missing, the structure brief renders an `Author's exception` line, and drafting / QC / near-final / AI triage judge the scene by the reason). Migration `20260914_0085` adds the nullable `expected_reader_emotion` / `story_time` columns to `snowflake_scene_plans` (2026-09-14 阶段 J: Ingermanson's on-stage cast, time-stamp and "what the reader should feel" scene columns; on-stage cast reuses `onstage_chars_json`; the book-level `narrative_stance` lives in the `book_brief` draft and reaches drafting through the design context's binding `Narrative stance` line; it is optional, and `snowflake_staleness.semantic_payload` treats any empty top-level value as absent, so a brief approved before this phase is not flipped back to pending_review by the first re-save that carries `narrative_stance: ""`). Migration `20260716_0073` **irreversibly** rewrites historical LLM-audit payloads (prompts, drafts, model output, provider error bodies) into bounded fingerprints, and `20260904_0083` irreversibly drops the tables of the retired feature clusters — back up an existing DB before upgrading past either. `GET /ready` performs the structural check (revision + required columns); the standalone preflight tool was removed.

### Author-State Reset
Wipes all project/snowflake/chapter data while preserving reference profiles and system config:
```powershell
cd backend
python -m novel_system.tools.reset_author_state           # dry-run
python -m novel_system.tools.reset_author_state --execute --yes
```

## Environment Variables

| Variable | Default | Purpose |
|---|---|---|
| `NOVEL_SYSTEM_DATABASE_URL` | `sqlite:///./novel_system.db` | SQLAlchemy DB URL |
| `NOVEL_SYSTEM_VECTOR_BACKEND` | `memory` | `memory` (deterministic, no ChromaDB — the code default in `settings.py:get_settings`, and `start-dev.cmd` / the Linux start scripts force it as well) or `chroma` (real ChromaDB; only exercised by the `chroma_integration` lane) |
| `NOVEL_SYSTEM_CHROMA_DIR` | `./.vector_store` | ChromaDB persistence path |
| `NOVEL_SYSTEM_LLM_ENABLED` | `false` | Enable real LLM calls |
| `NOVEL_SYSTEM_LLM_PROVIDER` | `openai_compatible` | Provider key resolved against the `services/llm_providers/` adapter registry (12 adapters: `openai_compatible`, `openai`, `anthropic`, `deepseek`, `zhipu_glm`, `gemini`, `qwen_dashscope`, `moonshot`, `minimax`, `doubao_ark`, `xai`, `ollama`) |
| `NOVEL_SYSTEM_LLM_BASE_URL` | `https://api.openai.com/v1` | Provider base URL |
| `NOVEL_SYSTEM_LLM_API_KEY` | — | API key for the provider |
| `NOVEL_SYSTEM_LLM_TIMEOUT_SECONDS` | `900` (`runtime_defaults.DEFAULT_LLM_TIMEOUT_SECONDS`) | Per-call LLM response timeout in seconds. `0` removes the response ceiling (wait as long as the model needs — slow long-context tasks are normal work, not faults); connection setup always keeps the finite `LLM_CONNECT_TIMEOUT_SECONDS` (30 s, `services/llm_client.py`) so an unreachable endpoint still fails fast. Precedence: a per-task `timeout_seconds` on the `task_routing` entry (the shipped `config/models.yaml` sets none) → the saved 系统配置 LLM snapshot's `timeout_seconds` (new snapshots default to 900) → this env var. Connectivity probes ("测试连接") are always finite and ignore all of these |
| `NOVEL_SYSTEM_ADMIN_TOKEN` | — | Admin endpoint auth token |
| `NOVEL_SYSTEM_CONFIG_SECRET` | — (launchers generate one) | Key that encrypts the 系统配置 snapshots holding provider API keys; secret-management endpoints answer 403 `CONFIG_SECRET_REQUIRED` without it. `start-dev.cmd` and `scripts/start-backend-linux.sh` generate a random value on first start and **persist it in `.codex-run/config.secret`** (0600), reusing it on every restart — delete that file, or start uvicorn by hand with a different value, and every provider key saved through the UI becomes undecryptable (`scripts/cleanup_runtime_artifacts.py` deliberately never prunes it; the E2E lanes use a throwaway value against their own DB) |
| `NOVEL_SYSTEM_AUTO_CREATE_TABLES` | `false` | If true, bypass Alembic and `create_all` tables on startup — **dangerous**: hides schema drift (see the schema-drift guard) |
| `NOVEL_SYSTEM_LLM_AUTO_CRITIQUE_ENABLED` | `false` | §8 opt-in: layer an independent LLM editor critic on top of the rule-based pass in `orchestrator.py` (after Best-of-N, before soft QC) |
| `NOVEL_SYSTEM_LLM_EVENT_EXTRACTION_ENABLED` | `false` | §2 opt-in: extract narrative events from finished prose (`prose_event_extractor.py`) |
| `NOVEL_SYSTEM_SCENE_BEST_OF_N_ENABLED` | `false` | 2026-09-14 opt-in: multi-candidate style drafts. Off = every scene drafts one candidate (the pre-existing behaviour). On = `scene_criticality` decides: transition 1, standard 2 with progressive top-up to 3, critical 3 → 5 and the run pauses at the **blinded author terminal selection** (`awaiting_candidate_selection`, React 关键场景 · 匿名候选终选 panel, `POST /api/v1/scenes/{id}/style-candidates/{row_id}/select` then `resume-after-selection`). Costs N style passes per non-transition scene. Under a `style_first` binding the candidates are the first draft + (N−1) targeted revisions ranked by fidelity `distance` (the first draft wins ties) |
| `NOVEL_SYSTEM_CHROMA_COLLECTION_PREFIX` | `novel_system` | Prefix for ChromaDB collection names |
| `NOVEL_SYSTEM_CORS_ORIGINS` | `…:5173/5174/5175/8081` | Comma-separated allowed CORS origins |
| `NOVEL_SYSTEM_EXPOSE_ERROR_DETAIL` | `false` | Whether the error envelope exposes `error.details` |
| `NOVEL_SYSTEM_LOCAL_ONLY` | `true` | Loopback-only: rejects non-loopback clients and forwarded headers. Setting `false` **requires** `NOVEL_SYSTEM_REMOTE_ACCESS_TOKEN` (`create_app` refuses to start otherwise) |
| `NOVEL_SYSTEM_REMOTE_ACCESS_TOKEN` | — | Shared access token when not local-only; the browser sends it as `X-Novel-Access-Token` (frontend default injectable via `VITE_NOVEL_SYSTEM_ACCESS_TOKEN`, runtime value lives in sessionStorage). Not user auth/RBAC |
| `NOVEL_SYSTEM_LLM_DAILY_TOKEN_LIMIT` / `…_MONTHLY_TOKEN_LIMIT` / `…_PROJECT_DAILY_TOKEN_LIMIT` / `…_DAILY_REQUEST_LIMIT` / `…_MAX_CONCURRENT_REQUESTS` | `0` (every fence off) | LLM quota family, all opt-in: `0` disables a fence, a positive value arms it and the accounting layer enforces it *before* provider dispatch. A single-author desktop install fences off nobody but the author, so nothing ships armed |
| `NOVEL_SYSTEM_LLM_DAILY_COST_LIMIT_USD` (+ `…_LLM_INPUT_COST_PER_MILLION_USD` / `…_LLM_OUTPUT_COST_PER_MILLION_USD`) | `0.0` (off) | Optional daily cost cap; a nonzero cap requires nonzero unit prices |
| `NOVEL_SYSTEM_LLM_RESERVATION_RECOVERY_TTL_SECONDS` | `3600` | TTL after which orphaned LLM accounting reservations are reclaimed |
| `NOVEL_SYSTEM_SCENE_TOKEN_BUDGET_MULTIPLIER` | `0` (disarmed) | Per-scene lifecycle budget: the end-to-end token ceiling is `N × single-shot baseline` plus finite business/provider attempt caps. This was the last hard fence that shipped armed; it now joins the fence family — `0` = no scene ceiling (finite sentinel budgets, the pre-dispatch CAS gate is a no-op, so a single scene run is never blocked mid-draft). Accounting is unchanged (the ledger + 成本看板 still record every token; the dashboard shows a disarmed scene as 不限). A positive `N` re-arms `N × baseline`, and the attempt caps fall back to `config/models.yaml` `retry_budget`. Only affects **newly** initialized scenes — a scene whose immutable `scene_budget_basis_json` already exists keeps it (expand it via the author topup) |
| `NOVEL_SYSTEM_SNOWFLAKE_INPUT_TOKEN_BUDGET` | `0` (use per-template value) | Snowflake-workspace prompt **input** budget override, in estimated tokens. `0` = use each template's declared `input_token_budget` (the whole family is `24000`, a measured number — see the comment above `snowflake_generate_book_brief` in `config/prompts.yaml`). Set a positive value to tighten it for a small-context local model. Over-budget payloads are shed by relevance via `services/snowflake_prompt_budget.py` — never the step contract, the author's adopted direction, or the focused members — and the shedding is recorded in the LLM audit summary (`prompt_budget_*` fields) plus surfaced to the author as `health.generation_notice` when the ladder still can't fit |
| `NOVEL_SYSTEM_SCENE_INPUT_TOKEN_BUDGET` | `0` (use per-template value) | Prompt **input** budget override for the scene / planning / chapter template families that go through `services/prompt_builder.py` (scene-run drafting + QC + near-final review, writer scene/chapter diagnosis + revision, deep review + passage patch, author structure-extract / proposal, scene blueprint + chapter architecture + character pressure). `0` = use `max(template.input_token_budget, RUNTIME_MIN_INPUT_BUDGETS[name])` — the measured family values `24000` (scene) / `8000` (planning + passage patch) / `30000` (chapter), see the comment block above `neutral_draft` in `config/prompts.yaml`; the code floor is what protects a live install whose DB prompts snapshot still carries the pre-estimator values. A positive value replaces both (tighten for a small-context local model); it does not touch the snowflake family, which has its own `NOVEL_SYSTEM_SNOWFLAKE_INPUT_TOKEN_BUDGET` |
| `NOVEL_SYSTEM_SCENE_STRUCTURE_BRIEF` | `true` | 2026-09-13 阶段 A: render the scene's structure (form, POV, crucible, Goal/Conflict/Setback or Reaction/Dilemma/Decision, cost, must-withhold) from the raw `SceneCard.writer_brief_json` keys as the `Scene Structure (Snowflake)` fact section of the drafting bundle, the scene-blueprint source snapshot and the near-final planning snapshot (`services/scene_structure_brief.py`). `false` is a rollback switch only — it removes the section and nothing else |
| `NOVEL_SYSTEM_SCENE_DESIGN_CONTEXT` | `true` | 2026-09-13 阶段 F: render the **confirmed** snowflake design around the scene (book logline, five-sentence spine + moral premise, the chapter's act / 灾 mark / goal from the chapter table, scene position, the POV character's sheet with values / storyline, a ≤300-char excerpt of their 视角故事, on-stage one-liners, what the previous scene ended on and the next opens on) as the compressible `Scene Design Context (Snowflake)` section right after the structure brief in the drafting bundle and the blueprint snapshot (`services/scene_design_context.py`; only `approved` / `stale` step versions count, `pending_review` never becomes a fact; the referenced `step_run_id`s go into `source_version_refs` so the bundle hash follows the design). It is background, not facts: the budget compresses it to essentials before any fact section and omits it as the last soft section; `hard_qc`-kind tasks never see it. `false` is a rollback switch only |
| `NOVEL_SYSTEM_CONTENT_SAFETY_MODE` | `review` | `review` or `audit` — content-risk triage mode on publication actions |
| `NOVEL_SYSTEM_SQLITE_FOREIGN_KEYS_ENABLED` | `true` | Enforce FK pragma on SQLite connections |
| `NOVEL_SYSTEM_CORS_ALLOW_CREDENTIALS` | `true` | CORS credentials flag |
| `NOVEL_SYSTEM_STYLE_REFERENCE_IMPORT_ROOTS` | — | Allowed filesystem roots for style-reference path imports (path import is disabled until at least one root is configured) |
| `NOVEL_SYSTEM_MAX_REQUEST_BODY_BYTES` | `16777216` (16 MiB) | Request-body ceiling installed by `create_app` (`api/request_limits.py`); a 参考书 upload above it is rejected before ingest |
| `NOVEL_SYSTEM_PROTECTED_SOURCE_TERMS_JSON` | — (empty) | JSON list of project-independent protected source terms; the single copy gate (`services/reference_copy_gate.py`) checks them next to the bound profile's generation-scope banned terms (incl. the learn job's `protected_auto` names) |
| `NOVEL_SYSTEM_ENABLE_FIXTURE_IMPORT` | `false` | Parsed into `settings.fixture_import_enabled` but has **no consumer** since the fixture-import endpoint was removed (`tests/test_fixture_import_boundary.py` keeps it gone); `conftest.py` still sets it. Subtraction candidate |

Launcher / lane / test-only variables the application itself never reads: `NOVEL_SYSTEM_BACKEND_PORT` (8000), `NOVEL_SYSTEM_FRONTEND_PORT` (5174), `NOVEL_SYSTEM_NODE_BIN` and `NOVEL_SYSTEM_ARTIFACT_RETENTION_DAYS` (14 — the Linux launchers prune `.codex-run` logs / pids / `e2e*` dirs older than this through `scripts/cleanup_runtime_artifacts.py`; DBs, backups and `config.secret` are never candidates) for `scripts/*-linux.sh`; `NOVEL_SYSTEM_PYTHON` (interpreter used by `scripts/verify_react_e2e.sh` and by `run-smokes.mjs`'s per-suite reseed — defaults to a bare `python`); `STYLE_REFERENCE_REPO_ROOT` (migration `0036`'s test-only override of the repo root for its backup guard); `NOVEL_SYSTEM_STYLE_REF_LOCAL_CORPUS` (opt-in real book for `tests/test_style_reference_local_corpus.py`); frontend build-time `VITE_NOVEL_SYSTEM_API_BASE` (default `http://127.0.0.1:8000`) and `VITE_NOVEL_SYSTEM_ACCESS_TOKEN`.

## High-Level Architecture

### Backend (`backend/src/novel_system/`)

FastAPI application (`api/app.py`) with one router per domain area (`api/routes/`). Each route file maps to a service in `services/`. The `db/models.py` file contains all SQLAlchemy ORM models; `db/session.py` manages the engine. The authoritative list of mounted routers is the `include_router` calls in `api/app.py`; `api/routes/__init__.py`'s `__all__` now mirrors it one-to-one, enforced by the drift guard `backend/tests/test_routes_all_manifest.py`.

**Domain layers:**
- `services/projects.py` + `services/snowflake_workspace.py` — Snowflake Method planning pipeline
- `services/snowflake_planner.py` + `services/snowflake_steps.py` — step catalog, completeness gates, materialization rules
- `services/snowflake_workspace_llm.py` + `services/llm_task_runner.py` — LLM call orchestration (the author direction brief and the coach's `conversation` are passed in by `snowflake_workspace.py` as plain payload dicts — the LLM layer only knows the protected key name; `services/snowflake_direction_brief.py` is a leaf). Two non-obvious contracts live in the first file: 「场景规划」(`scene_details`) full-table generation is **batched server-side** (`SCENE_DETAIL_BATCH_SIZE` scenes per call, `SCENE_DETAIL_MAX_BATCHES_PER_RUN` batches per request, resuming at the first scene that still has a missing field) because one call for a whole scene table always overflows `max_output_tokens`; and every task on this path goes through `services/snowflake_prompt_budget.py` for a real, enforced **input** budget (see the env var table). Progress, partial failures, and un-fittable payloads all report through `WorkspaceLLMResult.notice` → `health_json.generation_notice` → a warning toast in `ws-snow.jsx` — a half-finished or shed generation is never reported as a plain success. **Third contract (2026-09-16): the wire schema is enriched from the step catalog.** Both OpenAI adapters send a template's `structured_schema` as a `json_schema` text format, and the collection templates (`characters[]` / `scenes[]` / `chapters[]`, the coach's `candidate_patch`, the triage `items[].repair_patch`) only declared `additionalProperties: true` — a schema-enforcing backend (a Gemini relay in `responses` mode) can then only decode `{}` per member, which surfaced as 「角色摘要表生成结果过于稀疏」 on 采纳并结构化 (audit output was literally `[{},{}]`). `enrich_structured_schema` derives the member `properties` from the step's editor `template` at request time (`_run_structured_task(schema_step_key=…)`; the audit's `response_schema_enriched` lists the patched members, e.g. `characters_items`), the prompt carries `current_draft_how_to_use` (this step's blanks are targets, upstream blanks are the author's), a still-sparse result gets one reason-carrying retry like the count-mismatch path (`details.sparse_output`, Chinese message), and `{}` members never mint phantom ids. `tests/test_snowflake_structured_schema.py` guards all of it; a step-less template such as `snowflake_chapter_plan_suggest` must carry its `properties` in `prompts.yaml` itself
- `services/llm_client.py` + `services/llm_providers/` — multi-provider LLM client built on a **pluggable adapter registry** (`llm_providers/registry.py` + `presets.py`, one adapter module per provider, 12 in total). Edit provider behavior / default base URLs in `llm_providers/`, not `llm_client.py`
- `services/scene_generation.py` + `services/qc_engine.py` — scene pipeline and quality gates
- `services/style_reference/` (+ `services/style_policy.py`, `services/style_prompt_injection.py`, `services/style_fidelity_view.py`) — reference-book style subsystem (import + LLM paragraph classification → learn job → dimension card + persisted windows → per-scene frozen injection → fidelity readings / 对照检查). See "Style Reference subsystem" below
- `services/reference_copy_gate.py` — the single copy gate (12-char n-gram against the bound book + protected terms) on every path into canonical text; `services/source_safety.py` keeps the protected-term matching helpers and `services/reference_safety.py` is a thin read-only entry on top of the gate
- `services/vector_store.py` — ChromaDB abstraction; swapped for in-memory store when `NOVEL_SYSTEM_VECTOR_BACKEND=memory`
- `services/idempotency.py` + `services/hash_engine.py` — idempotency contracts for LLM calls and content hashing
- `services/orchestrator.py` (+ `bundle_builder.py`, `scene_execution.py`) — the scene-run pipeline (bundle context → generate → Best-of-N → optional auto-critique → QC). The **blueprint quality-floor v2 / anti-AI-taste** cluster hangs off it: `services/literary_quality.py` (21 weighted quality dimensions incl. `perception_filter` / `self_repetition` / `conflict_too_clean`; route `/api/v1/literary-quality`), `services/best_of_n_blind_eval.py`, `services/self_repetition.py` (cross-scene n-gram + semantic guard, reuses the style-reference plagiarism engine), `services/auto_critique.py` (Reflexion-style editor pass), `services/scene_criticality.py`
- Narrative-coherence / continuity overlay (backs the scene-run pipeline): `services/narrative_event_log.py` + `services/prose_event_extractor.py` (append-only event sourcing, populated by the 成稿中心 正史 extract flow), `services/canon_continuity.py`, `services/character_continuity.py` (deterministic pronoun/identity drift checks), `services/pov_knowledge_projection.py` (who-knows-what redaction). The theme/tension/foreshadow/arc/psychology/relationship/voice/drift advisory modules were removed in the 2026-09 subtraction
- **LLM cost cluster**: `services/llm_accounting.py` (durable accounting boundary around *every* provider POST — short reservation → dispatch → settlement transactions, no DB write txn held during network I/O; quotas fail before dispatch), `services/cost_aggregation.py` + `services/pricing.py` + `config/pricing.yaml` (centralized per-provider/model price snapshots, all placeholder rates marked `is_estimate: true`), `services/llm_audit.py` (audit rows store bounded fingerprints, not payloads — see migration `0073`), route `api/routes/cost.py`
- **Persistent jobs & crash recovery**: `services/scene_run_jobs.py` (cancellable persistent scene-run jobs) + `scene_run_checkpoint.py` + `scene_run_preflight.py`, `services/chapter_runner.py` (advanced-mode 运行本章 chapter jobs with polled progress), `services/background_recovery.py` (`run_startup_recovery` wired into the FastAPI lifespan — startup scan submits durable candidates; workers own the CAS so duplicate scans from multiple ASGI workers are harmless)
- **Canonical final-text lifecycle**: `services/canonical_manuscripts.py` + `services/final_text_gate.py` + `services/chapter_approval.py` — 成稿中心 final approval requires an explicit read-through confirmation of the *server-side* text, then a body-hash confirm, then project-level `approve-final`; reopening requires a reason and the server cascades revocation of downstream approvals. `services/archiver.py` and `services/narrative_position.py` support this layer
- `services/content_safety.py` — deterministic, auditable content-risk triage on publication: blocks only unattended publication of a few compound high-risk patterns; the author acknowledges exact finding codes on the exact publication action (dark themes stay writable, ordinary genre violence is advisory)
- `services/author_preferences.py` + `services/author_instructions.py` — scoped, prompt-safe author preference/instruction summaries shared by generation, review, and bundle construction

**Key data models** (all in `db/models.py`):
- `StoryProject` / `OutlinePlan` — top-level novel project and its outline
- `SnowflakeArtifact` / `SnowflakeStepRun` — per-step artifacts and run state for the 10-step snowflake
- `SnowflakeScenePlan` / `SnowflakeSceneTriageItem` — scene-level plans and quality triage
- `SnowflakeCharacterPlan` — per-character snowflake data
- `SnowflakeAssistantTurn` / `SnowflakeDirectionBrief` — the coach's persisted turns (`turn_kind` `chat` | `candidates` since 阶段 U, with `candidates_json` / `brief_delta_json` / `adoption_json`) and, per step, the author's intent brief distilled from them (阶段 T, 2026-09-16)
- `ChapterGoal` / `SceneCard` — materialized chapter/scene production units (created by structure materialization)
- `StyleReferenceBook` / `…Paragraph` / `…Run` / `…Extraction` / `…Finding` / `…Evidence` / `…Quote` / `…Profile` / `…InjectionBinding` / `…BannedTerm` / `…MetricEvent` + the v3 `StyleReferenceJob` / `StyleReferenceWindow` / `StyleFidelityReading` / `StyleReferenceSceneWindows` — the Style Reference subsystem's table family (the run / extraction / finding / evidence / quote rows are the learn job's lineage)
- `NarrativeEvent` (append-only event-sourcing log — replay events up to a scene to reconstruct entity state), `VolumeSummary`, plus blueprint-v2 columns (`SceneCard.constraint_intensity`, `SceneRunState.criticality_level` / `candidate_dispersion_score`) — the causal/foreshadow/theme overlay on the snowflake pipeline
- `LlmCall` / `LlmCallAttempt` — durable LLM accounting rows (reservation → dispatch → settlement); payload columns hold bounded fingerprints since migration `0073`
- `ChapterRunJob` / `BackgroundRecoveryLease` — persistent chapter runs and the startup-recovery lease
- `AuthorPreferenceProfile` — scoped author preferences injected into prompts

**Configuration** lives in the project-root `config/` directory (not inside `backend/`):
- `config/models.yaml` — model profiles (`local_fast`, `quality_strong`, `dual_track`), task routing (task name → provider/model/temperature/response_format), and top-level `retry_budget` + `job_runtime` (lease/idempotency TTLs). The blueprint-v2 quality floor is config-driven here too: new task-routing entries (`scene_blueprint`, `character_pressure_blueprint`, …) and decoding penalties on `stylize` (`frequency_penalty` / `presence_penalty`, §7 anti-mean sampling)
- `config/prompts.yaml` — prompt templates with `system_prompt`, `task_prompt`, `structured_schema`, and `input_token_budget` (incl. the Scene Literary Blueprint v2 / Character Pressure Blueprint templates)
- `config/allowlists.yaml` / `config/hash_contract.yaml` — domain policy files
- `config/style_reference/` — Style Reference policy files: `injection_budget.yaml` (sample-window / card budgets, `draft_mode_default`, style-first length slack and reference scene scale, `continuity_anchor_max_chars`, the `fidelity:` thresholds of the style step and soft-patch rollback), `function_words.yaml` (the measurement kernel's closed-class word table), `voice_baseline.yaml` (golden-corpus quantiles, now only for the deliberate-repetition flag — rebuild with `python -m novel_system.services.style_reference.voice_signature build-baseline`), `input_thresholds.yaml` (per-layer corpus gates of the learn job), `tolerance_floors.yaml`, `banned_adjectives.yaml`, `anti_plagiarism_template.txt`
- `config/pricing.yaml` — per-(provider, model) price snapshots with `effective_at`, used by cost aggregation (shipped rates are placeholder estimates, `is_estimate: true`)

Runtime LLM config can also be stored in the DB (via `SystemConfigSnapshot`) and applied on top of env vars by `settings.py:get_settings()`.

**Gotcha — the DB snapshot wins over the repo file.** Once the 系统配置 UI has saved a category, `load_active_config_payload(category)` returns the *active DB snapshot* and the repo file is never read: editing `config/models.yaml` (or `prompts.yaml`) then has **no effect on a running install**, silently. Re-importing the whole file is not a safe workaround either — it clobbers the per-node provider/model routing configured through the UI. To change one field on a live install, patch the active snapshot and activate a new version; `tools/raise_llm_output_budget.py` does exactly that for `max_output_tokens` (dry-run by default, `--execute` to apply) and is the pattern to copy; `tools/sync_prompt_templates.py` does it for `prompts.yaml` after any template edit (every template by default, `--prefix` / `--template` narrow it, a UI-edited prompt whose `version` did not move is kept unless `--force-text`). **Second gotcha — two routing tables, `node_routing` wins.** `resolve_node_route` reads the UI-synced `node_routing` first and only falls back to `task_routing`; 「一键补齐」 seeds `node_routing` from the `llm_node_registry` spec defaults. So a `models.yaml` `task_routing` value (e.g. `snowflake_step_generate: 8192`) is silently shadowed by a smaller spec default that was synced earlier (`snowflake_step_generate` was 3200 until 2026-09-16 — every 一键补齐 install generated whole character / scene tables under a 3200 cap). Keep the spec and `task_routing` equal (`test_snowflake_structured_schema.py` pins this node), and the raise tool now patches both tables.

**LLM node registry** (`services/llm_node_registry.py`) defines the catalog of all LLM-calling nodes as `LLMNodeSpec` dataclasses (node_id, model, temperature, reasoning_level, api_mode). The system config UI routes each node to a specific provider at runtime. `config/models.yaml` provides task-level defaults; DB-stored routes override them.

**Response envelope**: all API responses use `{ok: bool, data: ..., error: {code, message, details}, request_id}` (see `api/response.py`). Frontend `client.js` parses this envelope and throws `ApiRequestError` with structured fields on failure.

**Request middleware**: every request gets `request.state.request_id` (a hex prefix) and `request.state.operator_ref` (from `X-Operator-Ref` header or `"operator"`). Mutating frontend calls pass `X-Idempotency-Key` and `X-Operator-Ref` headers for idempotency and audit trails.

**Author-action pattern** (`services/author_actions.py`): when the backend detects a missing prerequisite (e.g., LLM not configured, step incomplete), it returns an `author_action` dict that tells the frontend which view to navigate to and what button to show. This avoids hard-blocking the user while still guiding them.

**Style Reference subsystem (风格参考 v3, 2026-09-23)** — current reference: `docs/style-reference.md`; change record, author decisions and the full issue ledger: `docs/style-reference-v3-2026-09-23.md`; older docs: `docs/history/style/` (their own run only). Goal: **full imitation** of one reference author — with a binding the snowflake design is only the framework (what happens, who, causality, order, POV); voice, humour, imagery, rhythm, dialogue and stance follow the reference. UI = three steps 参考书 → 学习文风 → 用于作品 (`frontend-react/src/ws-styleref*.jsx`, store `ws-styleref-store.js`, vocabulary `ws-labels.js`); routes = package `api/routes/style_reference/` (`books` / `learn` / `profiles` / `bindings` / `activity`, prefix `/api/v2/style-reference`) + `api/routes/style_fidelity.py`.
- **Jobs** (`services/style_reference/jobs.py`, table `style_reference_jobs`, kinds `classify` / `learn` / `check`): a claim mints an `owner_token` and every later write is conditional on it (the old worker of a swept / cancelled / deleted-book job just stops). Heartbeat 15 s, stale after 60 s; the lifespan starts `start_job_sweeper` (at boot, then every 30 s: requeue stale, dispatch queued to a 2-thread pool). Handlers register on import (`import_job`, `learn_job`, `check_job`). `GET …/activity` lists job entries (`job:<id>`) only.
- **Classify** (`import_job.py`, `segmentation/llm.py`; batches ≤6,000 chars, 3 in parallel): modes `import` / `reclassify` (destructive) / `retype` (in place, keeps profile + bindings). **Learn** (`learn_job.py` + `learn_*.py`, `protected_terms.py`): windows → select (~12 windows / 40k chars) → 4 layer extractions → dimension card → protected terms (`protected_auto` banned terms) → window tags → finalize (in-place update of the same `profile_id`).
- **Data**: migration 0090 added `style_reference_windows` (persisted window index: kernel features, typicality, tags), `style_fidelity_readings`, `style_reference_scene_windows`; 0091 dropped the 👍/👎 and old validation-report tables. `profile_json` v3: `dimension_card` (16 dims, do / avoid lines with evidence, temperament), `card_line_states` (✓ pinned / ✗ excluded), `voice` (+ the transitional alias `voice_signature` without the distribution — the contract allow-list and the deliberate-repetition readers still use it; keep it), `structure_card`, `planning_guidance`, `narrative_guidance`, `learned_from`, `protected_terms_version`. Binding `config_json` = `reference_mode` (full / samples_only / card_only; `segments_only` books are forced to card_only), `sample_windows` (0–16, default 12), `dimension_states` (emphasize / normal / exclude), `draft_mode` (style_first / neutral_first); `binding_config.normalize_binding_config` is the only interpreter; `POST /profiles/{id}/apply` keeps one active binding per target.
- **Injection**: `services/style_policy.py` `StylePolicy` is the only "bound / defer house taste / how to send" decision (`style_policy_for_bundle` memoised per contract hash, `style_policy_live` for the desks); runtime contract v2 freezes the single most specific binding. `inject/selection.py` freezes each scene's windows once (quotas: position ≤3, situation tags ≈4, devices ≈2, rest typicality-weighted over the whole book), so every node of a scene sees the same windows; `inject/render.render_style` takes a `StyleRenderRequest(role=draft|revise|review|plan)` — review nodes get the first `REVIEW_K`=4, planning `PLAN_K`=3; draft / revise put the samples at the **tail of the user message** (`apply_style_user_tail`), the system prompt keeps the card, voice, recent gaps and the never-truncated red line. Entry point: `style_prompt_injection.inject_style_reference_prefix`.
- **Drafting (style_first)**: facts-only `scene_blueprint_facts` (situation tags) → `style_first_draft` → `hard_qc` → fidelity reading (`fidelity.py`, against the author's own windows; kernel `measure.py`) → `style_step.py`: in range → no model call, else `style_targeted_revision` (style_draft route) on the out-of-band dimensions, kept only if closer and through the copy / safety gates → soft QC = reference review (16 dims 0–10), a patch that is not better is reverted → near-final → archive. Thresholds: `injection_budget.yaml` `fidelity:` (provisional). House taste defers only via `policy.defers_house_taste()`; hard gates never. `readings.record_fidelity_reading` is the only readings writer; 对照检查 = the `check` job (`POST /api/v2/style-reference/checks`, template `style_ref_check_judge` on the `soft_qc` route).
- **Copy gate**: `services/reference_copy_gate.py` guards every path into canonical text (archive / final text gate, adopt, 成稿中心, writer proposals): ≥12 normalized chars shared with the bound book, or a protected term → 409 `SOURCE_SAFETY_BLOCKED` + `author_action`; it records hashes and positions only.
- **Gotchas**: (1) installs with a prompts snapshot need `sync_prompt_templates --execute` after any template edit (the learn job refuses stale templates: 409 `STYLE_REFERENCE_LEARN_CONFIG_MISSING`); (2) the v3 nodes `style_ref_protected_terms` / `style_ref_tag_windows` need routes (系统配置「一键补齐」); targeted revision, the check judge and the facts blueprint reuse existing routes; (3) output budgets: registry spec = `models.yaml` task and a saved models snapshot wins — `raise_llm_output_budget --node … --floor …` (classify 8192 with reasoning off, extract / synthesize 16384, soft_qc / near-final 5000); (4) strict LLM: no heuristic fallback in product paths; `local_only` books need a local model on the node's actual route; (5) changing sentence splitting, the heuristic classifier or the measurement kernel (the kernel also bumps `measure.KERNEL_VERSION`) means regenerating `tests/golden/style_reference/expected/*.json` (`regen_expected.py`) and `voice_baseline.yaml` (`voice_signature build-baseline`) — say why in the commit; (6) public repo: tests use synthetic text and neutral names only, a real book only via `NOVEL_SYSTEM_STYLE_REF_LOCAL_CORPUS`; (7) import trap: in a worktree's `backend/`, `python -m novel_system…` / `python -m alembic` may import the live checkout through the editable `.pth` — run CLIs from a neutral dir with `PYTHONPATH=<worktree>/backend/src` and check `novel_system.__path__` (pytest is fine: `pythonpath=["src"]`); (8) `purge_style_reference_books --book ID` and/or `--id-prefix P` / `refresh_style_reference_books --book ID | --all` dry-run unless `--execute`.

### Key Architectural Concepts

- **Snowflake Method pipeline**: The primary authoring flow. Author progresses through 10 ordered steps (reader positioning → one-line summary → one-paragraph summary → character summaries → one-page synopsis → character backstories → long outline → character bibles → scene list → scene planning). Each step produces a `SnowflakeStepRun` with draft JSON. Steps 1, 2, 3, 9, 10 are hard gates for structure materialization; others produce warnings.
- **Structure Materialization**: `POST /api/v2/projects/{id}/snowflake-workspace/materialize` converts approved `SnowflakeScenePlan` rows into `ChapterGoal` and `SceneCard` records. Proactive scenes get `Goal/Conflict/Setback` written to `SceneCard.writer_brief_json`; reactive scenes get `Reaction/Dilemma/Decision`. **Gotcha (fixed 2026-09-13, 阶段 A)**: `writer_briefs.normalize_scene_writer_brief` only keeps the 13 `writer_brief_v2` keys, so these snowflake keys never survived into the drafting bundle / blueprint / preflight — the writer saw only unlabeled `Beats`. `services/scene_structure_brief.py` now reads the raw keys (any scene whose brief carries a three-beat trio or a crucible, snowflake-materialized or hand-filled in 章节编排) and injects a labeled `Scene Structure (Snowflake)` fact section right after `scene_card` in `bundle_builder.py`, `scene_blueprint._source_snapshot` and `near_final._source_snapshot`; it is never compressed or omitted by the context budget. The drafting / planning / QC templates (`neutral_draft` v7, `style_first_draft` v2, `scene_blueprint` v5, `hard_qc` v4, `soft_qc` v6, `near_final_acceptance_review` v5) say what to do with it: proactive = Goal early → escalating Conflict → the designed Setback, reactive = Reaction felt → Dilemma → committed Decision; the ending outcome, POV, cost and must-withhold are bundle facts for `hard_qc`, scene-shape gaps are `scene_shape` patch guidance for `soft_qc`. `scene_run_preflight` reports `SCENE_STRUCTURE_INCOMPLETE` (missing beats) for structured scenes instead of the v2 `SCENE_LITERARY_INTENT_INCOMPLETE`. Tests: `tests/test_scene_structure_brief.py`. **阶段 F (2026-09-13, second-round evaluation)**: the rest of the confirmed design (02/03/04/06, the chapter table, neighbouring scene rows) now reaches drafting and the blueprint through the compressible `Scene Design Context (Snowflake)` section (`services/scene_design_context.py`, env `NOVEL_SYSTEM_SCENE_DESIGN_CONTEXT`; prompts `neutral_draft` v9, `style_first_draft` v4, `scene_blueprint` v7), and materialization no longer writes boilerplate as facts — `must_withhold`, `hook`, `expected_reader_emotion` and unplanned beats / crucible stay empty (the brief prints 未规划), `must_include_text` no longer falls back to the scene summary, `exit_change` still falls back to the author's setback / decision but never to a canned sentence; `_scene_card_resync_patch` keeps the same recipe. Tests: `tests/test_scene_design_context.py`. **阶段 G (让回溯便宜, same day)**: `snowflake_staleness.FIELDS_CONSUMED` now lists every direct-parent edge (01→02 minus `safety_rules`, 03→04/05, 04+05→06, 05→07, 04+06→08, 07→09, 09→10) and an upstream *absent* from a step's table means "not consumed, never stale" (an unknown step still degrades to edge-level); accepted-stale rows stay in the `_mark_downstream_stale` candidate set so 「已复核」 is re-evaluated on the next upstream change; a 09 re-approval marks only the scene plans whose content projection (`snowflake_staleness.scene_row_content`, keyed by `row_uid` or legacy `scene_id`) changed, and `_sync_scene_plans(approved=False)` resets only rows whose content changed (an unchanged row keeps its status and 复核 trace, so 「AI 补全这一场」 no longer un-approves the whole table); the minted `row_uid` / `scene_id` stamping into `draft_json` is now persisted via `flag_modified` (in-place mutation made SQLAlchemy skip the write). `project_runtime_invalidation` is **advisory** (`scope="advisory"`, nothing mutated) for 01/02/03/05 — re-approving a book-level step no longer stales every draft / QC / final and resets every run state — and scoped for `scene_list` (changed rows) and `long_synopsis` (scenes in changed chapter rows); 10 and the character steps were already scoped. The workspace step payload carries `revised_after_approval` (latest version is `pending_review` while an approved/stale version exists): the React sync layer no longer auto-approves such a step 700 ms after a keystroke or on hydrate — `SnowSync.needsReconfirm` / `approveStep` and the 「已改动 · 待重新确认」 pill / list marker hand the decision back to 「确认本步」, so the downstream stale cascade fires when the author confirms, not while typing. Tests: `tests/test_snowflake_cheap_backtracking.py`, the 阶段 G blocks of `ws-snow-sync.test.jsx`.
- **Scene Triage**: Before materialization, each scene plan is scored and assigned a triage status (`qualified`, `needs_fix`, `rewrite`). The `suggest` endpoint uses LLM to recommend triage decisions. **Rule layer (2026-09-13 阶段 B)**: `snowflake_steps.diagnose_scene_detail` only flags *missing* required beats and *placeholder* content (`placeholder_<field>` — empty, equal to an editor hint / placeholder / `SCENE_FIELD_EXAMPLES` repair example, or containing 待补/TODO/TBD/占位); **阶段 H (2026-09-13)** demoted everything else to `advice` lines (`建议：…`, also appended to `fix_steps`) that never change score or status: the generic-phrase table, a missing `cost_requirement` (a project addition, not one of the method's beats — Ingermanson's own scene sketches now `pass`), the keyword heuristics (escalating conflict, costly setback, two-cost dilemma, decision → next goal), and at step level the 但/却/cost marker checks and `_looks_generic` flags for 01/02/03/05/09 and the per-character `_pressure_too_soft` flag (which also read top-level keys the bible normalizer had moved into `psychological_profile`, so every bible character was permanently "too soft"; it now reads the nested profiles and only nags lead roles about empty sheets — minor characters may stay sparse). The rule-derived `rewrite` recommendation is a materialization *warning* (`triage_unreviewed_rewrite`), not a blocker; the execution contract's `cost_requirement` is always advisory. The five-slot synopsis steps keep positional empty slots and never truncate (`_coerce_positional_list`). Prompts `character_sheets` v5 / `character_synopses` v5 / `character_bibles` v4 / `scene_details` v10 say leave-empty-do-not-invent (only the protagonist and antagonist must be complete), and `UPSTREAM_STEPS_HOW_TO_USE` tells the model an unconfirmed draft's blanks are the author's, not gaps to fill — Ingermanson's own Goldilocks scene sketches are the golden fixtures (`tests/test_snowflake_method_contract.py`). Same batch: `_looks_generic` takes a per-field `min_chars` (no more blanket 28-char threshold); the logline contract is ≈40 Chinese characters everywhere (catalog, guidance, prompt v3, FE meter); `short_synopsis` is exactly five paragraphs and `one_paragraph_summary` exactly five sentences — extra items raise `StructuredCountMismatch` → one automatic regeneration with the rejection reason, then a 409 (`count_mismatch`), never silent truncation (coach patches drop the key instead); scene type defaults to `proactive` (no row-parity alternation) and the scene-list prompt v7 / guidance state Ingermanson's rule (after a setback: switch POV thread, next proactive scene, or a reactive scene only when the next goal is not obvious; reactive scenes are a minority); materialization stamps the 主角 from character sheets as `protagonist_hint` / `protagonist_character_id` on every scene brief (阶段 L: the 04 draft's explicit `protagonist_character_id` wins, the first 主角-role character is only the fallback) and prompt v8 / the structure brief say setbacks are measured against the protagonist, not the POV character. **Reactive rendering (2026-09-13 阶段 C)**: `SnowflakeScenePlan.rendering_mode` (`full` | `summary`, migration `20260913_0084`, `RENDERING_MODES` in `snowflake_steps.py`) lets step 10 mark a *reactive* scene as 「概述两段」; proactive scenes are always `full` (coerced in `_apply_scene_patch`, ignored by the scene_details sanitizer, omitted by the React `canonFromFE`). Materialization and resync stamp `writer_brief_json.rendering_mode` and give a summary scene the numeric band `SUMMARY_LENGTH_BAND = "200-500"` (so the existing numeric length machinery enforces it; switching back to `full` returns the card to `medium`); the structure brief renders a `Rendering mode: summary` line and `neutral_draft` v8 / `style_first_draft` v3 / `scene_blueprint` v6 / `snowflake_generate_scene_details` v9 tell the model to write it as two or three paragraphs of narrative summary. `snowflake_staleness.semantic_payload` strips a default `rendering_mode: full` from scene rows so pre-C drafts don't flip to pending on the first autosave; the chaptering rhythm report weights summary scenes 0.5 (`weighted_scene_counts`, `summary_scene_count`). **阶段 I (2026-09-14)** added Ingermanson's third option `skip` (`RENDERING_MODES = ("full", "summary", "skip")`, reactive only): the beat is still designed, materialization creates no `SceneCard` for it, `_scene_card_resync_patch` sends an already-materialized card to the trash (`trashed_flag=1`, brief `skipped_by_plan`) and takes it back when the mode changes again, the chaptering rhythm weighs it 0 (`skipped_scene_count`), and the following scene's `Scene Design Context` carries the skipped Reaction / Dilemma / Decision in full. **阶段 N (2026-09-15, third-round evaluation)**: `summary` is legal for **both** forms (Ingermanson's own scene 1 is a proactive narrative summary carrying both trios; his closing scenes are narrative wrap-ups) while `skip` stays reactive-only — the single coercion rule is `snowflake_steps.effective_rendering_mode` (workspace `_effective_rendering_mode` delegates, the LLM sanitizer, chaptering preview and the React `canonFromFE` follow it; the structure brief's `Rendering mode: summary` line is form-aware). The author's design-time triage verdict gained `cut` (待删, author-only — `_normalize_triage_output` never accepts it from the LLM): `EXCLUDED_TRIAGE_STATUSES = {rewrite, cut}` scenes are excluded from materialization like `skip` (no card; resync trashes an existing card with `excluded_by_triage`, restores it when the verdict changes), weigh 0 in the rhythm report, `cut` scenes are invisible to neighbours in the design context, and neither verdict blocks the rest of the book any more (the `SNOWFLAKE_TRIAGE_BLOCKED` 409 is gone; gate items `triage_rewrite` / `triage_cut` are warnings, `no_materializable_scene` is the only new blocker). `save_scene_triage` upserts one row per scene (`_latest_triage_row`) and every consumer reads the latest row per scene via the leaf module `services/snowflake_triage.py` (`latest_triage_rows` / `latest_triage_plan_ids` / `excluded_scene_plan_ids`; it must stay a leaf — importing `snowflake_chaptering` from `scene_design_context` closed a cycle through `projects` / `bundle_builder` that `tests/test_service_architecture.py` catches). The AI triage prompt (`snowflake_scene_triage_suggest` v3, 阶段 Q) judges by Ingermanson's own tests (a story on its own + a nameable crucible; the four No conditions; the seven-step rescue as `fix_steps`) and knows summary / skip / mixed victory / follow-up trios / advisory cost / `exception_reason`. Same phase: a scene may carry the *other* trio as follow-up beats (Goldilocks scenes 1 / 8 / 13) — the structure brief renders `Follow-up beats` + `Scene ends on`, `_beats_from_detail` appends them after the primary trio, and `neutral_draft` v10 / `style_first_draft` v5 / `hard_qc` v5 / `scene_blueprint` v8 / `snowflake_generate_scene_details` v11 know the scene then ends on the last follow-up beat; the setback field may describe a costly (mixed) victory when losing would end the story. Tests: `tests/test_snowflake_rendering_mode.py`, `tests/test_migration_0084_scene_plan_rendering_mode.py`. **阶段 O / P (2026-09-15)**: the scene-book standards reached the prompts and the rule layer — `snowflake_generate_scene_details` v13 carries the five goal tests (filmable, fits the scene's time slot, possible, hard-not-absurd, in character — "time-bound" and the countdown examples are gone), the four reaction and four decision tests, the dilemma-on-the-04-value-fault-line rule, "no upper limit on conflict rounds" (the FE hint / advice no longer prescribe 2–3 rounds), "the setback is the last attempt, told briefly", an advisory-only cost clause, `target_length_band` as "short/medium/long or a numeric range, never medium by default", and `exception_reason` as author-only; `snowflake_generate_scene_list` v10 adds "POV = the character with the most to lose" and "a fresh crucible every scene"; `snowflake_generate_one_paragraph_summary` v3 states what each disaster forces (commitment / moral-premise flip / drive to the ending) and asks for a deliberate happy / sad / bittersweet ending; `snowflake_generate_one_sentence_summary` v4 adds "do not reveal the ending"; the coach (`snowflake_workspace_assistant` v4) and `snowflake_step_candidates` v4 now say what `UPSTREAM_STEPS_HOW_TO_USE` says (an unconfirmed draft's blanks are the author's). Rule layer: `_repeated_crucible_advice` (adjacent identical crucibles → advice only), `_REFERENCE_STEP_INSTRUCTIONS` (which reach the LLM as `step_instruction`) fixed to 600–1000 chars for 07 and 主动/反应 for 09. Drafting: `neutral_draft` v12 / `style_first_draft` v7 carry the method's proportions (POV in the first paragraph; setback = last attempt, brief; reaction proportional, dilemma the bulk, decision short with no dancing around the plan; emotion shown in the body, never named; explain only this scene's crucible, never the story crucible) and honour the `Author's exception` line, as do `hard_qc` v6, `soft_qc` v8 (scene_shape gains dragged setback / named emotion / story-crucible exposition / hedged decision), `near_final_acceptance_review` v8 and `scene_blueprint` v10. **阶段 R (frontend)**: step 10 has author controls for `title` (empty = follow the 09 summary; `canonFromFE` no longer overwrites the model's short title on every save), `target_length_band` (短/中/长 or a custom numeric range; hidden for summary scenes), `must_include_text` (the method's "good dialogue snippets"), `exception_reason`, the render selector for both forms (skip only when reactive), and a verdict row (通过 / 需修补 / 该重写 / 待删 → `SnowSync.saveTriageVerdict` → `POST …/scene-triage`); `SnowSync.rowUidForSceneId` / `sceneIdForRow` map the workspace's scene ids, and the 成稿中心 story-check badge offers 「回第 10 步」 (`ws:snow-step` + `ws:snow-scene` intents, `window.__snowSceneTarget` for the mount race) and, on a No verdict, 「标待删」 (`WsCatalog.removeScenes`, i.e. the trash). **阶段 S**: the v1 planner's `_outline_scene_detail` no longer guesses a scene's form from its index parity (default proactive, the v2 rule); the canned `_outline_scene_list` fixture keeps one proactive + one reactive sample scene per chapter because the resync / catalog test lanes need a reactive sample — its boilerplate briefs remain a batch-2 subtraction candidate. Tests: `tests/test_snowflake_round3.py`, the 阶段 R blocks of `ws-snow.test.jsx` / `ws-snow-sync.test.jsx`. **Method fidelity (2026-09-13 阶段 D)**: step 04 character sheets carry Ingermanson's two storyline columns (`one_sentence_summary` / `one_paragraph_summary` — the React form fields 「一句话故事线」/「一段话故事线」 now round-trip them, and `canonFromFE` omits the keys when an older local cache lacks them so a server-generated storyline is never cleared) and `values` are one statement per element phrased 「没有什么比___更重要」 (the form stores the middle text one per line and wraps/strips the phrase on the way up/down; prompt `snowflake_generate_character_sheets` v4 asks for 2–3 in tension). Step 06 `synopsis` has a sixth prefixed line 「视角故事：」 (the whole book retold from that character's POV — the book's step 5; prompt `snowflake_generate_character_synopses` v4; unprefixed prose hydrates into that field, no longer into 「信念」). **阶段 K (2026-09-14)**: chapters come *after* scenes — `snowflake_generate_long_synopsis` v6 no longer hard-codes a chapter count and returns `chapters: []` unless the draft already has a table (the 07 `chapters` editor field is `optional`, so an empty table is not a missing field); `SnowflakeChapteringService.propose_from_scenes` (`POST …/chapter-plan/propose`, body `target_chapter_count` / `scenes_per_chapter` / `replace`) splits the ordered scene list into chapters — each 灾 scene closes its chapter (hinges outrank the target count), chapter count from the project setting or ≈3 scenes per chapter, ≥1 chapter per act — assigns the scenes, mirrors the rows into the latest 07 draft, and the 章节编排 panel offers 「按场景生成 / 重排章表」 (the preview's `SNOWFLAKE_CHAPTER_PLAN_EMPTY` is no longer a dead end); the five expansions are now about a page each (600–1000 chars, `chapter_planning_context` canon budget 5200). Step 07 `long_synopsis.paragraphs` is again the book's step 6 — exactly five page-length expansions of the five short-synopsis paragraphs (`LONG_SYNOPSIS_PARAGRAPHS`, same `StructuredCountMismatch` retry/409 contract as step 05; normalization pads to five but never truncates) while `chapters` stays the chaptering truth; `canonFromFE("outline")` no longer overwrites `paragraphs` with per-act chapter lines, `feFromCanon` treats a legacy chapter-line mirror as empty slots, and `parse_outline_chapters` / the FE fallback only recognise real `NN 章名：` lines (prose never fabricates chapters); prompts `snowflake_generate_long_synopsis` v5 and `snowflake_generate_scene_list` v8 (scenes are traced to the five expansions, chapters give membership only); `snowflake_staleness.FIELDS_CONSUMED` now lists `chapters` next to `paragraphs` as a scene-list input (the mirror used to carry chapter edits implicitly), and the chapter-planning canon budget for `long_synopsis` is 3200 chars. One-time effect on pre-D projects: the first 07 save after upgrading replaces the chapter-line mirror with empty expansions, so step 09 flips to `stale` once and the author accepts it. Post-draft triage: `near_final_acceptance_review` v6 returns a required `scene_story_check {crucible_identified, shape_landed, verdict yes|no|maybe, note}` (Ingermanson's Yes/No/Maybe); `near_final._normalize_scene_story_check` normalizes it, it rides on the review `AttemptTracker.details_json`, is surfaced by the workbench near-final summary and per scene by the catalog (`story_check`, `CatalogService.story_checks`, one batched query per chapter) and rendered as a non-blocking badge in the 成稿中心 结构 tab — it never changes `pass_flag`. Tests: the 阶段 D section of `tests/test_snowflake_method_contract.py`, `test_scene_story_check_is_normalized_recorded_and_surfaced` in `tests/test_near_final_engine.py`, and the 阶段 D block of `frontend-react/src/ws-snow-sync.test.jsx`. **Backtracking UX (2026-09-13 阶段 E)**: the workspace step `artifact` now carries `input_refs` (the upstream `step_run_id`s consumed when the version was written); `SnowSync.upstreamChanges(workId, feKey)` compares them with each upstream's current `stepRunId` and fetches `history?include_draft=true` to show a consumed-version vs current-version diff (`S2UpstreamDiff`), the stale banner offers 「按新上游重新展开」 (`structuredGenerate({source: "fe_restale_regen"})`, the step returns to 进行中 for re-confirmation) and 「已复核」 calls `accept-stale` first when the backend marked the step stale (`SnowSync.acceptStale`; a failed call leaves the local map untouched). Backend `status == stale` (not yet accepted) is the **only** 需复核 signal (E3 step 2, same day): the local `revs` / `confirmRevs` map, its content-signature bump effect and the `book_brief.fe_meta.revs` write-through were removed — `s2StaleMap(health)` lists backend-stale steps and `s2UpstreamDrift(health, key)` names the upstream steps whose current `step_run_id` differs from the step's `input_refs` (a direction hint and the diff input, never a second staleness algorithm); approve and accept-stale responses carry the whole workspace and `captureWorkspaceHealth` refreshes every step's health so downstream stale shows without a re-hydrate; `accept_stale_step` re-snapshots `input_refs_json` / `consumed_input_sigs_json` to the current upstream versions (「仍然有效」 is said about the upstream as it is now). Hydration maps backend `stale` to FE state `done` (the flag lives in `health.beStatus` / `staleReason`). The 验收门 is explicitly advisory (copy says 验收未过也可确认本步，后端闸门仍会守住依赖; no lock icons), the coach textarea owns ⌘↵ (`stopPropagation`, window handler skips `.sf-coach-input`), and the step-10 coverage cells count slots by the 09 scene type (`s2PlanState(plan, type)`). Docs: `docs/snowflake-method-contract.md` is the step-by-step contract against Ingermanson (indexed from `docs/README.md`, README and the operator manual; the two dangling links to deleted docs were removed and `tests/test_docs_links.py` guards relative links). Tests: `tests/test_snowflake_phase_e.py`, the 阶段 E blocks of `ws-snow-sync.test.jsx` / `ws-snow.test.jsx`. **阶段 M (2026-09-14, frontend only)**: the 09 board is Ingermanson's movable spreadsheet — `snow-scene-insert-{i}` inserts a fresh row after row *i* (inheriting line / POV / place), rows are HTML5 drag-and-drop reorderable through the pure `s2ReorderScenes(list, from, to)`, rows are keyed by scene id, and a read-only chapter header row (`snow-scene-chapter-{i}`) is rendered before the first scene of each chapter from the hydrated `chapter_title` (hidden when it is only the `chapter_id` fallback; `chapter` never goes up with the 09 PATCH — chapters are edited in the 章节编排 panel). Step 10 gained `hook` / `exit_change` inputs (`snow-plan-hook` / `snow-plan-exit-change`; the columns always existed on the scene card). `captureTriage` stores the workspace's `triage_items` per work keyed by the 09 `row_uid` (mapped through the `scene_list` draft's `scene_id`; author `effective_status` outranks `recommended_status`) and `SnowSync.triageItems(workId)` seeds the step-10 triage state on hydrate, so a saved triage survives a reload. 「略过此步」 now calls `SnowSync.skipStep(workId, feKey, reason)` → `POST …/steps/{beKey}/generate {skip: true, skip_reason}` (the view refuses essential steps 01/02/03/09/10 up front and requires a reason; a server rejection leaves the local state untouched) — it used to be a local-only marker the backend never saw, so the materialization gate stalled silently. Tests: the 阶段 M blocks of `ws-snow-sync.test.jsx` / `ws-snow.test.jsx`.
- **Vector Backend Split**: Windows tests always use `memory` backend; real ChromaDB only runs in Linux/WSL and is gated by the `chroma_integration` pytest marker. `conftest.py` applies this automatically.
- **Style Reference (style imitation)**: imitate the reference author's hand at every level (see the Style Reference subsystem above and `docs/style-reference.md`) but never copy source sentences or the book's proper names — `services/reference_copy_gate.py` is the single gate.
- **Dual-stack Pagination**: API responses support both `page`/`page_size` (offset) and `cursor`/`limit` (cursor-based) patterns via `services/pagination.py`.
- **Test Isolation**: `conftest.py` auto-creates an isolated SQLite DB per test in `tmp_path`, resets the engine, and auto-skips `chroma_integration`-marked tests on Windows. No shared test state between tests.
- **Snowflake coach → author direction brief (阶段 T, 2026-09-16)**: the three snowflake LLM entry points used to be isolated — the coach (`POST …/snowflake-workspace/assistant`, template `snowflake_workspace_assistant`) was a single-shot call that did not even see its own previous turns, and neither `…/fe-candidates` nor `…/generate` saw any of the conversation; the only bridge was the coach's `candidate_patch`, which carries content, not intent. Now every coach turn receives `conversation` (`services/snowflake_direction_brief.py::DirectionBriefStore.conversation_payload`: the step's current brief lines with `line_id` / `origin`, the lines the author withdrew, inherited book-level lines, the last 6 LLM turns of the step) and must return `brief_update.lines` (v5 schema, `required`) — its complete restatement of the coach-owned intent lines (kinds `decision` / `rejection` / `constraint` / `pending`, scope `step` / `book`; an exploratory "what if" is `pending`, never `decision`). The server diffs that against the persisted `SnowflakeDirectionBrief` row (`apply_coach_restatement`: same `line_id` or same text key = kept / updated, omitted coach line = superseded, new line = added; `origin=author` lines are never touched by the coach and author-dismissed lines are never revived; ≤16 active lines, ≤160 chars each, `pending` coach lines are dropped first over the cap) and returns `direction_brief` + `brief_delta`. `PUT …/steps/{step_key}/direction-brief` is the author's edit (the request's `lines` is the wanted list: an absent active line is dismissed, an edited line becomes `origin=author`, `status=active` on a dismissed id restores it, `inherit_upstream` toggles inheritance). `generate_step`, `fe_step_candidates` and `suggest_scene_triage` read `DirectionBriefStore.prompt_payload_for` (this step's active lines + upstream steps' `scope=book` lines when `inherit_upstream`, ≤24) into the **protected** prompt key `author_direction_brief` (`snowflake_prompt_budget.AUTHOR_DIRECTION_BRIEF_KEY`, never shed; the hard caps are what make protecting it safe) with a Chinese `how_to_use` (decisions/constraints bind, rejections are forbidden, pending is never settled for the author; it outranks the pressure rubric and the model's taste but never a `confirmed: true` upstream fact); generation requests default to `use_direction_brief: true` (`false` = pure exploration) and record what they consumed in `health_json.direction_brief` (`used`, `revision`, `sha`, `line_ids`, `inherited_line_ids`) so the React card can say 「本稿未采用最新要点」 and offer `fe_brief_regen`. `direction_text` gained `direction_kind` (`candidate` | `coach_reply`): 「以此为方向生成」 on a coach reply changes the `adopted_direction.how_to_use` from "blueprint prose" to "follow this reply's judgement". The coach is now **fail-closed** like every other node (`SnowflakeWorkspaceAssistantService` and its canned 「规则」 reply were deleted; LLM off → 409 `SNOWFLAKE_LLM_NOT_CONFIGURED` and no turn is written; historical `source=fallback` turns are excluded from `recent_turns`). React (`ws-snow.jsx` `S2BriefCard` in the 教练 tab, `ws-snow-sync.jsx` `directionBrief` / `saveDirectionBrief` (optimistic + rollback) / `setDirectionBrief` / `captureBriefs` / `briefUsage`): the card lists active lines (click the kind to cycle it, click the text to edit, 本步/全书 toggle, ×, 恢复 for dismissed lines, 加入 for author lines), 「继承上游」 and the per-work localStorage preference 「生成时带入」 (`ws_snow_use_brief::<work>`), plus the stale hint. Prompt sync: `snowflake_workspace_assistant` v5 and `snowflake_step_candidates` v5 need `sync_prompt_templates --execute` on a live install. Tests: `tests/test_snowflake_direction_brief.py`, `tests/test_migration_0087_direction_briefs.py`, `frontend-react/src/ws-snow-coach.test.jsx` (was `ws-snow-brief.test.jsx` until 阶段 U), the 阶段 T block of `ws-snow-sync.test.jsx`.
- **Coach · brief · directions · generate (阶段 U, 2026-09-17; contract doc `docs/snowflake-coach-and-directions.md`)**: the user found the 候选 ↔ 教练 interaction opaque (five differently worded "let the AI change this step" buttons: 采纳并结构化 / 仅作草稿 / 只更新「X」 / 应用补丁 / 以此为方向生成, plus a 生成时带入 toggle buried in the coach tab). The author-facing model is now four nouns and one verb — **要点** (persistent intent, the brief), **方向** (a one-shot blueprint for one generation: a card from 「先看 3 个方向」 or a coach reply), **改写** (the coach's `candidate_patch`, 「填入本步」), **生成** (「AI 生成本步」 / 「按此生成本步」 / 「按最新要点重新生成」 / 「按新上游重新生成」 / per-member 补全). The 候选 tab is gone: `fe_step_candidates` is **fail-closed** (409 like the coach, no more `source=fallback` + empty list), takes `ask` / `draft_override` / `focus_scene_id` (the FE no longer folds a `context` string; legacy `context` / `draft` still accepted → `fe_local_context` / `current_draft_text`), and persists the result as a `turn_kind=candidates` row of `snowflake_assistant_turns` (`candidates_json={items,target_chars}`; empty output → 502 `SNOWFLAKE_CANDIDATES_EMPTY`, no row), returned with `turn` + `assistant_history`; the coach's `conversation.recent_turns` now carries `{kind: candidates, directions[], chosen}` and `{kind: chat, …, adopted_as_direction}` (`CONVERSATION_HOW_TO_USE` says a chosen direction counts as accepted). `generate` gained `direction_turn_id` / `direction_index`: the server derives `direction_kind` from the turn kind, writes `adoption_json={step_run_id, candidate_index, adopted_at}` on the turn and `health_json.direction={kind, turn_id, candidate_index, label, sha}` on the version (404 `SNOWFLAKE_DIRECTION_TURN_NOT_FOUND` / 400 `SNOWFLAKE_DIRECTION_INDEX_INVALID` / 400 `SNOWFLAKE_DIRECTION_TEXT_REQUIRED`); each coach turn also stores its `brief_delta_json`. React: `S2AiBar` on the edit tab of every step (AI 生成本步 except 09/10 whose scaffolds own 整表生成 / 补全所有场景, 先看 3 个方向, 要点 N 条 → coach tab, provenance line from `health.generationSource` / `health.direction` / `health.directionBrief` + the stale-brief regen), `S2DirectionCards` inside the coach log (按此生成本步 / 只更新「X」 / 「就用这一句」 on the free-text 02 step, 「已按此生成」 badge from `turn.adoption`), `S2Coach` with 「给 3 个方向」 next to 发送 (the textarea text becomes `ask`), per-turn 「要点 +2 · 改 1」 from `turn.brief_delta`, `S2BriefRail` read-only mirror in the right rail; the in-flight indicator is scoped to the clicked entry (`genTargetMap` per step → `busyTarget` `{kind, turnId, index, focused, id}`; only that card / reply / bar button / scaffold tool shows 「生成中…」, the rest are merely disabled — with one shared `structBusy` all three direction cards used to spin at once and read as "all triggered"); removed `S2Cands`, the FE prompt helpers (`s2GenPrompt` / `s2ParseCands` / `s2UpstreamContext`), the 生成时带入 pref (`ws_snow_use_brief::*`, the request simply omits `use_direction_brief`), 仅作草稿 (no new free-draft overrides; `S2DraftOverride` stays for legacy text), the candidates keyboard shortcuts, the hard-coded 「参考画像 · 冷峻短句」 fake blocks in `S2Links` / `S2Ref`, and the dead `scripts/smoke-g5.mjs`. Prompts `snowflake_workspace_assistant` v6 / `snowflake_step_candidates` v6 (`sync_prompt_templates --execute` on a live install). Tests: `tests/test_snowflake_coach_directions.py`, `tests/test_migration_0088_assistant_turn_kinds.py`, `frontend-react/src/ws-snow-coach.test.jsx`.
- **Snowflake sync hydrate gate + wipe guard (2026-09-18)**: a fresh browser profile (or cleared localStorage) used to be able to blank a whole project. The view persists its default blank state 450 ms after mount (`_t` = now) and queues a push 700 ms later; when the workspace hydrate GET **failed** (no `lastPushed` seeding, no server canon mirror) or was merely **slower than ~1.1 s** (the blank cache's newer `_t` won the whole-cache 「本地为准」 comparison), `snowPushKey` sent ten blank `force: true` PATCHes — and `update_step` rewrites a `pending_review` step **in place**, so an unconfirmed draft had no history to come back from (found when a Playwright session with cleared storage hit a dev backend whose workspace GET was 500ing; only the same 500s stopped the PATCH wave). `ws-snow-sync.jsx` now has three guards: (1) **push gate** — `ensureHydrated(workId)` inside `snowPushKey`: nothing is pushed until a workspace GET has succeeded in this session (`snowHydrateOk`, exposed as `SnowSync.hydrated()`; hydrates for one work are chained through `snowHydrateInflight`, `snowHydrate` now returns a never-rejecting `Promise<boolean>`); a still-unreadable server leaves the sync state at `phase: "error"`, `error.scope: "hydrate"`, 「读不到服务器上的构思版本，已暂停上行…」 and 重试 goes through the same gate; (2) **pristine rescue** — when local wins the `_t` comparison, every local step that `stepIsPristine` (no free draft, state todo / active, every non-empty scaffold leaf also present in `s2NormalizeState({})`'s blank scaffold, e.g. `sel=c1` / `role=主角`) while the server step is not is replaced by the server step (after a synchronous `ws:snow-flush-local`, then `ws:snow-hydrated`); (3) **never push a pristine step that has no `lastPushed` record** (also covers server content the hydrate loop did not recognise as content; a brand-new project no longer gets ten empty PATCHes on first open) — a step the author cleared *after* a sync still has its record and is pushed as before, so 重置 keeps working. Backend last line of defence in `update_step`: `_would_wipe_story` (story text via `semantic_payload`, ignoring `_NON_STORY_KEYS` such as ids / `role` / `primary_form` / `spine`; ≥ 8 chars → 0) makes a total wipe of a `pending_review` draft create a **new version** instead of mutating in place (+ `OperationLog` `snowflake_step_wipe_preserved`), so the previous draft stays in `history` and `POST …/steps/{key}/restore` brings it back; ordinary edits still rewrite in place. Same change: `snowflake_step_candidates` `max_output_tokens` 1800 → 4096 in both `llm_node_registry` and `models.yaml` (three 400-char directions + reasoning tokens never fit; the client's truncation ladder had to double-and-retry every time) — an install with a saved models snapshot needs `raise_llm_output_budget --node snowflake_step_candidates --floor 4096 --execute`. Tests: the 「水合闸门与空白步保护」 block of `ws-snow-sync.test.jsx` (verified falsifiable: without the guards four of them fail with "called 10 times"), `tests/test_snowflake_wipe_guard.py`, the budget pin in `tests/test_snowflake_fe_candidates.py`.
- **Chaptering = contiguous slices of the story order (阶段 V, 2026-09-18)**: the author reported 「整理成章节结构」 as 乱七八糟; the real project showed five stacked defects. (1) **Story order has one source**: the row order of the latest non-superseded 09 draft (`services/snowflake_scene_order.py`, a leaf: `story_positions` / `sort_in_story_order` / `renumber_scene_seq`). `SnowflakeScenePlan.scene_seq` used to have three writers and two meanings (FE 09 PATCH sent the global `i + 1`, the step-10 sync counted per `chapter_id`, chaptering `save` wrote the in-chapter position) while the chaptering service read `(scene_seq, scene_id)` as a global order — after the first saved chaptering a re-propose interleaved the chapters' scenes (1, 10, 2, 11, …). Now `scene_seq` **only** means "position inside its chapter, in story order", `renumber_scene_seq` is its only writer (end of `_sync_scene_plans` — 09 passes its own row order, a partial step-10 draft never defines order — and end of chaptering `save`), and `SnowflakeWorkspaceService._scene_plans`, `SnowflakeChapteringService.scene_plans`, `_build_chaptered_outline_plan` (members keep input order) and `scene_design_context._ordered_plans` all use the story order; the workspace's 09 / 10 step `draft` is built from those rows, so hydration no longer re-shuffles the scene list by chapter. (2) **Disaster marks**: `scene_spine` falls back to `spine_from_role` (灾一 / 灾难一 / 灾难 1 / 第一个灾难 / Disaster 1 — the prompt's own example 「灾难一·一幕高潮」 never matched the old `灾[一二三]` regex), `spine_positions` prefers explicit marks and takes the **last** occurrence, `propose_chapter_chunks` gives the act containing a mid-act 灾二 a minimum quota of 2, and `enrich_structured_schema` adds `spine` to the 09 wire schema (it is deliberately absent from the editor template, so a schema-enforcing relay could never emit it). (3) **Panel contract**: `preview` strategies are `spine_anchor` / `even` / `keep_current` / `from_scenes` plus `auto` (any scene assigned → `keep_current`; an authored table → `spine_anchor`; none or placeholder-only — `is_placeholder_chapter`: 「（待补）」 rows from the 07 editor — → `from_scenes`). `from_scenes` is a **read-only** preview with transient chapters `new:N`; the response carries `scale` (`chapter_scale`: request target → request `scenes_per_chapter` → project target → reference chapter length → 3, plus `hinge_min_chapters`), `chapter_table {count, authored, saved}`, per-scene `story_index` / `function` / `summary`, and the advisories `chapter_order_conflict` / `catalog_hand_made_chapters` / `catalog_leftover_chapters`. `save(replace_chapters=true)` treats `chapters` as the whole table (known `row_uid` → update, `new:*` → create, unlisted → soft-delete), refreshes system-made fields (`_refresh_auto_chapter_fields`: a blank or 「第 N 章」 title follows the chapter's position, a blank summary or one copied verbatim from a scene becomes the chapter's current last scene — authored text is never touched), re-stamps `chapter_id` / title / goal on **every** bound scene (no fallback to the row's stale values), renumbers, and mirrors the table into the 07 draft's `chapters` **and** `fe_scaffold.chapters`; the preview returns the raw `chapter_goal` (no summary fallback — the panel echoes it back); `propose_from_scenes` is the same algorithm persisted directly (API only), chapter summary = the chapter's **last** scene, `suggest` output is forced contiguous (`_enforce_contiguity`), `_derive_from_catalog` only counts catalog chapters that hold cards of live scene plans. React `ws-snow-chapters.jsx`: opens with `auto`, 「每章约 N 场」 + explanation (`scaleExplanation`), boundary-only moves (`moveSceneToChapter`: last scene → **first** of the next chapter, first scene → end of the previous), `splitChapterAt`, `mergeChapterIntoPrevious`, `homeChapterFor`, markers follow scenes (`respine`), payload always `replace_chapters`; `SnowSync.adoptServerChapters` (after materialize / propose / saveChapterPlan) writes the server chapter table into the local 07 scaffold + 09 row chapter labels, realigns `snowCanon` / `lastPushed`, and fires `ws:snow-hydrated` — hydration cannot do this because the local cache is always newer. (4) **Chaptering is not a story edit**: `semantic_payload` strips `_SCENE_ROW_PACKAGING_KEYS` from scene rows (`scene_plan_id`, `chapter_plan_id`, `chapter_id`, `chapter_title`, `chapter_goal`, `scene_seq`, `status`, `stale_*`, `diagnosis`); FE `mergeCanon` echoes those server-derived keys back, which used to mint a new `pending_review` version of 09 / 10 after every chaptering or approval and then blocked the materialization gate. (5) **Catalog placement** (`projects._CatalogPlacement`, used by `approve_outline_plan`): new chapters take `display_order` after chapters outside the plan (a hand-made 「第 1 章」 used to raise the `(project_id, display_order)` unique index → 500 on 确认写入), scene cards are parked on high seqs and placed in two phases (re-materializing after re-chaptering used to hit `(chapter_id, scene_seq)`), cards outside the plan keep following their previous planned neighbour. Resync no longer patches `scene_seq`: `_scene_card_order_drift` reports `scene_order` (planned cards compared with each other only, so hand-made / skipped cards cause no false 待同步) and `_park_scene_cards` + `_settle_scene_card_order` re-place the touched chapters (the park base clears trashed cards' seqs too, because the same resync may un-trash one; a card the resync trashes gets its pre-park seq back so a manual restore lands where it was). Tests: `tests/test_snowflake_chaptering_story_order.py`, `frontend-react/src/ws-snow-chapters.test.jsx`, the 分章面板确认后 block of `ws-snow-sync.test.jsx`. Follow-ups landed the next day as 阶段 W (below).
- **Chaptering follow-ups (阶段 W, 2026-09-19)**: (1) **AI chapter naming** — `POST …/chapter-plan/titles` (`SnowflakeChapteringService.suggest_titles`, read-only, fail-closed like every advisory node: 409 `SNOWFLAKE_LLM_NOT_CONFIGURED`, 502 `SNOWFLAKE_CHAPTER_TITLES_EMPTY` when nothing usable came back). The body carries the panel's **current** table `[{row_uid, title, act, spine, scene_plan_ids}]` (unsaved `new:*` chapters included; absent = the saved chaptering); only system-made titles are named (`is_auto_chapter_title`: blank / 「第 N 章」 / 「（待补）」; `rename_all` overrides), authored titles go to the prompt as `named_chapters` so the manner stays consistent, work is batched (`TITLE_BATCH_SIZE` 12 × `TITLE_MAX_BATCHES` 6, later batches see earlier names, a partial result carries a `CHAPTER_TITLES_PARTIAL` notice), and `_normalize_chapter_titles_output` + `clean_chapter_title` drop unknown `row_uid`s, duplicates, numbered prefixes, wrapping marks, bare structural labels (高潮 / 结局 …) and anything over 24 chars. New template `snowflake_chapter_titles_suggest` (properties written in the yaml — no step template to derive them from) is dispatched under the **existing `snowflake_chapter_plan` node route**, so an install with a saved models snapshot needs no 「一键补齐」; an install with a saved *prompts* snapshot still needs `sync_prompt_templates --execute`. React: `SnowSync.chapterTitles`, panel button 「AI 起章名」 (`chapter-plan-name`), pure `applyChapterNames` / `buildChapterTitlesRequest` / `isAutoChapterTitle`, and an editable per-chapter summary line; an AI summary only replaces a summary that was copied from a scene. (2) **Emptied chapters** — chapter ids are positional, so shrinking 6 → 4 used to leave `CH05` / `CH06` as empty shells. `projects.trash_emptied_snowflake_chapters` (called at the end of `approve_outline_plan` and after a resync that moved cards) trashes a catalog chapter only when it is snowflake-made (`_CHnn` id + `writer_brief_json.source` ∈ snowflake), outside the current chaptering, holds **no** scene card at all (active or trashed) and is not final-approved; `trashed_by = AUTO_TRASHED_EMPTY_CHAPTER`, event `snowflake_empty_chapter_trashed`, result key `trashed_empty_chapters` (the confirm toast reports it; the panel advisories are `catalog_leftover_chapters` = will be trashed / `catalog_leftover_chapters_kept` = still holds hand-made or trashed cards). When a later plan targets a **trashed** chapter id, `approve_outline_plan` revives it (treated as new: title / display_order re-seeded through `_CatalogPlacement`, planned cards that were trashed *together with* the chapter come back, `restored_chapter_ids`) instead of moving cards into an invisible chapter. (3) **Catalog names follow the confirmed table** — on re-materialization `narrative_json.title` / `act` / `spine` are updated whenever the catalog still holds the value the previous materialization seeded (kept in `writer_brief_json.chapter_title` / `chapter_act` / `chapter_spine`); a name the author changed in 章节编排 is never touched. `_preview_from_scenes` reuses a live chapter whose scene set is identical (same `row_uid`, authored / AI title, summary, goal; `replaces_chapter_count` counts only the rest). (4) **07 `chapters` is no longer a 09 input** — `FIELDS_CONSUMED["scene_list"] = {"long_synopsis": {"paragraphs"}}`: chapters are derived from scenes and mirrored back into 07, so listing them upstream of 09 was a cycle (renaming a chapter and re-confirming 07 marked 09 需复核). Runtime invalidation by changed chapter rows (`project_runtime_invalidation`) is unchanged. Tests: `tests/test_snowflake_chaptering_followups.py`, the 阶段 W blocks of `ws-snow-chapters.test.jsx` / `ws-snow-sync.test.jsx`. Verified end to end on a DB copy with a title-echoing fake provider (real adapter, `json_schema` wire mode). Still open: naming does not use the style reference (the structure card has no title samples). Positional chapter ids were replaced by pinned ids in 阶段 Y (below).
- **One book spine: snowflake → catalog → the three desks (阶段 X, 2026-09-19; contract doc `docs/book-spine-catalog-contract.md`)**: the author reported 「雪花生成的章节感觉是孤立的，没有同步到 AI 起草台和写作台」; every cause below was reproduced on a copy of the real project. (1) **Acts**: materialization wrote `narrative_json.act` as ints 1/2/3 while 章节编排 groups by `act === "act1"` — snowflake chapters were *invisible* on the board; `_build_chaptered_outline_plan` now writes `act1/2/3`, `catalog.normalize_act` normalizes legacy rows on read and `catNormalizeAct` guards the FE (an unknown value falls into act 1, never off the board). (2) **Pristine placeholder chapter**: a hand-made, never-touched 「第 N 章 / 开场」 (system title, no beats, zero words, untouched rev-1 draft, no run / final / LLM rows) used to sit in front of the snowflake chapters (numbering off by one, duplicate names, the writer landing on it); leaf module `services/catalog_placeholders.py` trashes it **before** `_CatalogPlacement` (`trashed_by = snowflake_placeholder`, event `snowflake_placeholder_chapter_trashed`, result key `trashed_placeholder_chapters`, panel advisory `catalog_placeholder_chapters`); anything the author touched stays. `approve_outline_plan` no longer resets `current_chapter_id` on re-materialization (only when the bookmark is empty or points at a chapter that left the catalog). (3) **Scene identity**: catalog scene `slug` **= `scene_id`** (it was positional `ch08s3`, and every per-scene FE store — `wr-doc:` draft binding / cache, `scn-run:`, queue lists, `wr-notes:` — keyed on it, so any structural move re-pointed text at another scene); `legacy_slug` stays for a one-time localStorage key migration (`ws_sid_migrated_v1::<work>`) and as a fallback in `WsCatalog.sceneById` (direct → session alias for optimistic `tmp_…` sids via `catTrackAliases` → `legacySid`); chapter slugs stay positional (display only); `resume.scene_no` replaces parsing the slug. (4) **The catalog payload is the only hand-off surface**: chapter `origin / summary / goal / spine`; scene `summary`, short `title` (`scene_display_title` / `short_scene_title`; `scene_title()` is unchanged for planning contexts), `design {…}` (crucible, location, story_time, cast, reader_emotion, must_include / withhold, cost, length_band, rendering_mode, `followup` trio, exception_reason, `owner` since 阶段 Y) and `work {run_status, has_final, has_words}`, batch-loaded by `CatalogService.read_context`. One shared component `ws-scene-design.jsx` (`sceneDesignModel` + `SceneDesignCard`, beats labelled by form, system placeholders count as unfilled) renders it in the writer header (compact), the writer's 上下文 · 戏剧 drawer and the drafting desk's preflight; snowflake scenes link back with `planIntentsForScene` (`ws:snow-step` + `ws:snow-scene`). (5) **One focus rule** — `WsCatalog.focusScene()` (= `writingScene()`; backend mirror `catalog.focus_scene_payload`, used by `project_overview`): current chapter's `writing` scene → first not-`done` → last. Home, 「进入写作房间」, the writer, the drafting desk and the delivery strip all use it (home used the first scene, the overview the *last*, the writer *any* `writing` scene in the book). (6) **AI 起草台 left column = the book spine** (`SceneSpine` in `ws-scene.jsx`): work items are pinned (在办, persisted) or `transient` (just viewing — never persisted, dropped when another scene is opened); 整章入列, `ws:scene-enqueue`, 开始起草 and backend run-state restore pin; pinned rows keep the old queue test ids / remove / multi-select contract, other rows are `scene-spine-item`; `ScenePicker` / 「加入场景」 and the fake preflight items are gone. (7) **Confirm = sync**: `POST …/steps/{key}/approve {"sync_catalog": true}` (the React client always sends it; API callers / tests without it keep the explicit-resync behaviour) resyncs materialized cards of `scene_list` / `scene_details` in the same transaction (`_auto_sync_catalog`, `actor_ref = auto_sync:…`, result `catalog_sync {synced_count, held[]…}`); held for the explicit diff preview: `plan_not_confirmed`, `would_trash_written_scene` (阶段 X also held `desk_edited` cards; 阶段 Y removed that marker — see below); a card whose target chapter is not in the catalog yet syncs its content without the move (`notice` `CHAPTER_MOVE_NEEDS_MATERIALIZE`). `ProjectRuntimeInvalidationService` now flips only scenes that actually have runtime artifacts (contract / draft / QC / final, or a run state that left `ready`) to `needs_replan` (`impact.invalidated_scene_ids`); a never-run scene stays `ready` and the project is not set to `chapter_blocked` — with confirm = sync a design edit would otherwise fake a failed run (`author_state = generation_failed`, a 待办 card for a draft that does not exist, a pinned row on the drafting desk). Desks read the light `GET …/snowflake-workspace/resync-status` through `ws-design-sync.jsx` (approved plans only; a non-snowflake work answers `supported: false`, never a 409 — the desks ask for every work and a 4xx is a browser console error that fails the E2E smokes; event-driven refreshes throttled to 20 s because `ws:catalog-changed` fires on word-count write-back). Scene titles follow the plan unless renamed at a desk (`title` + `seeded_title`; only cards carrying `seeded_title` compare titles, so no mass 待同步 at upgrade); `_scene_writer_brief` now also writes the follow-up trio (it used to arrive only with the first resync). (8) **Chapters are contiguous slices — enforced server-side**: `heal_assignment` (longest non-decreasing subsequence of chapter order; outliers join their predecessor's chapter, so one dragged scene moves one scene), `preview(auto)` answers a non-contiguous saved chaptering with `from_scenes` + `chapter_order_healed`, `keep_current` shows the healed version, `save` heals before persisting (`healed_scene_plan_ids` in the operation log). (9) Blank scene drafts are blank (`_blank_source_for_target` no longer copies the card into the prose as a 「【章节目标】…」 scaffold); the writer's blank-page placeholder is not counted as words; `WrNextCue` names the real next scene (it hard-coded a retired demo title); the writer outline shows localized chapter states (`planned` was printed raw). React: post-materialize delivery strip (`snow-delivered`, 去写作台 / 去 AI 起草台) and the `ws:snow-catalog-synced` receipt. Tests: `tests/test_catalog_book_spine.py`, `ws-book-spine.test.jsx`, `ws-scene-spine.test.jsx`, `ws-writer-spine.test.jsx`, the 阶段 X case in `ws-snow-sync.test.jsx`. Both items left open here (desk edits not written back, positional chapter ids) were settled the next day as 阶段 Y (below).
- **A chapter is a chapter, and a scene's design has one editor (阶段 Y, 2026-09-20; contract doc `docs/book-spine-catalog-contract.md` §9 / §10)**: the two items 阶段 X left open, evaluated on the user's request (「评估一下要不要做，做的话设计的好一点」). (1) **Stable chapter ids — done.** The materialization target used to be `{project}_CH{chapter_seq:02d}`: any split / merge / different scenes-per-chapter shifted every later id, so chapter names, the bookmark, chapter state and approvals stuck to a *position*, scene cards moved across chapters in bulk, and the denormalized `chapter_id` on their runtime rows went stale (drafting context could not find a scene's own finals; purging an auto-trashed emptied chapter from the trash hit the FK → 500). Now `SnowflakeChapterPlan.catalog_chapter_id` (migration `20260920_0089`) is minted once and pinned: ids are **serial numbers** (`SnowflakeChapteringService.catalog_chapter_id(chapter, mint=)` is the only mint; first materialization still yields CH01…CHnn; next = max serial over catalog chapters *including trashed* and every pinned id *including soft-deleted plans*, +1 — never reused; scene-row stamps do not count because the FE default stamps every scene `…_CH01`; `_is_pinnable_chapter_id` keeps arbitrary LLM stamps from becoming targets), `save` mints in table order, the preview shows `chapter_id: ""` for unsaved `new:*` chapters. **Identity on re-proposal** (`match_chunks_to_chapters`, shared by the `from_scenes` preview and `propose_from_scenes`): a re-proposed chunk *is* an existing chapter when the shared scenes are at least half of both sides; an exact half goes to the earlier one in story order — the same rule as the panel gestures (split keeps the first half, merge keeps the previous chapter); chunks claim in story order, most shared scenes first, one claim per chapter; when that rule is silent a **whole-split / whole-merge fallback** gives the identity to the side whose *opening scene coincides* and where one side contains the other (a 7-scene chapter re-cut 2 / 2 / 3 keeps its row on the first piece instead of going to the trash with the author's chapter name and drama card — verified on a copy of the real project: re-cutting 17 scenes at 2 per chapter trashed nothing). **Catalog order** = the confirmed table: `_CatalogPlacement.settle_chapter_order()` (last step of `approve_outline_plan`; per-chapter `display_order` seeding is gone) puts plan chapters in table order, keeps foreign chapters (hand-made, kept leftovers) behind the chapter they followed, and never moves an approved chapter — same rule as `CatalogService.reorder_chapters`; when it would, nothing is reordered, new chapters wait at the end and the result carries `chapter_order_held: true` (the confirm receipt says so). **Hand-made scene cards follow their anchor scene across chapters** (`_follow_after` / `_follow_before`; a chapter with no active planned card keeps its cards). **Runtime rows follow the scene**: `services/scene_rehome.py::rehome_scenes` (called from `approve_outline_plan` and from `resync_materialized_scenes`) updates `chapter_id` on `REHOMED_MODELS` (18 tables incl. `ReviewItem` and the per-scene `ChapterRollingNote`) + scene-scope `ContinuitySnapshot`, rebuilds both chapters' chapter-scope snapshots (`CanonContinuityService.rebuild_chapter_snapshot`) and deletes the snapshot of a chapter left without scenes; `NOT_REHOMED_TABLES` names the deliberate exceptions (`llm_calls` ledger, `chapter_run_jobs`, …) and `tests/test_scene_rehome.py` fails when a new table carrying both `scene_id` and `chapter_id` is in neither list. `trash_emptied_snowflake_chapters` / `_catalog_warnings` identify snowflake-made chapters by `writer_brief_json.source`, not by id shape; revival of a *chapter* from the trash now only happens when a plan's pinned row is trashed (a chapter kept in the table, emptied, then refilled); scene cards trashed together with a chapter follow their own rule (next bullet). (2) **Desk edits written back — deliberately not built; the design has one editor instead.** Writing back would need a second server-side write channel into the 09 / 10 step drafts and the FE snowflake cache whose merge rule is 「本机为准」 (the module that had the wipe incident). Leaf module `services/scene_design_ownership.py`: a card is `owner = plan` when it is snowflake-origin **and** its scene-plan row is still live (else `desk` — hand-made scenes, and snowflake cards whose plan row was deleted but were kept because they hold text). For plan-owned cards `catalog.update_scene` rejects a *real* change to kind / either trio / POV / exit_change / hook with **409 `CATALOG_SCENE_DESIGN_OWNED_BY_PLAN`** (`details.fields`, `author_action` → `snowflake_step:scene_details:<scene_id>`; unchanged whole-card echoes pass; title / state / words are not design), `POST /api/v1/chapters/{id}/scene-order` rejects a change to their relative order with **409 `CATALOG_SCENE_ORDER_OWNED_BY_PLAN`** (hand-made scenes may move anywhere), `sanitize_plan_patch(plan_owned_scene_ids=…)` drops design fills (`design_owned_by_plan`), the planning prompt payload carries `design_owner` per scene (`chapter_scene_plan_fill` v4, `chapter_plan_review` v3 → `sync_prompt_templates --execute` on a live install), and preflight's `SCENE_STRUCTURE_INCOMPLETE` points to the right place. The 阶段 X `desk_edited` machinery (`writer_brief_json.desk_edited_at`, `design.desk_edited`, the `desk_edited` hold reason, `pending_scenes[].desk_edited`) is gone; resync still strips a stray legacy marker. Catalog payload: `design.owner`. React: `ws-author.jsx` rows of plan-owned scenes are read-only (`arrPlanOwned`, `arr-scene-plan-owned`, 「在构思里改 ↗」 `arr-scene-edit-plan`, grip not draggable, kind pill disabled, `editScene` / `cycleKind` strip design keys defensively), the writer outline does not drag them, `SceneDesignCard` and the drafting desk's decision bar (`scene-decide-edit-plan`) choose 「在构思里改」 vs 「编辑卡」 by `owner`. Tests: `tests/test_snowflake_stable_chapter_ids.py`, `tests/test_scene_rehome.py`, `tests/test_migration_0089_chapter_plan_catalog_chapter_id.py`, the ownership cases of `tests/test_catalog_book_spine.py`, the rewritten trash / revive / resync cases of `tests/test_snowflake_chaptering_followups.py`; FE `ws-author.test.jsx`, `ws-writer-spine.test.jsx`, `ws-scene-spine.test.jsx`. **Deploy note**: a dev backend started with `--reload` picks the new model up immediately and answers `schema_revision_mismatch` (and 500s on anything touching chapter plans) until the launcher is restarted (`alembic upgrade head`). Existing projects: chapters whose scenes were scrambled before 阶段 X stay unpinned and mint fresh serials on the next 确认写入 (the old rows are emptied and auto-trashed). Still open: chapter slugs (`chNN`) and the `CH 02 · SC 02` stamps remain positional display numbers; no DB-level unique constraint ties one live chapter plan to one catalog chapter.
- **One chapter table, two doors (阶段 Z, 2026-09-20; contract doc `docs/book-spine-catalog-contract.md` §13)**: the author reported 「章节编排功能和雪花的场景以及整理为章节结构有点孤立」; reproduced on a copy of the real project (4 chapters / 17 scenes). Root causes: (a) 章节编排 could not arrange a snowflake book at all — split / merge / boundary moves / naming lived only in the 构思 panel, with no door from the desk; (b) a chapter renamed at the desk changed the catalog's *own copy* (`narrative_json.title`) while the chapter-plan row (source of the 07 table, the panel and the 09 chapter heads) kept the old name, so 「AI 起章名」 re-named chapters the author had named, and a rename in 07 waited for the next 确认写入; (c) plan chapters could be dragged to another position / 卷 at the desk and were silently put back by the next `settle_chapter_order`; (d) the desk had its own chapter-level model — tension / chapter POV / time / place / entry / exit / threads / align — that **no UI can edit and nothing feeds** (prototype demo-data leftovers): flat 0.3 「故事弧线」, fake OKs (「张力曲线健康」, 「与上一章出口对齐」, 「线索待交接 0 项」), a bogus 「字数超额 1,803/0」 (target 0), an empty 「视角 · 时空」 — while nothing the author built in 构思 (act, disaster, scene range, chapter summary, per-scene POV / time / place / exit_change) showed in the chapter detail. Fix, same principle as 阶段 Y (one owner per fact, reachable where the author is looking): **(1) structure has one editor, two doors** — `WsChapterPlanPanel` opens from 章节编排 (`author-open-plan` in the overview header whenever the gate is ready **or** the catalog already holds plan chapters — a step waiting for re-confirmation must not take the door away; the plan strip / right rail of the chapter detail, the doctor's 「还没起名的章」, `author-empty-open-plan` on an empty catalog; same `SnowSync.materialize` path, receipt toast), the 09 chapter head row is a button (`ws:snow-chapter-plan`), and each panel scene row has `onGoToScene` (step intent then scene intent). Leaf `services/chapter_structure_ownership.py`: a catalog chapter is `structure.owner = plan` when it is snowflake-origin **and** pinned by a live chapter plan → `POST …/catalog/chapter-order` changing the *relative* order of plan chapters is **409 `CATALOG_CHAPTER_ORDER_OWNED_BY_PLAN`** (hand-made chapters may go anywhere), a real `act` change is **409 `CATALOG_CHAPTER_STRUCTURE_OWNED_BY_PLAN`**; FE cards / rail rows are `draggable=false` with a fixed grip; drama card, blueprint, word target, delete stay desk-owned (the delete confirm says the plan's chaptering survives and the next confirm revives the scenes). **(2) one chapter name** — leaf `services/chapter_title_sync.py` (`catalog` must not import `snowflake_chaptering`: that closes a cycle through `trash`, caught by `test_service_architecture.py`; `is_auto_chapter_title` / the 07 mirror `mirror_chapters_into_long_synopsis` moved there and are re-exported): desk rename → `adopt_catalog_title` in the same transaction (plan row, scene-row `chapter_title` stamps, 07 `chapters` + `fe_scaffold.chapters`, the catalog seed `writer_brief_json.chapter_title`; response `plan_title_synced`) and `WsCatalog` then calls the new `SnowSync.adoptServerChapters()` (flushes pending pushes first) because the snowflake cache merges 「本机为准」; 07 save → `follow_plan_titles` at the end of `_sync_chapter_plans` (catalog follows only while it still holds the seeded name); a cleared title on a plan chapter falls back to `第 N 章` and 「未命名」 counts as an auto title. **(3) the plan shows through** — catalog payload: chapter `structure {owner, row_uid, scene_range, planned_scene_count, title_auto}`, scene `design.story_index` (same numbering as the panel's `story_index`, `story_scene_numbers`, one pass per catalog read); pure `ws-author-derive.js` derives entry (first scene) / exit (last scene's `exit_change`), POV / time / place from the scenes (authored chapter fields win, derived values are labelled), `arrBookFacts`, `arrBookSpine`; chapter detail gets the **构思条** (`arr-plan-strip`: 卷 · 灾 · 第 6–12 场 · 还没起名 · summary + doors), honest checks (`三拍已规划`, `场景动笔`, `戏剧卡（可选）`, `字数预算: 未设目标`, `与构思同步`); overview default lens is **结构** (`ws-author-spine.jsx`, 卷 → 章 → 场 with story numbers, hinge ring on the chapter's last scene), 节奏镜头 colours by scene POVs, 故事弧线 / 线索织布机 appear only when a chapter really carries tension / threads (`tensionSet` in `ws-catalog.jsx`), the doctor drops the fake OKs and adds three plan items with doors. Tests: `tests/test_chapter_structure_ownership.py`; FE `ws-author-derive.test.js`, the 阶段 Z cases of `ws-author.test.jsx` / `ws-catalog.test.jsx` / `ws-snow.test.jsx`. Verified in a real browser on a DB copy (desk rename → backend panel, local 07 table and 09 heads show the same name, no push-back). Still open: the dead chapter-level field cluster itself (`narrative_json` tension / pov / time_label / place / entry / exit / align / threads, `ArrTensionCurve`, `ws-author-loom.jsx`, the `_chapter_card_slot` prompt keys) is a subtraction candidate; chapter summary / goal are read-only at the desk; a fresh browser profile echoes two no-op PATCHes of 09 / 10 on hydrate (pre-existing).
- **场景诊断统一 (2026-09-22; contract doc `docs/scene-diagnosis-2026-09-22.md`)**: the author asked whether 文学质量's findings and the writer's 深改诊断 were 「各是各的」. They were, four times over: the 21-dimension rule engine (文学质量 view, recomputed per request), three browser-local regexes in the writer's deep posture (`echo` / `dump` / `rdn`), the LLM `writer_deep_review` node (10 dims × 5 lenses, persisted, **never called by any UI**, with a canned no-LLM fallback keyed on demo-story words), and the same 21 rules re-run inside the pipeline (`final_text_gate` Q3 warnings, the workbench's unread `anti_template_quality_summary`) — three vocabularies, navigation-only handoffs (「去写作台处理」/「直达深改」/「在写作台深改」 dropped the finding), and `wrRequestRewrite` sent the toolbar's free text as `issue_dimension`, so every patch was `local_patch` and the preference profile learned 「润色」. Now **one finding record, one place**: leaf `services/scene_diagnosis.py` assembles `{signal_id, source ∈ rules|craft|review|ai, dimension, label, lens, severity ∈ blocking|revision|taste|info, issue, recommendation, why, evidence{excerpt, paragraph_index, start, end}|null, context, ignored, stale, house_taste, origin, patch}` from the 21 rules (each rule finding now carries `needle` + `anchor`, `literary_quality.rule_signal_id` hashes *what* was matched, Chinese text lives once in `literary_quality.DIMENSION_NOTES`), the three writer rules ported server-side (`craft_findings`; 「段落偏长」 defers to a style binding), the latest near-final acceptance review (`review`) and the latest AI deep review (`ai`), pinned to editor paragraphs by `manuscript_html.manuscript_paragraphs` (same block rule as the editor's `p, blockquote`; `plain_manuscript_text` is the visible text every rule consumer now analyses — the engine used to eat the draft's HTML, so one finding got two ids). `GET /api/v1/scenes/{id}/deep-review` returns that payload (+ `text`, `style_bound`, `summary`, `ai/review {status not_run|current|stale}`, `preferences`, `patch_candidates`, and the old keys); `POST` runs the node **fail-closed** (409 `WRITER_DEEP_REVIEW_LLM_REQUIRED` + `author_action`; old rows are superseded only after the model answered; prompt `writer_deep_review` v5 wants verbatim ≤80-char `evidence_excerpt` and gets the scene's `Scene Structure` / `Scene Design Context` sections; `_diagnose_by_lens` deleted). The ignore list (`SceneCard.deep_review_ignored_keys_json`, unchanged endpoint) now holds signal ids and is honoured everywhere: the 文学质量 overview / chapter-set / analyze-text items drop ignored findings (`ignored_count`, `ignored_findings`, `open_dimensions`, `recommended_next_action.signal_id`), and `final_text_gate._literary` drops the Q3 warning of a dimension whose findings were all ignored (`ignored_rule_dimensions`, `ignored_dimensions` in the audit block). Patches: `POST /api/v1/passages/patch-candidates` takes `instruction` + `issue_note`; `issue_dimension` is a dimension key (or `author_instruction` for a free toolbar rewrite), `candidate_category_for_dimension` knows both vocabularies, the prompt gains `Diagnosed Issue:` / `Author Instruction:` lines. React: `ws-deep.jsx` fetches the diagnosis (`wrDxFetch` / `wrDxRunAi`), marks findings by `paragraph_index` + offsets (`mark.wr-dx.sev-*`, paragraph-level `.wr-dx-para`), and the drawer has an AI 深评 block (fail-closed error → 去系统设置), a `style_bound` notice, source chips, expandable findings (证据 / 改法 / 为什么, 「按诊断改写」 auto-runs the toolbar with the finding's recommendation, 「选中这一句去改写」, 忽略), 「已忽略 N 项」 with per-item 恢复, and the decision log; `useDeepPosture` (`ws-writer-deep-posture.js`) flushes the pending save before fetching, syncs preferences by backend scene id, accepts `ws:writer-posture` detail `{posture:"deep", signal_id}` (文学质量's per-finding 「在写作台看这一处」 and item button, 成稿中心 / 待办 deep links) and selects that finding or reports `handoffMiss`; `WrInlineRewrite` takes `finding` / `onFindingDone` so every rewrite of that selection carries `quality_signal_id / issue_dimension / issue_note / instruction / candidate_category / source_draft_id` (`wrRequestRewrite`). The workbench payload no longer computes `anti_template_quality_summary` on every poll. Deploy: no migration; `sync_prompt_templates --execute` for v5. Tests: `tests/test_scene_diagnosis.py`, rewritten `tests/test_writer_deep_review.py` (fail-closed), `frontend-react/src/ws-writer-deep.test.jsx`, updated `ws-quality.test.jsx`. **Round 2 (same day, 「把没做的部分也做了」; contract doc §7):** (1) craft calibration — under a binding `SceneDiagnosisService.craft_calibration` reads the bound book's paragraph table (cached per (book, count, latest) in-process; 『龙族』 26k paragraphs ≈1.1 s once): 「段落偏长」 threshold = max(170, p95 paragraph length), and adjacent-echo / same-opening are not reported when the author does them at ≥5‰ / ≥10‰ of paragraphs or the profile marks `deliberate_repetition` (`craft_calibration.note` is shown in the drawer's binding notice); (2) 「AI 看这一处」 — `POST /api/v1/scenes/{id}/deep-review/passage` `{signal_id | paragraph_index | excerpt, question?}`, template `writer_passage_review` v1 on the `writer_deep_review` node route, focus paragraph ± 1, verdict `holds|partly|does_not_hold|no_finding` + assessment + focus-only findings + `rewrite_brief`, persisted as rubric `literary_revision_passage_v1` (`lens=passage`, metadata in `contract_field_refs_json`, same-target reruns supersede), merged as `finding.opinion` / `origin.kind=passage` / `passage_reviews[]`; drawer buttons 「AI 看这一处」/「按 AI 的改法改写」/「按 AI 的判断忽略」, deep-toolbar 「AI 看这一段」; (3) 「AI 通读本章」 — `GET/POST /api/v1/chapters/{id}/deep-review` return `chapter_payload` (chapter `ai` status incl. `stale` when any scene draft changed after it, `chapter_findings` that pin to no scene, per-scene `findings_from_chapter` which also appear in that scene's drawer as `origin.kind=chapter`), the assembled chapter text carries 【第 N 场】 markers; 成稿中心 gets a 「诊断」 tab (`ws-manuscripts-diagnosis.jsx`) and 「诊断 N」 chips on chapter rows / structure scene rows; (4) counts — `GET /api/v1/projects/{id}/diagnosis-summary` (`project_summary`), FE store `ws-diagnosis-summary.jsx` (`useDiagnosisSummary`, `announceDiagnosisChanged`, design-sync-style throttling), home chapter cards 「诊断 N」 + spine 「诊断待改 N」. Two real bugs found on the way: the deep review's user message **never contained the text** (PromptBuilder renders only `inline_digests`; the old code put `scene_summary` at the snapshot top level and nobody had ever called the node) — text / structure brief / design context now go in as inline digests, as visible text rather than the draft's HTML; and resolving the binding loaded the whole `profile_json` per scene (0.66 s), so `binding_profile` resolves bindings lightly (binding rows + profile status + `json_extract` of `voice_signature.deliberate_repetition`) and rule / craft findings are cached per (scene, text sha, calibration) — real project 17 scenes: summary 1.4 s cold / 0.43 s warm. **Round 3 (same day, the four leftovers quoted back with 「重构修复优化」; contract doc §8):** (1) **passage review sees the whole scene** — `POST …/deep-review/passage` takes `paragraph_index | excerpt | signal_id | paragraph_start + paragraph_end` (≤40 focus paragraphs); `passage_scope` marks 【焦点段 N】 / 【上下文 N】 and gives every other paragraph as 【第 N 段】 in full (scenes over 12,000 chars abbreviate far paragraphs to their opening, 【第 N 段·略】), the prompt's `## Cross-Paragraph Check` asks for contradictions / repetitions / dropped setups against any other paragraph with `related_excerpt` + `related_paragraph_index` (1-based marker number) + `relation`, and the unified finding carries `related {excerpt, paragraph_index, start, end, kind, label, stale}`; rows record `focus_paragraphs` and supersede on focus overlap; templates `writer_passage_review` v2 / `writer_deep_review` v6 declare the findings member `properties` (a schema-enforcing relay decodes property-less members as `{}`); writer toolbar 「AI 看这几段」 for a multi-block selection, drawer row tag 「与第 N 段矛盾」 + 「看第 N 段」 + `mark.wr-dx.is-related`. (2) **the 21 rules are calibrated to the bound book** — `literary_quality.RuleCalibration` (data) computed by `scene_diagnosis.compute_reference_rules` (units cut at title / scene-break paragraphs, ~2,400-char windows, ≤48 sampled; ending rules measured on ≤48 real unit endings, ≥8 required): a word of the 14 「命中即毛病」 lexicons (`FAULT_LEXICONS`) the author uses ≥1 per 10k chars is waived (`habitual_needles`; still reported when the draft uses it ≥3 times at ≥4× the reference rate), absence lexicons (choice / pressure / cost / ending action) are never calibrated, and a rule that fires on ≥50% of the reference windows is demoted to `info` with `calibrated {kind: dimension_habit, share}` (`habitual_dimensions`); `analyze_literary_quality(text, calibration=)` is byte-identical without one; 文学质量 reads the same calibration through `LiteraryQualityService(rule_calibration_resolver=SceneDiagnosisService.rule_calibration_for_scene)` injected by the route (`literary_quality` cannot import `scene_diagnosis`); payload `craft_calibration.rules` + a longer `note`. On the real 『龙族』 binding: 26 habitual words, 7 habitual dimensions (image_homogeneity / syntax_monotony 100%, repetitive_action 85%, image_field_reuse 81%, choice_pressure 58%, expository_dialogue / over_explained_motive 56%), a 25-paragraph scene went from 5 revision findings to 2 + 3 info. (3) **read-through of only the changed scenes** — the chapter evaluation's `contract_field_refs_json` records `{scope, scenes: [{scene_id, sha256, layer, ref}], reviewed_scene_ids, carried_scene_ids, carried_from}`, chapter `ai.status` is judged by per-scene sha (old rows fall back to timestamps) and reports `changed_scene_ids / changed_count / incremental_available`; `POST …/chapters/{id}/deep-review {scope: "changed"}` sends changed scenes in full (【第 N 场 · 本次通读】) and unchanged ones as opening / ending / previous-findings digests (【第 N 场 · 未改 · 摘要】) plus a `## Read-Through Scope` tail listing the previous chapter-level findings; unchanged scenes' findings are carried over (`carried_from`, same signal ids), a no-change request returns `notice.code = CHAPTER_REVIEW_UP_TO_DATE` without an LLM call, a previous row without shas falls back to `all`; `_chapter_source` now always assembles the scenes' diagnosis texts (no chapter-level author draft / chapter memory). 成稿中心 offers 「只通读改过的 N 场」 + 「整章重新通读」 and marks 「通读后改过」 / 「沿用上次通读」. (4) **counts are pushed** — every write that changes findings returns `diagnosis_rollup {project_id, chapter_id, chapters, scenes}` (the scene's chapter entry + every scene entry of that chapter: author-draft save when the text changed, preferences save, scene deep-review GET/POST, passage review, chapter deep-review GET/POST; `GET /api/v1/scenes/{id}/diagnosis-rollup` for the desk's adopt-current); `WsDiagnosis` fetches the whole book only on mount / work switch, `applyRollup` merges (dropping the chapter's stale scene entries), `applySceneFindings` books ignore / restore locally, totals are summed client-side by the same rule as `summarize_counts`, `ws:diagnosis-changed` carries `rollup` / `findings` (a bare event still refetches), `ws:catalog-changed` only prunes entries that left the catalog — the 20 s throttle is gone; chapter entries gain `chapter_level_blocking`. Tests: `tests/test_scene_diagnosis_round3.py`, updated marker assertions in `test_scene_diagnosis.py`, FE `ws-diagnosis-summary.test.js` (rewritten), `ws-manuscripts-diagnosis.test.jsx`, `ws-writer-deep.test.jsx`. Deploy: no migration; `sync_prompt_templates --execute` on installs with a prompts snapshot. **Wrap-up (same day, contract doc §8.5):** the calibration thresholds are learned, not fixed — a lexicon word is waived per draft when the draft's count is not improbable under the author's measured rate (Poisson tail ≥ 1/10 over a scene-sized expectation of ≥2,400 chars; the same rule flags overuse; `calibrate_lexicons` / `poisson_tail`, `craft_calibration.waived_in_scene` lists what was waived), a dimension is 常态 (`info`) / 常见 (`taste`, never raised) by the 80% Wilson lower bound of its firing share (≥1/2 / ≥1/4; `dimension_level`, 96 sampled windows, ≥4 required), and `voice_signature.deliberate_repetition` makes the repetition family (`RULE_REPETITION_DIMENSIONS`) 常态 outright (`calibrated.kind = profile_deliberate_repetition`); endings come from title paragraphs + symbol lines + the book's recorded `stats_json.scene_breaks` (blank-line breaks) and, when fewer than 4, from paragraphs preceding `transition`-typed ones (`endings_source ∈ units | units+transitions | transitions | none`); background archives push counts too — `GET /api/v1/chapters/{id}/diagnosis-rollup` + `WsDiagnosis.refreshChapter` from the chapter-run poll (`completed_count` grew while live) and `refreshScene` from `scnRun` when the backend auto-archived. Live 『龙族』 after this: 6 常态 + 3 常见 dimensions (choice_pressure at 47% share falls to 常见 under the lower bound), 13 waived words in the probed scene (手 ×9, 光 ×4, …).
- **Drafting preconditions + cards trashed with their chapter (2026-09-20; contract doc `docs/book-spine-catalog-contract.md` §11 / §12)**: two real failures reported the same afternoon the live DB was migrated to 0089. (1) 「AI 起草台任务总是提示被阻断」: `scene_run_preflight` **and** `bundle_builder` (409 `BUNDLE_SOURCE_MISSING`) hard-required an active `VoiceProfile` `VOICE_<pov>` and, for exactly two on-stage characters, a `RelationProfile` `REL_<a>_<b>` — orphans of the knowledge-card system removed in the 2026-09 subtraction. Nothing in the product can author them; the only source was the preflight minting a canned placeholder line (`POST …/preflight/create-cards`, raw character id included) that then reached the prompt as a fact. Every scene of every real project was blocked (the live DB had zero drafts ever), and the one exit button (「补齐声线卡并重试」) was erased by a race: the terminal-job recovery effect in `ws-scene.jsx` overwrote the `startRun` catch's message / `needsCards` with a generic 「任务已阻断…请检查阻断原因后重试」. Now the cards are an **optional** bundle injection (present → `POV Voice` / `Relation Digest` + provenance as before, absent → no section; character voice comes from the `Scene Design Context`, names from `StoryCharacter`), the preflight has no `VOICE_PROFILE_MISSING` / `RELATION_PROFILE_MISSING` / `missing_dependencies` / `create_actions`, and the mint path is deleted (route, `create_missing_cards`, FE `scnCreateCards` / `needsCards`). Remaining preflight blockers: `SCENE_EXECUTION_CONTRACT_BLOCKED` / `_STALE`, `SCENE_CONSTRAINT_CONFLICT`. `scnTerminalJobMessage(job)` (`ws-scene-run.jsx`) is the single sentence both the catch and the recovery effect use, so the author always sees the job's own reason; legacy job rows carrying `VOICE_PROFILE_MISSING` read 「这项检查已经取消，直接重新起草即可」; `preflight_blocked` / `queued` / `blocked` / `cancelled` got readable step labels. The `VoiceProfile` / `RelationProfile` tables and the two optional slots remain (fixtures use them) — subtraction candidate. **The next wall, found the same day on the real cards**: materialization wrote the anti-copy *policy sentence* 「不得复制参考书原文表达、人物、设定或桥段。」 into every scene card's `forbidden_text`, a field whose contract is *literal forbidden terms* (split on 、/，, `A|B` alternatives; shared by hard QC, `quality_classifier` `forbidden_text` = verified **Q1**, and the final-text gate's `continuity:forbidden_text`) — so **`人物`** was a forbidden word and any prose containing 「这号人物」 could not be archived (same class of bug as 阶段 F's `must_include_text`). Materialization no longer writes it (empty unless the plan supplies real terms; a re-materialization clears legacy cards), and `qc_constraints.forbidden_terms` / `strip_reference_policy` are the only way to read the field (literal checks, preflight constraint conflicts, classifier evidence, the execution contract's `must_withhold` fallback, the `Forbidden text:` digest line); the author's real terms — even appended to the legacy sentence — still bind. (2) **Delete the old chapters, then re-chapter**: `trash_chapters` cascades to the chapter's scene cards with the **same `trashed_at`**; after 阶段 Y a re-chaptering mints new chapter ids, and cards only came back when the target chapter was the revived one — so 16 of 17 cards were moved into the new chapters yet stayed in the trash (4 chapters, 1 visible scene). Leaf module `services/catalog_trash_cascade.py`: a planned card whose `trashed_at` equals a trashed chapter's stamp went to the trash *structurally* → `approve_outline_plan` revives it **before** `_CatalogPlacement` (`restored_scene_ids`, event `snowflake_scene_cards_revived`, receipt 「N 场…已取回」; original seq kept when free, else parked high); a card the author trashed **on its own** is a verdict on that scene and stays. The panel says so up front: advisories `catalog_trashed_scenes_return` / `catalog_trashed_scenes_kept` (with `scene_ids`). Cards already stranded by a pre-fix confirm come back on the next confirm while the old chapters are still in the trash. Verified on a copy of the real project (16 cards revived, 17 / 17 scenes pass preflight, a strict scene job reaches the first-draft LLM call against the fake provider). Tests: `tests/test_snowflake_trashed_chapter_revival.py`, the rewritten gate cases in `test_scene_workbench_preflight.py` / `test_scene_run_jobs.py` / `test_fe_scene_run_guards.py` / `test_orchestrator_flow.py`, three 「预检阻断」 cases in `ws-scene-run.test.jsx`, the `policy` cases in `test_qc_constraints.py` / `test_quality_classifier.py` / `test_scene_adopt_archive.py` — all verified red without the fix.
- **Schema build split**: tests build the schema via `Base.metadata.create_all`, but dev/prod build it via Alembic migrations (`auto_create_tables` defaults to `False`). A model change without a matching migration therefore passes CI but 500s at runtime — the `test_metadata_isolation.py` drift guard (see Backend commands above) is the tripwire. Adding a column/index means writing a migration *and* keeping the ORM `__table_args__` in sync.
- **Scene-run orchestration & quality floor**: `services/orchestrator.py` runs a scene end-to-end — bundle context → generate (one candidate unless `NOVEL_SYSTEM_SCENE_BEST_OF_N_ENABLED` is on, then criticality-driven Best-of-N with the blinded author selection gate on critical scenes; under a `style_first` binding the style step decides by fidelity readings — see the Style Reference subsystem) → optional LLM auto-critique → QC gates — scored by `literary_quality.py`'s 21 dimensions and guarded against repetition by `self_repetition.py`. This is the "blueprint quality floor v2" layer on top of raw scene generation.
- **Narrative event sourcing**: `NarrativeEvent` is an append-only log treated as the single source of truth for story state; entity state at a given scene is reconstructed by replaying events up to that point (`narrative_event_log.py`, populated from prose by `prose_event_extractor.py` when event extraction is enabled).
- **Durable LLM accounting & quotas**: every provider POST goes through `llm_accounting.py`'s reservation → dispatch → settlement transactions; daily/monthly/per-project token, request, concurrency, and cost quotas are checked *before* dispatch, and no DB write transaction is held while waiting on the network. Orphaned reservations are reclaimed after a TTL. **Every quota is opt-in and ships disabled** (`0` = no ceiling) — with none armed the pre-dispatch gate short-circuits before running any counting scan. Accounting itself is unconditional: the ledger and the 成本看板 usage readings do not depend on a fence being armed. A provider **usage overage** (actual `total_tokens` above the reservation `input UTF-8 upper bound + max_output_tokens` — thinking relays report reasoning tokens inside `completion_tokens` and do not cap them under `max_tokens`; a 2,000-token classification answer came back with 11,776) blocks delivery with `LLM_USAGE_EXCEEDS_RESERVATION` **only while a fence is armed** (`_usage_overage_is_fenced`: any global quota, or a scene whose budget is armed); unfenced it is settled at actual usage, delivered, logged as a warning and kept as `usage_overage_tokens` in the audit summary (2026-09-15 — before that every 参考书 import batch on such a relay failed with a 500).
- **Startup background recovery**: the FastAPI lifespan runs `run_startup_recovery` to re-submit durable scene/chapter jobs after a crash (and to retire legacy style runs / orphaned classifications), then starts the style-reference job sweeper (`style_reference/jobs.start_job_sweeper`) for the classify / learn / check jobs; persistent leases + worker-owned CAS make duplicate scans harmless. Scene runs are cancellable and checkpointed.
- **Canonical final-text lifecycle**: chapter/project final approval is a deliberate multi-step gate (read-through confirm of server text → body-hash confirm → `approve-final`); the catalog cannot fake `approved`, and reopening needs an auditable reason with server-side cascade revocation of downstream approvals.
- **Local-only network boundary**: the backend defaults to `NOVEL_SYSTEM_LOCAL_ONLY=true` (loopback clients only, forwarded headers rejected); remote access requires an explicit shared token — there is no user auth, RBAC, or tenant isolation.
- **Demo vs. real data**: retired. There is no product demo work and no fake/offline generation — the 「潮汐档案」/「盐镇来信」 seeds and all offline stub clients were removed. Neutral placeholder works survive only as test fixtures (`backend/tests/fixture_works.py`, ids `work-a`/`work-b`). Every generation path is fail-closed: no live LLM → 409/502 + `author_action`, never canned output.
